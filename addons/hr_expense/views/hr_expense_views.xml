<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="hr_expense_view_expenses_analysis_tree" model="ir.ui.view">
        <field name="name">hr.expense.list</field>
        <field name="model">hr.expense</field>
        <field name="arch" type="xml">
            <list
                string="Expenses"
                multi_edit="1"
                sample="1"
                decoration-info="state == 'draft'"
                js_class="hr_expense_tree">
                <field name="is_editable" column_invisible="True" />
                <field name="company_id" column_invisible="True" />
                <field name="company_currency_id" column_invisible="True" />
                <field name="nb_attachment" column_invisible="True" />
                <field name="is_multiple_currency" column_invisible="True" />
                <field name="product_has_cost" column_invisible="True" />
                <field name="selectable_payment_channel_ids" column_invisible="True" />

                <field
                    name="employee_id"
                    widget="many2one_avatar_employee"
                    readonly="not is_editable" />
                <field name="name" readonly="not is_editable" />

                <field name="department_id" optional="hide" readonly="True" />
                <field name="date" optional="show" readonly="not is_editable" />
                <field
                    name="product_id"
                    optional="show"
                    readonly="not is_editable"
                    required="1" />
                <field name="payment_mode" optional="show" readonly="not is_editable" />
                <field name="activity_ids" widget="list_activity" optional="show" />
                <field
                    name="analytic_distribution"
                    widget="analytic_distribution"
                    optional="show"
                    options="{'product_field': 'product_id', 'business_domain': 'expense'}"
                    groups="analytic.group_analytic_accounting"
                    readonly="not is_editable" />
                <field
                    name="account_id"
                    optional="hide"
                    groups="account.group_account_readonly"
                    readonly="not is_editable" />
                <field
                    name="journal_id"
                    optional="hide"
                    groups="account.group_account_readonly"
                    readonly="True" />
                <field
                    name="payment_channel_id"
                    optional="hide"
                    groups="account.group_account_readonly"
                    readonly="not is_editable" />
                <field name="manager_id" optional="hide" readonly="not is_editable" />
                <field
                    name="company_id"
                    optional="show"
                    groups="base.group_multi_company"
                    readonly="True" />
                <field
                    name="price_unit"
                    string="Unit Price"
                    widget="monetary"
                    optional="hide"
                    options="{'currency_field': 'company_currency_id', 'field_digits': True}"
                    readonly="True" />
                <field
                    name="quantity"
                    optional="hide"
                    readonly="not is_editable or not product_has_cost" />
                <field
                    name="tax_ids"
                    widget="many2many_tax_tags"
                    optional="hide"
                    groups="account.group_account_invoice,account.group_account_readonly"
                    readonly="not is_editable or not product_has_cost" />
                <field
                    name="tax_amount"
                    optional="hide"
                    sum="Total Taxes"
                    groups="account.group_account_invoice,account.group_account_readonly"
                    readonly="True" />
                <field
                    name="nb_attachment"
                    widget="nb_attachment"
                    nolabel="1"
                    readonly="True" />
                <field
                    name="total_amount"
                    widget="monetary"
                    sum="Total Amount"
                    options="{'currency_field': 'company_currency_id'}"
                    readonly="not is_editable or product_has_cost"
                    decoration-bf="1" />
                <field
                    name="total_amount_currency"
                    widget="monetary"
                    optional="hide"
                    options="{'currency_field': 'currency_id'}"
                    groups="base.group_multi_currency"
                    readonly="not is_editable or not is_multiple_currency or product_has_cost"
                    decoration-bf="1" />
                <field
                    name="currency_id"
                    optional="hide"
                    groups="base.group_multi_currency"
                    readonly="not is_editable or product_has_cost" />
                <field
                    name="state"
                    widget="badge"
                    optional="show"
                    readonly="True"
                    decoration-danger="state == 'refused'"
                    decoration-info="state == 'draft'"
                    decoration-success="state in ['approved', 'posted', 'in_payment', 'paid']"
                    decoration-warning="state == 'submitted'" />
            </list>
        </field>
    </record>

    <record id="view_expenses_tree" model="ir.ui.view">
        <field name="name">hr.expense.list</field>
        <field name="model">hr.expense</field>
        <field name="inherit_id" ref="hr_expense_view_expenses_analysis_tree" />
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//list" position="attributes">
                <attribute name="js_class">hr_expense_dashboard_tree</attribute>
            </xpath>
        </field>
    </record>

    <record id="view_my_expenses_tree" model="ir.ui.view">
        <field name="name">hr.expense.list</field>
        <field name="model">hr.expense</field>
        <field name="inherit_id" ref="hr_expense.view_expenses_tree" />
        <field name="mode">primary</field>
        <field name="priority">20</field>
        <field name="arch" type="xml">
            <xpath expr="//list" position="attributes">
                <attribute name="js_class">hr_expense_dashboard_tree</attribute>
            </xpath>
        </field>
    </record>

    <record id="hr_expense_view_form" model="ir.ui.view">
        <field name="name">hr.expense.view.form</field>
        <field name="model">hr.expense</field>
        <field name="arch" type="xml">
            <form string="Expenses" js_class="hr_expense_form_view">
            <header>
                <button
                    name="action_submit"
                    type="object"
                    string="Submit"
                    invisible="nb_attachment == 0 or state != 'draft'"
                    class="oe_highlight o_expense_submit_highlight"
                    data-hotkey="v" />
                <button
                    name="action_approve"
                    type="object"
                    string="Approve"
                    context="{'validate_analytic': True}"
                    invisible="nb_attachment == 0 or not can_approve or state != 'submitted'"
                    class="oe_highlight o_expense_approve_highlight"
                    data-hotkey="q" />
                <button
                    name="action_post"
                    type="object"
                    string="Post Journal Entries"
                    groups="account.group_account_invoice"
                    invisible="nb_attachment == 0 or state != 'approved'"
                    class="oe_highlight o_expense_post_highlight"
                    data-hotkey="y" />

                <widget
                    name="attach_document"
                    string="Attach Receipt"
                    invisible="nb_attachment &gt;= 1"
                    action="attach_document"
                    highlight="1" />
                <button
                    name="action_submit"
                    type="object"
                    string="Submit"
                    invisible="nb_attachment &gt;= 1 or state != 'draft'"
                    class="o_expense_submit"
                    data-hotkey="v" />
                <button
                    name="action_approve"
                    type="object"
                    string="Approve"
                    context="{'validate_analytic': True}"
                    invisible="nb_attachment &gt;= 1 or not can_approve or state != 'submitted'"
                    class="o_expense_approve"
                    data-hotkey="q" />
                <button
                    name="action_post"
                    type="object"
                    string="Post Journal Entries"
                    groups="account.group_account_invoice"
                    invisible="nb_attachment  &gt;= 1 or state != 'approved'"
                    class="o_expense_post"
                    data-hotkey="y" />

                <button
                    name="action_refuse"
                    type="object"
                    string="Refuse"
                    groups="hr_expense.group_hr_expense_team_approver"
                    invisible="state not in ('submitted', 'approved')"
                    class="o_expense_refuse"
                    data-hotkey="x" />
                <button
                    name="action_reset"
                    type="object"
                    string="Reset"
                    groups="account.group_account_invoice"
                    invisible="not can_reset or state in 'draft'"
                    data-hotkey="r" />
                <button
                    name="action_reset"
                    type="object"
                    string="Reset"
                    groups="!account.group_account_invoice"
                    invisible="not can_reset or state in ('draft', 'posted', 'in_payment','paid')"
                    data-hotkey="r" />
                <button
                    name="action_split_wizard"
                    type="object"
                    string="Split Expense"
                    groups="!hr_expense.group_hr_expense_team_approver,!account.group_account_invoice"
                    invisible="state != 'draft' or product_has_cost"
                    class="o_expense_split_user" />
                <button
                    name="action_split_wizard"
                    type="object"
                    string="Split Expense"
                    groups="hr_expense.group_hr_expense_team_approver,account.group_account_invoice"
                    invisible="state in ('in_payment', 'paid', 'refused', 'posted') or product_has_cost"
                    class="o_expense_split_manager" />

                <widget
                    name="attach_document"
                    string="Attach Receipt"
                    invisible="nb_attachment == 0"
                    action="attach_document" />

                <field
                    name="state"
                    widget="statusbar"
                    invisible="state in ('submitted', 'in_payment', 'refused')"
                    statusbar_visible="draft,approved,posted,paid" />
                <field
                    name="state"
                    widget="statusbar"
                    invisible="state != 'submitted'"
                    statusbar_visible="draft,submitted,approved,posted,paid" />
                <field
                    name="state"
                    widget="statusbar"
                    invisible="state != 'in_payment'"
                    statusbar_visible="draft,approved,posted,in_payment,paid" />
                <field
                    name="state"
                    widget="statusbar"
                    invisible="state != 'refused'"
                    statusbar_visible="draft,approved,posted,paid,refused" />
            </header>
            <div
                class="alert alert-warning oe_edit_only"
                role="alert"
                name="warning_duplicate_receipt_expense"
                invisible="not same_receipt_expense_ids">
                An <a type="object" name="action_show_same_receipt_expense_ids">expense</a> with the same receipt already exists.
            </div>
            <sheet>
                <div class="oe_button_box" name="button_box">
                    <button
                        name="action_view_split_expense"
                        type="object"
                        string="Split"
                        icon="fa-regular fa-file-lines"
                        invisible="not split_expense_origin_id"
                        class="oe_stat_button" />
                    <button
                        name="action_view_account_move"
                        type="object"
                        icon="fa-bars"
                        groups="account.group_account_invoice"
                        invisible="not account_move_id"
                        class="oe_stat_button">
                        <div class="o_stat_info">
                            <span class="o_stat_text">Journal Entry</span>
                        </div>
                    </button>
                </div>
                <div class="oe_title">
                    <label for="name" />
                    <h1>
                        <field
                            name="name"
                            placeholder="e.g. Lunch with Customer"
                            readonly="not is_editable" />
                    </h1>
                </div>
                <group>
                    <group>
                        <field name="product_has_cost" invisible="1" />
                        <field name="product_has_tax" invisible="1" />
                        <field name="is_multiple_currency" invisible="1" />
                        <field name="is_editable" invisible="1" />
                        <field name="currency_id" invisible="1" />
                        <field name="company_currency_id" invisible="1" />
                        <field name="tax_amount" invisible="1" />
                        <field name="price_unit" invisible="1" />
                        <field name="nb_attachment" invisible="1" />
                        <field name="total_amount" invisible="1" />
                        <field name="duplicate_expense_ids" invisible="1" />
                        <field name="currency_rate" invisible="1" />
                        <field name="selectable_payment_channel_ids" invisible="1" />

                        <label for="product_id" />
                        <div>
                            <field
                                name="product_id"
                                context="{                                         'default_type': 'service',                                         'default_can_be_expensed': 1,                                         'list_view_ref': 'hr_expense.product_product_expense_tree_view',                                         'form_view_ref': 'hr_expense.product_product_expense_form_view',                                     }"
                                readonly="not is_editable"
                                required="1"
                                class="w-100" />
                            <div
                                class="fst-italic"
                                invisible="not is_editable or not product_description or not product_id">
                                <field name="product_description" />
                            </div>
                        </div>

                        <field
                            name="price_unit"
                            widget="monetary"
                            options="{'currency_field': 'currency_id', 'field_digits': True}"
                            invisible="not product_has_cost"
                            readonly="True"
                            required="1" />
                        <label for="quantity" invisible="not product_has_cost" />
                        <div invisible="not product_has_cost">
                            <div class="o_row">
                                <field
                                    name="quantity"
                                    readonly="not is_editable"
                                    class="oe_inline" />
                                <field
                                    name="product_uom_id"
                                    widget="many2one_uom"
                                    force_save="1"
                                    options="{'no_open': True, 'no_create': True}"
                                    groups="uom.group_uom"
                                    readonly="not is_editable"
                                    required="1" />
                            </div>
                        </div>

                        <label
                            for="total_amount_currency"
                            string="Total"
                            invisible="product_has_cost"
                            readonly="not is_editable" />
                        <div class="o_row" invisible="product_has_cost">
                            <field
                                name="total_amount_currency"
                                widget="monetary"
                                options="{'currency_field': 'currency_id'}"
                                readonly="not is_editable"
                                class="oe_inline mw-50 me-0" />
                            <field
                                name="currency_id"
                                options="{'no_create': True, 'no_open': True}"
                                groups="base.group_multi_currency"
                                readonly="not is_editable"
                                class="mw-25 ms-0" />
                            <span class="d-flex" invisible="tax_amount == 0">(incl <field name="tax_amount" class="mx-1" /> tax)</span>
                        </div>

                        <label
                            name="total_amount_product_cost_label"
                            for="total_amount"
                            string="Total"
                            invisible="is_multiple_currency or not product_has_cost" />
                        <label
                            name="total_amount_multicurrency_label"
                            for="total_amount"
                            string=""
                            invisible="not is_multiple_currency" />
                        <div
                            class="o_row"
                            invisible="not is_multiple_currency and not product_has_cost">
                            <field
                                name="total_amount"
                                widget="monetary"
                                force_save="1"
                                options="{'currency_field': 'company_currency_id'}"
                                readonly="not is_editable or product_has_cost"
                                class="oe_inline" />
                            <field name="label_currency_rate" class="ps-0" />
                        </div>

                        <label for="tax_ids" />
                        <div class="o_row">
                            <field
                                name="tax_ids"
                                widget="many2many_tax_tags"
                                force_save="1"
                                options="{'no_create': True}"
                                readonly="not is_editable" />
                            <field name="tax_amount_currency" />
                        </div>
                        <field
                            name="employee_id"
                            widget="many2one_avatar_employee"
                            context="{'default_company_id': company_id}"
                            options="{'no_create': True}"
                            groups="!hr.group_hr_user"
                            readonly="not is_editable" />
                        <field
                            name="employee_id"
                            widget="many2one_avatar_employee"
                            context="{'default_company_id': company_id}"
                            options="{'relation': 'hr.employee'}"
                            groups="hr.group_hr_user"
                            readonly="not is_editable" />
                        <label for="payment_mode" id="lo" />
                        <div id="payment_mode">
                            <field
                                name="payment_mode"
                                widget="radio"
                                readonly="state != 'draft'" />
                        </div>
                    </group>
                    <group>
                        <field name="date" readonly="not is_editable" class="w-50" />
                        <field
                            name="company_id"
                            groups="base.group_multi_company"
                            readonly="state != 'draft'" />
                        <field
                            name="manager_id"
                            placeholder="Auto-validation"
                            widget="many2one_avatar_user"
                            readonly="state != 'draft'" />
                        <field
                            name="department_id"
                            context="{'default_company_id': company_id}"
                            invisible="1"
                            readonly="not is_editable" />
                        <field
                            name="vendor_id"
                            invisible="payment_mode != 'company_account'" />
                        <field
                            name="account_id"
                            domain="[('account_type', 'not in', ('asset_receivable','liability_payable','asset_cash','liability_credit_card')), ('company_ids', 'parent_of', company_id)]"
                            context="{'default_company_id': company_id}"
                            options="{'no_create': True}"
                            groups="account.group_account_readonly"
                            readonly="not is_editable" />
                        <field
                            name="payment_channel_id"
                            options="{'no_open': True, 'no_create': True}"
                            invisible="payment_mode != 'company_account'"
                            readonly="not is_editable"
                            required="payment_mode == 'company_account'" />
                        <field
                            name="analytic_distribution"
                            widget="analytic_distribution"
                            options="{'product_field': 'product_id', 'account_field': 'account_id', 'business_domain': 'expense'}"
                            groups="analytic.group_analytic_accounting"
                            readonly="not is_editable" />
                    </group>
                </group>
                <div>
                    <field
                        name="description"
                        placeholder="Notes..."
                        readonly="not is_editable" />
                </div>
            </sheet>
            <div class="o_attachment_preview o_center_attachment" />
            <chatter />
            </form>
        </field>
    </record>

    <record id="hr_expense_view_form_without_header" model="ir.ui.view">
        <field name="name">hr.expense.view.form</field>
        <field name="model">hr.expense</field>
        <field name="inherit_id" ref="hr_expense.hr_expense_view_form" />
        <field name="mode">primary</field>
        <field name="priority" eval="35" />
        <field name="arch" type="xml">
            <xpath expr="/form/header" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <field name="employee_id" position="attributes">
                <attribute name="readonly">1</attribute>
            </field>
            <field name="company_id" position="attributes">
                <attribute name="readonly">1</attribute>
            </field>
        </field>
    </record>

    <record id="hr_expense_view_expenses_analysis_kanban" model="ir.ui.view">
        <field name="name">hr.expense.kanban</field>
        <field name="model">hr.expense</field>
        <field name="arch" type="xml">
            <kanban
                sample="1"
                class="o_kanban_mobile"
                js_class="hr_expense_kanban"
                quick_create="false">
                <field name="currency_id" />
                <progressbar
                    field="activity_state"
                    colors="{&quot;planned&quot;: &quot;success&quot;, &quot;today&quot;: &quot;warning&quot;, &quot;overdue&quot;: &quot;danger&quot;}" />
                <templates>
                    <t t-name="card">
                        <div class="d-flex align-items-baseline">
                            <field name="name" class="fw-bold fs-5 me-2" />
                            <field
                                name="total_amount_currency"
                                widget="monetary"
                                class="fw-bold ms-auto flex-shrink-0" />
                        </div>
                        <div class="d-flex mt8 text-muted">
                            <field
                                name="employee_id"
                                widget="many2one_avatar_employee"
                                options="{'display_avatar_name': True}"
                                readonly="True" />
                            <field
                                name="state"
                                widget="label_selection"
                                options="{'classes': {                                         'draft': 'default', 'submitted': 'warning', 'refused': 'danger', 'posted': 'warning',                                         'approved': 'success', 'in_payment': 'success', 'paid': 'success'}}"
                                class="ms-auto" />
                        </div>
                        <field name="date" class="mt8 text-muted" />
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="hr_expense_kanban_view_minimal" model="ir.ui.view">
        <field name="name">hr.expense.kanban</field>
        <field name="model">hr.expense</field>
        <field name="inherit_id" ref="hr_expense_view_expenses_analysis_kanban" />
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//templates//field[@name='employee_id']" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
            <xpath expr="//templates//field[@name='state']" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
        </field>
    </record>

    <record id="hr_expense_kanban_view" model="ir.ui.view">
        <field name="name">hr.expense.kanban</field>
        <field name="model">hr.expense</field>
        <field name="inherit_id" ref="hr_expense_view_expenses_analysis_kanban" />
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//kanban" position="attributes">
                <attribute name="js_class">hr_expense_kanban</attribute>
            </xpath>
        </field>
    </record>

    <record id="hr_expense_kanban_view_header" model="ir.ui.view">
        <field name="name">hr.expense.kanban</field>
        <field name="model">hr.expense</field>
        <field name="inherit_id" ref="hr_expense_view_expenses_analysis_kanban" />
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//kanban" position="attributes">
                <attribute name="js_class">hr_expense_dashboard_kanban</attribute>
            </xpath>
        </field>
    </record>

    <record id="hr_expense_view_pivot" model="ir.ui.view">
        <field name="name">hr.expense.pivot</field>
        <field name="model">hr.expense</field>
        <field name="arch" type="xml">
            <pivot string="Expenses Analysis" sample="1">
                <field name="employee_id" type="row" />
                <field name="date" type="col" interval="month" />
                <field name="total_amount_currency" type="measure" />
            </pivot>
        </field>
    </record>

    <record id="hr_expense_view_graph" model="ir.ui.view">
        <field name="name">hr.expense.graph</field>
        <field name="model">hr.expense</field>
        <field name="arch" type="xml">
            <graph string="Expenses Analysis" sample="1">
                <field name="price_unit" invisible="1" />
                <field name="quantity" invisible="1" />
                <field name="date" />
                <field name="employee_id" />
                <field name="total_amount" type="measure" />
                <field name="tax_amount" />
            </graph>
        </field>
    </record>

    <record id="hr_expense_view_search" model="ir.ui.view">
        <field name="name">hr.expense.view.search</field>
        <field name="model">hr.expense</field>
        <field name="arch" type="xml">
            <search string="Expense">
                <field
                    name="name"
                    string="Expense"
                    filter_domain="[('name', 'ilike', self)]" />
                <field
                    name="department_id"
                    filter_domain="[('department_id.name', 'ilike', self)]" />
                <field
                    name="company_id"
                    filter_domain="[('company_id.name', 'ilike', self)]"
                    groups="base.group_multi_company" />
                <field
                    name="employee_id"
                    filter_domain="[('employee_id.name', 'ilike', self)]" />

                <filter
                    name="my_open_expenses"
                    string="My Expenses"
                    domain="[('employee_id.user_id', '=', uid)]" />
                <filter
                    name="my_team_expenses"
                    string="My Team"
                    help="Expenses of Your Team Member"
                    domain="[('employee_id.parent_id.user_id', '=', uid)]"
                    groups="hr_expense.group_hr_expense_team_approver" />
                <separator />
                <filter
                    name="by_company"
                    string="Paid by Company"
                    domain="[('payment_mode', '=', 'company_account')]" />
                <filter
                    name="by_employee"
                    string="Paid by Employee"
                    domain="[('payment_mode', '=', 'own_account')]" />
                <separator />
                <filter
                    name="draft"
                    string="To Submit"
                    domain="[('state', '=', 'draft')]" />
                <filter
                    name="submitted"
                    string="Waiting Approval"
                    domain="[('state', '=', 'submitted')]" />
                <filter
                    name="approved"
                    string="To Post"
                    domain="[('state', '=', 'approved')]" />
                <filter
                    name="to_pay"
                    string="Waiting Reimbursement"
                    domain="[('state', '=', 'approved'), ('payment_mode', '=', 'own_account')]" />
                <separator />
                <filter name="date" string="Expense Date" date="date" />
                <separator />
                <filter
                    name="all_approved"
                    string="All to Post"
                    domain="[('state', '=', 'approved')]"
                    invisible="True" />
                <filter
                    name="all_to_pay"
                    string="All to Pay"
                    domain="[('state', '=', 'posted'), ('payment_mode', '=', 'own_account')]"
                    invisible="True" />
                <filter
                    name="all_paid"
                    string="All Paid"
                    domain="[('state', 'in', ['in_payment', 'paid']), ('payment_mode', '=', 'own_account')]"
                    invisible="True" />

                <filter
                    name="filter_activities_my"
                    string="My Activities"
                    domain="[('activity_user_id', '=', uid)]"
                    invisible="1" />
                <separator invisible="1" />
                <filter
                    name="activities_overdue"
                    string="Late Activities"
                    help="Show all records whose next activity date is past"
                    domain="[('my_activity_date_deadline', '&lt;', 'today')]"
                    invisible="1" />
                <filter
                    name="activities_today"
                    string="Today Activities"
                    domain="[('my_activity_date_deadline', '=', 'today')]"
                    invisible="1" />
                <filter
                    name="activities_upcoming_all"
                    string="Future Activities"
                    domain="[('my_activity_date_deadline', '&gt;', 'today')]"
                    invisible="1" />

                <group name="group_filters">
                    <filter
                        name="employee"
                        string="Employee"
                        context="{'group_by': 'employee_id'}" />
                    <filter
                        name="product"
                        string="Category"
                        context="{'group_by': 'product_id'}" />
                    <filter
                        name="status"
                        string="Status"
                        context="{'group_by': 'state'}" />
                    <filter
                        name="expensesmonth"
                        string="Expense Date"
                        help="Expense Date"
                        context="{'group_by': 'date'}" />
                    <filter
                        name="company"
                        string="Company"
                        context="{'group_by': 'company_id'}"
                        groups="base.group_multi_company" />
                    <filter
                        name="department"
                        string="Department"
                        context="{'group_by': 'department_id'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="hr_expense_view_activity" model="ir.ui.view">
        <field name="name">hr.expense.activity</field>
        <field name="model">hr.expense</field>
        <field name="arch" type="xml">
            <activity string="Expenses">
                <field name="employee_id" />
                <field name="currency_id" />
                <templates>
                    <div t-name="activity-box">
                        <img
                            class="rounded"
                            t-att-src="activity_image('hr.employee', 'avatar_128', record.employee_id.raw_value)"
                            t-att-title="record.employee_id.value"
                            t-att-alt="record.employee_id.value" />
                        <div class="ms-2">
                            <field name="name" display="full" class="o_text_block" />
                            <field
                                name="total_amount_currency"
                                widget="monetary"
                                display="full"
                                muted="1" />
                        </div>
                    </div>
                </templates>
            </activity>
        </field>
    </record>

    <record id="hr_expense_actions_my_all" model="ir.actions.act_window">
        <field name="name">My Expenses</field>
        <field name="res_model">hr.expense</field>
        <field name="path">expenses</field>
        <field name="view_mode">kanban,list,form,graph,pivot,activity</field>
        <field name="search_view_id" ref="hr_expense_view_search" />
        <field name="context">{'search_default_my_open_expenses': 1}</field>
        <field name="help" type="html">
            <div class="o_view_nocontent_expense_receipt o_view_pink_overlay">
                <p class="o_view_nocontent_expense_receipt_image" />
                <h2 class="d-md-block">
                    Upload or drop an expense receipt
                </h2>
            </div>
        </field>
    </record>

    <record id="hr_expense_actions_my_all_tree" model="ir.actions.act_window.view">
        <field name="view_mode">list</field>
        <field name="view_id" ref="view_my_expenses_tree" />
        <field name="act_window_id" ref="hr_expense_actions_my_all" />
    </record>

    <record id="hr_expense_actions_my_all_kanban" model="ir.actions.act_window.view">
        <field name="view_mode">kanban</field>
        <field name="view_id" ref="hr_expense_kanban_view_header" />
        <field name="act_window_id" ref="hr_expense_actions_my_all" />
    </record>

    <record id="hr_expense_view_search_with_panel" model="ir.ui.view">
        <field name="name">hr.expense.view.search.with.panel</field>
        <field name="model">hr.expense</field>
        <field name="inherit_id" ref="hr_expense_view_search" />
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='group_filters']" position="after">
                <searchpanel>
                    <field
                        name="state"
                        icon="fa-regular fa-square-check"
                        enable_counters="1"
                        expand="1"
                        select="multi" />
                    <field
                        name="employee_id"
                        icon="fa-users"
                        limit="20"
                        hierarchize="0"
                        select="one" />
                    <field
                        name="company_id"
                        icon="fa-building"
                        groups="base.group_multi_company"
                        expand="1" />
                </searchpanel>
            </xpath>
        </field>
    </record>

    <record id="hr_expense_actions_to_process" model="ir.actions.act_window">
        <field name="name">Expenses to Process</field>
        <field name="res_model">hr.expense</field>
        <field name="path">expenses-to-process</field>
        <field name="view_mode">list,kanban,form,graph,pivot,activity</field>
        <field name="search_view_id" ref="hr_expense_view_search_with_panel" />
        <field name="context">{'searchpanel_default_state': ['submitted']}</field>
        <field name="help" type="html">
            <div class="o_view_nocontent_expense_receipt o_view_pink_overlay">
                <p class="o_view_nocontent_expense_receipt_image" />
                <h2 class="d-md-block">
                    Upload or drop an expense receipt
                </h2>
            </div>
        </field>
    </record>

    <record id="hr_expense_actions_all" model="ir.actions.act_window">
        <field name="name">Expenses Analysis</field>
        <field name="res_model">hr.expense</field>
        <field name="path">expenses-analysis</field>
        <field name="view_mode">graph,pivot,list,form</field>
        <field name="search_view_id" ref="hr_expense_view_search_with_panel" />
        <field
            name="context">{ 'searchpanel_default_state': ["draft", "submitted", "approve", "posted", "in_payment", "paid"] }</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_empty_folder">
                No data yet!
            </p><p>
                Create new expenses to get statistics.
            </p>
        </field>
    </record>

    <record id="hr_expense_actions_all_graph" model="ir.actions.act_window.view">
        <field name="view_mode">graph</field>
        <field name="view_id" ref="hr_expense.hr_expense_view_graph" />
        <field name="act_window_id" ref="hr_expense_actions_all" />
    </record>

    <record id="hr_expense_actions_all_pivot" model="ir.actions.act_window.view">
        <field name="view_mode">pivot</field>
        <field name="view_id" ref="hr_expense.hr_expense_view_pivot" />
        <field name="act_window_id" ref="hr_expense_actions_all" />
    </record>

    <record id="hr_expense_actions_all_tree" model="ir.actions.act_window.view">
        <field name="view_mode">list</field>
        <field name="view_id" ref="hr_expense_view_expenses_analysis_tree" />
        <field name="act_window_id" ref="hr_expense_actions_all" />
    </record>

    <record id="action_hr_expense_account" model="ir.actions.act_window">
        <field name="name">Employee Expenses</field>
        <field name="res_model">hr.expense</field>
        <field name="path">expenses-employee</field>
        <field name="view_mode">list,kanban,form,pivot,graph</field>
        <field name="view_id" ref="view_expenses_tree" />
        <field name="search_view_id" ref="hr_expense_view_search" />
        <field name="domain">[]</field>
        <field
            name="context">{
                'search_default_all_approved': 1,
                'search_default_all_to_pay': 1,
            }</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new expense
            </p><p>
                Once you have created your expense, submit it to your manager who will validate it.
            </p>
        </field>
    </record>

    <record id="action_hr_expense_department_to_approve" model="ir.actions.act_window">
        <field name="name">Expense to Approve</field>
        <field name="res_model">hr.expense</field>
        <field name="path">expense-to-approve</field>
        <field name="view_mode">list,kanban,form,pivot,graph</field>
        <field name="search_view_id" ref="hr_expense_view_search_with_panel" />
        <field name="domain">[('department_id', '=', active_id)]</field>
        <field name="context">{ 'searchpanel_default_state': ["submitted"] }</field>
    </record>

    <record id="action_hr_expense_department_filtered" model="ir.actions.act_window">
        <field name="name">Expense Analysis</field>
        <field name="res_model">hr.expense</field>
        <field name="view_mode">graph,pivot</field>
        <field
            name="context">{
                'search_default_department_id': [active_id],
                'default_department_id': active_id}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No data yet!
            </p>
        </field>
    </record>

</odoo>
