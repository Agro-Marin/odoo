<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_message_subtype_tree" model="ir.ui.view">
        <field name="name">mail.message.subtype.list</field>
        <field name="model">mail.message.subtype</field>
        <field name="priority">10</field>
        <field name="arch" type="xml">
            <list string="Subtype">
                <field name="sequence" widget="handle" />
                <field name="name" />
                <field name="res_model" />
                <field name="default" />
            </list>
        </field>
    </record>

    <record id="view_mail_message_subtype_form" model="ir.ui.view">
        <field name="name">mail.message.subtype.form</field>
        <field name="model">mail.message.subtype</field>
        <field name="arch" type="xml">
            <form string="Email message">
                <sheet>
                    <group>
                        <group string="Description">
                            <field name="name" />
                            <field name="sequence" />
                            <field name="res_model" />
                            <field name="description" />
                            <field name="default" />
                            <field name="internal" />
                            <field name="hidden" />
                            <field name="track_recipients" />
                        </group>
                        <group string="Auto subscription">
                            <field name="parent_id" />
                            <field name="relation_field" />
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="mail_message_subtype_view_search" model="ir.ui.view">
        <field name="name">mail.message.subtype.view.search</field>
        <field name="model">mail.message.subtype</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
                <field name="res_model" />
                <field name="description" />
                <filter
                    string="Default"
                    name="filter_default"
                    domain="[('default', '=', True)]" />
            </search>
        </field>
    </record>

    <record id="action_view_message_subtype" model="ir.actions.act_window">
        <field name="name">Subtypes</field>
        <field name="res_model">mail.message.subtype</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
