<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- base.user_root is OdooBot itself: it must never be onboarded into a
         chat with itself. -->
    <record id="base.user_root" model="res.users">
        <field name="odoobot_state">disabled</field>
    </record>
</odoo>
