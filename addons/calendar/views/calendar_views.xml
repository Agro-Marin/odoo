<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Calendar Events Types : Views and Actions -->
    <record id="view_calendar_event_type_tree" model="ir.ui.view">
        <field name="name">calendar.event.type</field>
        <field name="model">calendar.event.type</field>
        <field name="arch" type="xml">
            <list string="Meeting Types" editable="bottom" sample="1">
                <field name="name" />
            </list>
        </field>
    </record>

    <record id="action_calendar_event_type" model="ir.actions.act_window">
        <field name="name">Meeting Types</field>
        <field name="res_model">calendar.event.type</field>
        <field name="view_id" ref="view_calendar_event_type_tree" />
    </record>

    <!-- Calendar Alarm : -->
    <record id="view_calendar_alarm_tree" model="ir.ui.view">
        <field name="name">calendar.alarm.list</field>
        <field name="model">calendar.alarm</field>
        <field name="arch" type="xml">
            <list string="Calendar Alarm" sample="1">
                <field name="name" column_invisible="True" />
                <field name="alarm_type" />
                <field name="duration" />
                <field name="interval" />
            </list>
        </field>
    </record>

    <record id="calendar_alarm_view_form" model="ir.ui.view">
        <field name="name">calendar.alarm.form</field>
        <field name="model">calendar.alarm</field>
        <field name="arch" type="xml">
            <form string="Calendar Alarm">
                <sheet>
                    <group>
                        <group name="left_details">
                            <field name="name" invisible="1" />
                            <field name="alarm_type" />
                            <field
                                name="mail_template_id"
                                context="{'default_model': 'calendar.event'}"
                                invisible="alarm_type != 'email'"
                                required="alarm_type == 'email'" />
                            <field
                                name="body"
                                invisible="alarm_type != 'notification'" />
                        </group>
                        <group name="right_details">
                            <label for="duration" />
                            <div class="o_row">
                                <field name="duration" />
                                <field name="interval" />
                            </div>
                            <field name="notify_responsible_available" invisible="1" />
                            <field
                                name="notify_responsible"
                                invisible="not notify_responsible_available" />
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_calendar_alarm" model="ir.actions.act_window">
        <field name="name">Calendar Alarm</field>
        <field name="res_model">calendar.alarm</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_calendar_alarm_tree" />
    </record>

    <!-- Calendar Events : Views and Actions  -->
    <record id="view_calendar_event_tree" model="ir.ui.view">
        <field name="name">calendar.event.list</field>
        <field name="model">calendar.event</field>
        <field name="arch" type="xml">
            <list
                string="Meetings"
                multi_edit="1"
                sample="1"
                js_class="calendar_list_view">
                <header>
                    <button
                        name="action_view_composer"
                        type="object"
                        string="Send Mail"
                        context="{'default_composition_mode':'mass_mail'}" />
                </header>
                <field
                    name="name"
                    string="Subject"
                    readonly="recurrency"
                    decoration-bf="1" />
                <field name="start" string="Start Date" readonly="1" />
                <field name="stop" string="End Date" readonly="1" />
                <field
                    name="user_id"
                    widget="many2one_avatar_user"
                    optional="hide"
                    readonly="recurrency" />
                <field
                    name="partner_ids"
                    widget="many2many_tags"
                    optional="show"
                    readonly="recurrency" />
                <field
                    name="alarm_ids"
                    widget="many2many_tags"
                    optional="hide"
                    readonly="recurrency" />
                <field
                    name="categ_ids"
                    widget="many2many_tags"
                    optional="hide"
                    options="{'color_field': 'color'}"
                    readonly="recurrency" />
                <field name="recurrency" optional="hide" readonly="1" />
                <field name="privacy" optional="hide" readonly="recurrency" />
                <field name="show_as" optional="hide" readonly="recurrency" />
                <field name="location" optional="show" readonly="recurrency" />
                <field name="duration" widget="float_time" readonly="1" />
                <field name="description" optional="hide" readonly="recurrency" />
                <field name="allday" column_invisible="True" />
                <field name="message_needaction" column_invisible="True" />
            </list>
        </field>
    </record>

    <record id="view_calendar_event_form" model="ir.ui.view">
        <field name="name">calendar.event.form</field>
        <field name="model">calendar.event</field>
        <field name="priority" eval="1" />
        <field name="arch" type="xml">
            <form
                string="Meetings"
                class="o_calendar_event_form_view"
                js_class="calendar_form">
                <header>
                    <button
                        name="action_view_composer"
                        type="object"
                        string="Send email"
                        invisible="not user_can_edit"
                        class="btn btn-primary" />
                </header>
                <div
                    invisible="not recurrence_id"
                    class="alert alert-info oe_edit_only"
                    role="status">
                    <p>Edit recurring event</p>
                    <field name="recurrence_update" widget="radio" />
                </div>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            name="action_view_calendar_event"
                            type="object"
                            icon="fa-bars"
                            invisible="not (res_id and res_model_name)">
                            <field name="res_model_name" />
                        </button>
                    </div>
                    <widget
                        name="web_ribbon"
                        title="Archived"
                        invisible="active"
                        bg_color="text-bg-danger" />
                    <field name="active" invisible="1" />
                    <field name="user_can_edit" invisible="1" />
                    <field name="invalid_email_partner_ids" invisible="1"/> <!-- this field will be used in
                        many2many_attendees widget -->
                    <div class="oe_title mb-3">
                        <div>
                            <label for="name" />
                        </div>
                        <h1>
                            <field
                                name="name"
                                placeholder="e.g. Business Lunch"
                                readonly="not user_can_edit" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field
                                name="start_date"
                                widget="daterange"
                                options="{'end_date_field': 'stop_date'}"
                                invisible="not allday"
                                readonly="not user_can_edit"
                                required="allday" />
                            <field
                                name="start"
                                widget="daterange"
                                options="{'end_date_field': 'stop'}"
                                invisible="allday"
                                readonly="not user_can_edit"
                                required="not allday" />
                            <field name="stop_date" invisible="1" />
                            <field name="stop" invisible="1" />
                            <field name="unavailable_partner_ids" invisible="1"/> <!-- this field will be used in many2many_attendees widget -->
                            <label
                                for="duration"
                                class="fw-bold text-900 opacity-100" />
                            <div class="d-flex gap-2">
                                <div
                                    class="d-flex align-items-start"
                                    invisible="allday">
                                    <field
                                        name="duration"
                                        string="Duration"
                                        widget="float_time"
                                        readonly="(id and recurrency) or not user_can_edit"
                                        class="oe_inline pe-2" />
                                    <span> hours</span>
                                </div>
                                <span invisible="allday" style="white-space: pre;"> or </span>
                                <div class="d-flex gap-2">
                                    <label for="allday" class="" />
                                    <field
                                        name="allday"
                                        widget="boolean_toggle"
                                        force_save="1"
                                        options="{'autosave': False}"
                                        readonly="not user_can_edit"
                                        class="oe_inline" />
                                </div>
                            </div>

                            <field
                                name="location"
                                placeholder="Online Meeting"
                                readonly="not user_can_edit" />
                            <label for="videocall_location" class="opacity-100" />
                            <div name="videocall_location_div">
                                <div class="d-flex">
                                    <field
                                        name="videocall_location"
                                        string="Video Link"
                                        widget="CopyClipboardChar"
                                        force_save="1"
                                        readonly="videocall_source == 'discuss' or not user_can_edit"
                                        class="mb-0" />
                                    <div class="d-flex flex-nowrap">
                                        <button
                                            name="clear_videocall_location"
                                            type="object"
                                            context="{'recurrence_update': recurrence_update}"
                                            invisible="not videocall_location or not user_can_edit"
                                            class="btn btn-link btn-lg ms-1 px-1 py-0">
                                            <span
                                                title="Clear meeting"
                                                class="fa-solid fa-times" />
                                        </button>
                                        <button
                                            name="action_join_video_call"
                                            type="object"
                                            help="Join Video Call"
                                            invisible="not videocall_location or not user_can_edit"
                                            class="btn btn-link btn-lg px-1 py-0">
                                            <span
                                                title="Join video call"
                                                class="fa-solid fa-right-to-bracket" />
                                        </button>
                                        <button
                                            name="set_discuss_videocall_location"
                                            type="object"
                                            context="{'recurrence_update': recurrence_update}"
                                            invisible="videocall_location or not user_can_edit"
                                            class="btn btn-link btn-sm px-0 text-nowrap">
                                            <span class="fa-solid fa-plus" /><span> Odoo meeting</span>
                                        </button>
                                    </div>
                                </div>
                            </div>
                            <field
                                name="res_id"
                                string="Linked to"
                                invisible="not res_id"
                                readonly="1" />
                            <field name="res_model" invisible="1" />
                            <field name="videocall_source" invisible="1" />
                            <field name="access_token" force_save="1" invisible="1" />
                        </group>
                        <group>
                            <field
                                name="current_status"
                                string="Going?"
                                widget="selection_badge"
                                options="{'horizontal': true, 'size': 'sm'}"
                                invisible="not should_show_status"
                                required="should_show_status"
                                class="o_calendar_form_status_selection_badge" />
                            <label for="privacy" string="Status" />
                            <div class="d-flex">
                                <field
                                    name="show_as"
                                    nolabel="1"
                                    readonly="not user_can_edit"
                                    class="oe_inline me-2" />
                                <field
                                    name="privacy"
                                    placeholder="User default"
                                    help="Set whether you're available for other simultaneous events and the privacy of this event to others. Manage your default privacy in your user preferences."
                                    nolabel="1"
                                    readonly="not user_can_edit"
                                    class="oe_inline me-2" />
                            </div>
                            <field name="should_show_status" invisible="1" />
                            <div class="d-flex mb-2" colspan="2">
                                <div>
                                    <span class="fw-medium o_form_label">
                                        <field name="attendees_count" nolabel="1" class="w-auto oe_inline" /><span> guests</span>
                                    </span>
                                    <div
                                        invisible="not accepted_count and not tentative_count and not declined_count and not awaiting_count"
                                        class="text-muted fw-normal">
                                        <span
                                            invisible="not accepted_count"
                                            class="me-1">
                                            <field name="accepted_count" nolabel="1" class="w-auto oe_inline" /> Yes
                                        </span>
                                        <span
                                            invisible="not tentative_count"
                                            class="me-1">
                                            <field name="tentative_count" nolabel="1" class="w-auto oe_inline" /> Maybe
                                        </span>
                                        <span
                                            invisible="not declined_count"
                                            class="me-1">
                                            <field name="declined_count" nolabel="1" class="w-auto oe_inline" /> No
                                        </span>
                                        <span
                                            invisible="not awaiting_count"
                                            class="me-1">
                                            <field name="awaiting_count" nolabel="1" class="w-auto oe_inline" /> Awaiting
                                        </span>
                                    </div>
                                </div>
                                <div
                                    name="send_buttons"
                                    class="d-flex gap-2 mb-auto ms-4">
                                    <button
                                        name="action_view_composer"
                                        type="object"
                                        string=" EMAIL"
                                        help="Send Email to attendees"
                                        icon="fa-envelope"
                                        invisible="not user_can_edit" />
                                </div>
                            </div>
                            <div class="d-flex align-items-baseline" colspan="2">
                                <field
                                    name="partner_ids"
                                    placeholder="Select attendees..."
                                    widget="many2manyattendee"
                                    domain="[('type','!=','private')]"
                                    context="{'form_view_ref': 'base.view_partner_simple_form'}"
                                    options="{'no_quick_create': True}"
                                    readonly="not user_can_edit"
                                    class="oe_inline" />
                            </div>
                        </group>
                    </group>
                    <notebook>
                        <page name="notes" string="Notes">
                            <field
                                name="notes"
                                type="html"
                                placeholder="Add notes about this meeting..."
                                options="{'resizable': false}"
                                readonly="not user_can_edit" />
                        </page>
                        <page name="options" string="Options">
                            <group>
                                <group>
                                    <field
                                        name="user_id"
                                        widget="many2one_avatar_user"
                                        readonly="not user_can_edit" />
                                    <field
                                        name="categ_ids"
                                        widget="many2many_tags"
                                        options="{'color_field': 'color', 'no_create_edit': True}"
                                        readonly="not user_can_edit" />
                                    <label
                                        for="description"
                                        string="Calendar description"
                                        colspan="2" />
                                    <field
                                        name="description"
                                        placeholder="Add description"
                                        colspan="2"
                                        nolabel="1"
                                        options="{'height': 120}"
                                        readonly="not user_can_edit"
                                        class="border border-1 rounded" />
                                </group>
                                <group>
                                    <field
                                        name="alarm_ids"
                                        widget="many2many_tags"
                                        options="{'no_quick_create': True}"
                                        readonly="not user_can_edit" />
                                    <field name="recurrence_id" invisible="1" />
                                    <field name="repeat_unit" invisible="1" />
                                    <label for="recurrency" string="Recurrent" />
                                    <div class="d-flex align-items-baseline gap-1">
                                        <field
                                            name="recurrency"
                                            widget="boolean_toggle"
                                            options="{'autosave': False}"
                                            readonly="not user_can_edit"
                                            class="w-auto" />
                                        <span invisible="not recurrency" class="mx-1">Repeat:</span>
                                        <field
                                            name="repeat_unit_ui"
                                            invisible="not recurrency"
                                            readonly="not user_can_edit"
                                            required="recurrency"
                                            class="w-auto o_input" />
                                    </div>
                                    <label
                                        for="repeat_interval"
                                        invisible="not recurrency or repeat_unit_ui != 'custom'"
                                        class="fw-bold text-900" />
                                    <div
                                        class="d-flex gap-1"
                                        invisible="not recurrency or repeat_unit_ui != 'custom'">
                                        <field
                                            name="repeat_interval"
                                            string="Repeat every"
                                            class="oe_inline w-auto o_input_5ch" />
                                        <field
                                            name="repeat_unit"
                                            nolabel="1"
                                            required="repeat_unit_ui == 'custom'"
                                            class="oe_inline" />
                                    </div>
                                    <span class="fw-bold text-nowrap" invisible="repeat_unit_ui not in ['week', 'custom'] or (repeat_unit_ui == 'custom' and repeat_unit != 'week')">Repeat on</span>
                                    <div invisible="repeat_unit_ui not in ['week', 'custom'] or (repeat_unit_ui == 'custom' and repeat_unit != 'week')"><widget name="calendar_week_days" readonly="not user_can_edit" /></div>
                                    <field name="recurrence_id" invisible="1" />

                                    <label
                                        for="month_by"
                                        string="Day of Month"
                                        invisible="repeat_unit_ui not in ['month', 'custom'] or (repeat_unit_ui == 'custom' and repeat_unit != 'month')"
                                        class="fw-bold text-900"
                                        style="width: fit-content;" />
                                    <div
                                        class="d-flex gap-2"
                                        invisible="repeat_unit_ui not in ['month', 'custom'] or (repeat_unit_ui == 'custom' and repeat_unit != 'month')">
                                        <field
                                            name="month_by"
                                            nolabel="1"
                                            required="repeat_unit_ui == 'month' or repeat_unit == 'month'"
                                            class="oe_inline w-auto" />
                                        <field
                                            name="day"
                                            nolabel="1"
                                            invisible="month_by != 'date'"
                                            required="(repeat_unit_ui == 'month' or repeat_unit == 'month') and month_by == 'date'"
                                            class="oe_inline w-auto" />
                                        <field
                                            name="byday"
                                            string="The"
                                            nolabel="1"
                                            invisible="month_by != 'day'"
                                            required="(repeat_unit_ui == 'month' or repeat_unit == 'month') and month_by == 'day'"
                                            class="oe_inline w-auto" />
                                        <field
                                            name="weekday"
                                            nolabel="1"
                                            invisible="month_by != 'day'"
                                            required="(repeat_unit_ui == 'month' or repeat_unit == 'month') and month_by == 'day'"
                                            class="oe_inline w-auto" />
                                    </div>
                                    <label
                                        for="repeat_type"
                                        string="Until"
                                        invisible="not recurrency"
                                        class="fw-bold text-900" />
                                    <div
                                        class="d-flex gap-2"
                                        invisible="not recurrency">
                                        <field
                                            name="repeat_type"
                                            nolabel="1"
                                            readonly="not user_can_edit"
                                            required="recurrency"
                                            class="oe_inline w-auto" />
                                        <field
                                            name="repeat_number"
                                            nolabel="1"
                                            invisible="repeat_type != 'count'"
                                            readonly="not user_can_edit"
                                            required="recurrency"
                                            class="oe_inline w-auto" />
                                        <field
                                            name="repeat_until"
                                            placeholder="e.g: 12/31/2023"
                                            nolabel="1"
                                            invisible="repeat_type != 'until'"
                                            readonly="not user_can_edit"
                                            required="recurrency and repeat_type == 'until'"
                                            class="oe_inline w-auto" />
                                    </div>
                                    <field
                                        name="event_tz"
                                        invisible="not recurrency"
                                        readonly="not user_can_edit" />
                                </group>
                            </group>
                        </page>
                        <page
                            name="page_invitations"
                            string="Invitations"
                            groups="base.group_no_one"
                            invisible="not user_can_edit">
                            <button
                                name="action_sendmail"
                                type="object"
                                string="Send Invitations"
                                icon="fa-envelope"
                                class="oe_link" />
                            <field
                                name="attendee_ids"
                                widget="one2many"
                                mode="list,kanban"
                                readonly="1">
                                <list
                                    string="Invitation details"
                                    editable="top"
                                    create="false"
                                    delete="false">
                                    <field name="partner_id" />
                                    <field name="email" widget="email" />
                                    <field name="phone_ids" widget="many2many_tags" />
                                    <field name="state" />
                                    <button
                                        name="do_tentative"
                                        type="object"
                                        string="Uncertain"
                                        icon="fa-asterisk"
                                        invisible="state not in ('needsAction', 'declined', 'accepted')" />
                                    <button
                                        name="do_accept"
                                        type="object"
                                        string="Accept"
                                        icon="fa-check text-success"
                                        invisible="state not in ('needsAction', 'tentative', 'declined')" />
                                    <button
                                        name="do_decline"
                                        type="object"
                                        string="Decline"
                                        icon="fa-times-circle text-danger"
                                        invisible="state not in ('needsAction', 'tentative', 'accepted')" />
                                </list>
                                <kanban
                                    create="false"
                                    delete="false"
                                    class="o_kanban_mobile">

                                    <templates>
                                        <t t-name="card">
                                            <field name="partner_id" />
                                            <field name="email" widget="email" />
                                            <span>Status: <field name="state" /></span>
                                            <footer class="justify-content-end">
                                                <button
                                                    name="do_tentative"
                                                    type="object"
                                                    string="Uncertain"
                                                    invisible="state not in ('needsAction', 'declined', 'accepted')"
                                                    class="btn fa-solid fa-asterisk" />
                                                <button
                                                    name="do_accept"
                                                    type="object"
                                                    string="Accept"
                                                    invisible="state not in ('needsAction', 'tentative', 'declined')"
                                                    class="btn fa-solid fa-check text-success" />
                                                <button
                                                    name="do_decline"
                                                    type="object"
                                                    string="Decline"
                                                    invisible="state not in ('needsAction', 'tentative', 'accepted')"
                                                    class="btn fa-solid fa-times-circle text-danger" />
                                            </footer>
                                        </t>
                                    </templates>
                                </kanban>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="view_calendar_event_form_quick_create" model="ir.ui.view">
        <field name="name">calendar.event.form.quick_create</field>
        <field name="model">calendar.event</field>
        <field name="priority" eval="2" />
        <field name="arch" type="xml">
            <form
                string="Meetings"
                class="o_calendar_event_form_quick_create"
                js_class="calendar_quick_create_form_view">
                <field name="invalid_email_partner_ids" invisible="1"/> <!--used in many2many_attendees widget-->
                <div class="d-flex align-items-center" colspan="2">
                    <i class="fa-solid fa-tag text-600" title="Booking Name" />
                    <field name="name" placeholder="Add Title" class="w-100 mb-0" />
                </div>
                <div class="d-flex align-items-center" colspan="2">
                    <i class="fa-regular fa-clock text-600" title="Dates" />
                    <!--when "stop" is set manually, "duration" is computed. When "start" is changed
                    "stop" is computed based on the previously-computed "duration". Hence we need to
                    keep the last computed duration in the front-end model and send it with "start"
                    when it changes-->
                    <!--field is also used for custom behavior in default_get, check before removing-->
                    <field name="duration" force_save="1" invisible="1" />
                    <field
                        name="start"
                        placeholder="Dates"
                        widget="daterange"
                        options="{'end_date_field': 'stop'}"
                        invisible="allday"
                        class="w-100 mb-0" />
                    <field
                        name="start_date"
                        placeholder="Dates"
                        widget="daterange"
                        options="{'end_date_field': 'stop_date'}"
                        invisible="not allday"
                        class="w-100 mb-0" />
                    <span class="ms-1 w-25">
                        <field name="allday" title="All Day" class="mb-0" />
                        <span>All day</span>
                    </span>
                    <field name="stop" invisible="1" />
                </div>
                <div class="d-flex" colspan="2">
                    <i class="fa-solid fa-user mt-1 text-600" title="Participants" />
                    <field
                        name="partner_ids"
                        placeholder="Participants"
                        widget="many2many_tags"
                        class="w-75 mb-0" />
                </div>
                <div class="d-flex align-items-center" colspan="2">
                    <i class="fa-solid fa-map text-600" title="Location" />
                    <field name="location" placeholder="Location" class="w-100 mb-0" />
                    <!--fields set via the fake front-end only actions must be force saved (see calendar_form)-->
                    <field name="recurrence_update" invisible="1" />
                    <field name="access_token" force_save="1" invisible="1" />
                    <field name="videocall_location" force_save="1" invisible="1" />
                    <field name="videocall_source" force_save="1" invisible="1" />
                    <button
                        name="set_discuss_videocall_location"
                        type="object"
                        context="{'recurrence_update': recurrence_update}"
                        invisible="videocall_location"
                        class="mx-1 btn-link text-nowrap">
                        <span class="fa-solid fa-plus me-1" />Video
                    </button>
                </div>
                <div
                    class="d-flex align-items-center"
                    colspan="2"
                    invisible="not videocall_location">
                    <i class="fa-solid fa-video text-600" title="Videocall URL" />
                    <field
                        name="videocall_location"
                        widget="CopyClipboardChar"
                        class="w-100 mb-0 o_field_CopyClipboardChar" />
                    <button name="clear_videocall_location" class="btn btn-sm px-0"><i class="fa-solid fa-xmark" title="Remove" /></button>
                </div>
                <div class="d-flex align-items-center" colspan="2">
                    <i class="fa-solid fa-lock text-600" title="Visibility" />
                    <field name="privacy" placeholder="Visibility" class="w-100 mb-0" />
                </div>
                <div class="d-flex" colspan="2">
                    <i class="fa-solid fa-sticky-note mt-2 text-600" title="Notes" />
                    <field
                        name="notes"
                        placeholder="Notes"
                        widget="calendar_event_notes_html"
                        class="w-100" />
                </div>
            </form>
        </field>
    </record>

    <record id="view_calendar_event_calendar" model="ir.ui.view">
        <field name="name">calendar.event.calendar</field>
        <field name="model">calendar.event</field>
        <field name="priority" eval="2" />
        <field name="arch" type="xml">
            <calendar
                string="Meetings"
                all_day="allday"
                color="partner_ids"
                date_delay="duration"
                date_start="start"
                date_stop="stop"
                event_limit="5"
                event_open_popup="true"
                js_class="attendee_calendar"
                quick_create="true"
                quick_create_view_id="%(calendar.view_calendar_event_form_quick_create)d">
                <field name="attendees_count" invisible="1" />
                <field name="accepted_count" invisible="1" />
                <field name="declined_count" invisible="1" />
                <field name="user_can_edit" invisible="1" />
                <field name="is_highlighted" invisible="1" />
                <field name="privacy" invisible="1" />
                <field name="effective_privacy" invisible="1" />
                <!-- For recurrence update Dialog -->
                <field name="recurrency" invisible="1" />
                <field name="recurrence_update" invisible="1" />

                <field
                    name="location"
                    options="{'icon': 'fa-solid fa-location-dot'}"
                    invisible="not location" />
                <field
                    name="partner_ids"
                    widget="many2manyattendeeexpandable"
                    options="{'block': True, 'icon': 'fa-solid fa-users'}"
                    avatar_field="avatar_128"
                    filter_field="partner_checked"
                    filters="1"
                    write_field="partner_id"
                    write_model="calendar.filters" />
                <field
                    name="videocall_location"
                    widget="CopyClipboardURL"
                    options="{'icon': 'fa-solid fa-video fa-lg'}"
                    invisible="not videocall_location"
                    text="Join Video Call" />
                <field
                    name="res_model_name"
                    options="{'icon': 'fa-solid fa-link', 'shouldOpenRecord': true}"
                    invisible="not res_model_name" />
                <field
                    name="alarm_ids"
                    options="{'icon': 'fa-regular fa-bell'}"
                    invisible="not alarm_ids" />
                <field
                    name="categ_ids"
                    options="{'icon': 'fa-solid fa-tag', 'color_field': 'color'}"
                    invisible="not categ_ids" />
                <field
                    name="partner_id"
                    string="Organizer"
                    options="{'icon': 'fa-regular fa-user'}" />
                <field
                    name="description"
                    options="{'icon': 'fa-solid fa-sticky-note'}"
                    invisible="not display_description"
                    class="text-wrap" />
            </calendar>
        </field>
    </record>

    <record id="view_calendar_event_search" model="ir.ui.view">
        <field name="name">calendar.event.search</field>
        <field name="model">calendar.event</field>
        <field name="arch" type="xml">
            <search string="Search Meetings">
                <field
                    name="name"
                    string="Meeting"
                    filter_domain="[('name', 'ilike', self)]" />
                <field name="partner_ids" />
                <field name="user_id" />
                <field name="location" />
                <field name="show_as" />
                <field name="categ_ids" />
                <field name="description" />
                <filter
                    name="mymeetings"
                    string="My Meetings"
                    help="My Meetings"
                    domain="[('partner_ids.user_ids', 'in', [uid])]" />
                <separator />
                <filter name="filter_start_date" string="Date" date="start" />
                <separator />
                <filter name="busy" string="Busy" domain="[('show_as', '=', 'busy')]" />
                <filter name="free" string="Free" domain="[('show_as', '=', 'free')]" />
                <separator />
                <filter
                    name="default"
                    string="Default Privacy"
                    domain="[('privacy', '=', False)]" />
                <filter
                    name="public"
                    string="Public"
                    domain="[('privacy', '=', 'public')]" />
                <filter
                    name="private"
                    string="Private"
                    domain="[('privacy', '=', 'private')]" />
                <filter
                    name="confidential"
                    string="Only Internal Users"
                    domain="[('privacy', '=', 'confidential')]" />
                <separator />
                <filter
                    name="recurrent"
                    string="Recurrent"
                    domain="[('recurrency', '=', True)]" />
                <separator />
                <filter
                    name="inactive"
                    string="Archived"
                    domain="[('active', '=', False)]" />
                <group>
                    <filter
                        name="responsible"
                        string="Responsible"
                        context="{'group_by': 'user_id'}" />
                    <separator />
                    <filter
                        name="group_by_start"
                        string="Date"
                        context="{'group_by': 'start'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="action_calendar_event" model="ir.actions.act_window">
        <field name="name">Meetings</field>
        <field name="res_model">calendar.event</field>
        <field name="path">calendar</field>
        <field name="view_mode">calendar,list,form</field>
        <field name="view_id" ref="view_calendar_event_calendar" />
        <field name="search_view_id" ref="view_calendar_event_search" />
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No meetings found. Let's schedule one!
            </p><p>
                The calendar is shared between employees and fully integrated with
                other applications such as the employee leaves or the business
                opportunities.
            </p>
        </field>
    </record>

    <record id="res_users_view_form" model="ir.ui.view">
        <field name="name">res.users.view.form.inherit.calendar</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form" />
        <field name="arch" type="xml">
            <!-- Placeholder container to hold information about external accounts (Google calendar, Microsoft calendar, ...) -->
            <xpath expr="//page[@name='calendar']" position="inside">
            <group name="calendar_accounts" groups="base.group_system" />
            </xpath>
        </field>
    </record>

    <record id="action_view_calendar_event_calendar" model="ir.actions.act_window.view">
        <field name="sequence" eval="1" />
        <field name="view_mode">calendar</field>
        <field name="view_id" ref="view_calendar_event_calendar" />
        <field name="act_window_id" ref="action_calendar_event" />
    </record>

    <record id="action_view_calendar_event_tree" model="ir.actions.act_window.view">
        <field name="sequence" eval="2" />
        <field name="view_mode">list</field>
        <field name="view_id" ref="view_calendar_event_tree" />
        <field name="act_window_id" ref="action_calendar_event" />
    </record>

    <record id="action_view_calendar_event_form" model="ir.actions.act_window.view">
        <field name="sequence" eval="3" />
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_calendar_event_form" />
        <field name="act_window_id" ref="action_calendar_event" />
    </record>

    <record id="calendar_settings_action" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="context">{'module' : 'calendar', 'bin_size': False}</field>
    </record>

</odoo>
