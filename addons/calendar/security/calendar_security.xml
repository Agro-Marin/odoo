<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="calendar_event_rule_my" model="ir.rule">
            <field name="name">Own events</field>
            <field ref="model_calendar_event" name="model_id"/>
            <field name="domain_force">[('partner_ids', 'in', user.partner_id.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        </record>

        <record id="calendar_event_rule_employee" model="ir.rule">
            <field ref="model_calendar_event" name="model_id"/>
            <field name="name">All Calendar Event for employees</field>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field eval="[(4,ref('base.group_user'))]" name="groups"/>
        </record>

        <record id="calendar_filters_rule_own" model="ir.rule">
            <field name="name">Own calendar filters</field>
            <field ref="model_calendar_filters" name="model_id"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
        </record>

        <record id="calendar_event_rule_private" model="ir.rule">
            <field ref="model_calendar_event" name="model_id"/>
            <field name="name">Private events</field>
            <field name="domain_force">['|', ('privacy', '!=', 'private'), '&amp;', ('privacy', '=', 'private'), '|', ('user_id', '=', user.id), ('partner_ids', 'in', user.partner_id.id)]</field>
            <field name="perm_read" eval="False"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>

    <!-- Deliberately NOT noupdate, so this correction reaches new databases.
         It will NOT reach existing ones: `ir.model.data` stores the noupdate
         flag per record and the stored flag wins, so a record first loaded
         under noupdate is never updated nor removed by an upgrade again.
         Deleting it from the file would have left the old one in place for
         ever, which is worse. The stale copy is inert (the only ACL any module
         grants portal on this model is calendar's own 0,0,0,0), so it is not
         worth a migration script; it is worth not lying in the file. -->
    <data>

        <!-- Was: name "Own attendees", domain [(1, '=', 1)]. It restricted
             nothing, and being attached to base.group_portal, whose ACL on
             this model is 0,0,0,0, it never restricted anybody either. Made to
             mean what it says. The real gate for internal users is
             `calendar.privacy.mixin`, which delegates to the event; an ir.rule
             cannot express it, because the default-privacy branch has to read
             res.users.settings through sudo(). -->
        <record id="calendar_attendee_rule_my" model="ir.rule">
            <field name="name">Own attendees</field>
            <field ref="model_calendar_attendee" name="model_id"/>
            <field name="domain_force">[('partner_id', '=', user.partner_id.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        </record>

    </data>
</odoo>
