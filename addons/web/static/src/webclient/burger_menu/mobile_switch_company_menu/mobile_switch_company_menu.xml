<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

<t t-name="web.MobileSwitchCompanyMenu">
    <div class="o_burger_menu_companies p-3 pb-0">
        <!-- A real disclosure button only when there is something to disclose:
             below the threshold the list is always shown and toggleCollapsible
             is a no-op, so a control there would announce an action it does not
             perform. Above it the list starts COLLAPSED, which made a plain
             clickable div the only way in — unreachable by keyboard. -->
        <button t-if="hasLotsOfCompanies" type="button"
            class="o_burger_menu_companies_toggle w-100 d-flex align-items-center border-0 bg-transparent p-0 text-start text-reset"
            t-att-aria-expanded="state.isOpen ? 'true' : 'false'"
            t-on-click="() => this.toggleCollapsible()">
            <i class="fa-solid me-2" style="width: 10px" t-att-class="{ 'fa-caret-right': !state.isOpen, 'fa-caret-down': state.isOpen }"/>
            <span class="mb-3 fs-4">Companies</span>
        </button>
        <div t-else="" class="w-100">
            <span class="mb-3 fs-4">Companies</span>
        </div>
        <div t-if="show" class="mt-2">
            <t t-call="web.SwitchCompanyMenu.Items"/>
        </div>
    </div>
</t>

</templates>
