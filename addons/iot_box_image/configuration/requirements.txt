# Python dependencies of the IoT box image and of the `iot_handlers/` trees
# shipped by iot_drivers (core) and by l10n_se_pos, pos_blackbox_be,
# pos_iot_adam_scale and quality_iot (enterprise).
#
# iot_handlers/ runs on the IoT box, never inside a server process, and
# iot_drivers itself is `installable: False` — so none of this may be declared
# in a manifest's external_dependencies and none of it belongs in the server's
# requirements.txt, where pyserial, pyusb and pywin32 used to sit.

# The following requirements are needed only on Raspberry Pi IoT:
gatt; sys_platform == "linux" and "rpi" in platform_release
/home/pi/odoo/addons/iot_box_image/configuration/aiortc-1.4.0-py3-none-any.whl; sys_platform == "linux" and "rpi" in platform_release

# The following requirements are needed only on Windows Virtual IoT:
aiortc==1.10.1; sys_platform == "win32"
ghostscript==0.7; sys_platform == "win32"
decorator; sys_platform == "win32"

# The following requirements are needed on every platforms, but are installed
# via apt-get on the Raspberry Pi:
PyKCS11==1.5.16; sys_platform == "win32"
schedule==1.2.1; sys_platform == "win32"

# The following requirements are needed on every platforms:
python-escpos==3.1
pyserial==3.5  # serial_base_driver.py, serial_scale_driver.py, l10n_ke_edi_serial_driver.py
pyusb==1.3.1  # printer/scale USB interfaces
websocket-client==1.9.0

# The box runs a real `odoo-bin` (see overwrite_before_init/.../odoo.service), so
# it needs the server's own dependencies too. Those come from packages.txt via
# apt wherever Debian has them; the ones below have no Debian package at all and
# so can only be pip-installed:
#
#   weasyprint  base/models/ir_actions_report.py imports it at module level, and
#               `base` is loaded before `web`/`iot_drivers` — so without it the
#               box does not boot at all. There is no python3-weasyprint in any
#               Debian suite.
#
# See ../README.md for the apt-vs-pip rule and for the two blockers that stop
# this image running the fork today.
weasyprint==69.0

# Windows Virtual IoT only: printer_driver_W.py / printer_interface_W.py
pywin32==312 ; sys_platform == "win32"

# The following requirements are needed only on Test IoT server:
./addons/iot_box_image/configuration/aiortc-1.4.0-py3-none-any.whl; sys_platform == "linux" and "rpi" not in platform_release
