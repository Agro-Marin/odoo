<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="utm_campaign_view_form" model="ir.ui.view">
        <field name="name">utm.campaign.view.form</field>
        <field name="model">utm.campaign</field>
        <field name="inherit_id" ref="utm.utm_campaign_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button
                    name="action_create_mass_sms"
                    type="object"
                    string="Send SMS"
                    groups="mass_mailing.group_mass_mailing_user"
                    invisible="not is_mailing_campaign_activated"
                    class="oe_highlight" />
            </xpath>
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <button
                    name="action_redirect_to_mailing_sms"
                    type="object"
                    icon="fa-mobile"
                    groups="mass_mailing.group_mass_mailing_user"
                    invisible="mailing_sms_count == 0 or not is_mailing_campaign_activated"
                    class="oe_stat_button order-11">
                    <field name="mailing_sms_count" string="SMS" widget="statinfo" />
                </button>
            </xpath>
            <xpath expr="//sheet" position="inside">
                <field name="ab_testing_mailings_sms_count" invisible="1" />
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page
                    name="sms"
                    string="SMS"
                    groups="mass_mailing.group_mass_mailing_user"
                    invisible="mailing_sms_count == 0 or not is_mailing_campaign_activated">
                    <field name="mailing_sms_ids">
                        <list>
                            <field name="calendar_date" string="Date" />
                            <field name="subject" string="Title" />
                            <field name="mailing_type" column_invisible="True" />
                            <field
                                name="mailing_model_id"
                                string="Recipients"
                                optional="hide" />
                            <field name="user_id" widget="many2one_avatar_user" />
                            <field
                                name="campaign_id"
                                string="Campaign"
                                optional="hide"
                                groups="mass_mailing.group_mass_mailing_campaign" />
                            <field
                                name="ab_testing_enabled"
                                string="A/B Test"
                                groups="mass_mailing.group_mass_mailing_campaign"
                                column_invisible="parent.ab_testing_mailings_sms_count == 0" />
                            <field name="sent" sum="Total Sent" />
                            <field
                                name="clicked"
                                string="Clicked (%)"
                                avg="Average of Clicked" />
                            <field
                                name="bounced"
                                string="Bounced (%)"
                                optional="hide"
                                avg="Average of Bounced" />
                            <field
                                name="state"
                                widget="badge"
                                decoration-info="state in ('draft', 'in_queue')"
                                decoration-success="state in ('sending', 'done')" />
                            <button
                                name="action_duplicate"
                                type="object"
                                string="Duplicate" />
                        </list>
                    </field>
                </page>
            </xpath>
            <xpath expr="//group[@name='ab_test_group']" position="attributes">
                <attribute name="invisible">ab_testing_mailings_sms_count == 0 and ab_testing_mailings_count == 0</attribute>
            </xpath>
        </field>
    </record>

    <record id="utm_campaign_view_kanban" model="ir.ui.view">
        <field name="name">utm.campaign.view.kanban</field>
        <field name="model">utm.campaign</field>
        <field name="inherit_id" ref="utm.utm_campaign_view_kanban" />
        <field name="arch" type="xml">
            <xpath expr="//ul[@id='o_utm_actions']">
                <a
                    name="action_redirect_to_mailing_sms"
                    type="object"
                    t-attf-class="pe-2 oe_mailings #{record.mailing_sms_count.raw_value === 0 ? 'text-muted' : ''}"
                    t-if="record.is_mailing_campaign_activated.raw_value"
                    groups="mass_mailing.group_mass_mailing_user">
                    <field name="mailing_sms_count" /> SMS
                </a>
            </xpath>
        </field>
    </record>

</odoo>
