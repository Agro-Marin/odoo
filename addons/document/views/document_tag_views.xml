<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="tag_view_form" model="ir.ui.view">
        <field name="name">document.tag.form</field>
        <field name="model">document.tag</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_title">
                        <label for="name" string="Tag Name" />
                        <h1>
                            <field name="name" placeholder="e.g. To Validate" />
                        </h1>
                    </div>
                    <group>
                        <field name="color" widget="color_picker" required="True" />
                    </group>
                    <group name="tooltip" string="Tooltip" invisible="1">
                        <field name="tooltip" />
                    </group>
                </sheet>
                <footer>
                    <button special="save" class="btn btn-primary" data-hotkey="q">Save</button>
                    <button special="cancel" class="btn btn-secondary" data-hotkey="x">Discard</button>
                </footer>
            </form>
        </field>
    </record>

    <record id="tag_view_list" model="ir.ui.view">
        <field name="name">document.tag.list</field>
        <field name="model">document.tag</field>
        <field name="arch" type="xml">
            <list default_order="sequence">
                <field name="name" />
                <field name="color" widget="color_picker" required="True" />
                <field name="tooltip" invisible="1" />
            </list>
        </field>
    </record>

    <record id="tag_view_search" model="ir.ui.view">
        <field name="name">tag search</field>
        <field name="model">document.tag</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" string="Tag" />
            </search>
        </field>
    </record>

    <record id="tag_action" model="ir.actions.act_window">
        <field name="name">Tags</field>
        <field name="res_model">document.tag</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'simple_name': True}</field>
        <field
            name="view_ids"
            eval="[Command.clear(), Command.create({'view_mode': 'list', 'view_id': ref('document.tag_view_list')}), Command.create({'view_mode': 'form', 'view_id': ref('document.tag_view_form')})]" />
    </record>

</odoo>
