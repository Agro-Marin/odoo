<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="project_task_view_todo_kanban" model="ir.ui.view">
        <field name="name">project.task.kanban</field>
        <field name="model">project.task</field>
        <field name="priority">800</field>
        <field name="arch" type="xml">
            <kanban
                default_order="state, priority desc, date_end asc, sequence, id desc"
                default_group_by="triage_id"
                sample="1"
                class="o_kanban_small_column"
                highlight_color="color"
                js_class="project_task_kanban"
                on_create="quick_create"
                quick_create_view="project_todo.project_task_view_todo_quick_create_form">
                <field name="color" />
                <field name="sequence" />
                <field name="active" />
                <field name="state" />
                <progressbar
                    field="activity_state"
                    colors="{&quot;planned&quot;: &quot;success&quot;, &quot;today&quot;: &quot;warning&quot;, &quot;overdue&quot;: &quot;danger&quot;}" />
                <templates>
                    <t t-name="menu">
                        <a t-if="widget.editable" role="menuitem" type="set_cover" class="dropdown-item" data-field="displayed_image_id">Set Cover Image</a>
                        <a role="menuitem" type="delete" class="dropdown-item">Delete</a>
                        <field name="color" widget="kanban_color_picker" />
                    </t>
                    <t t-name="card">
                        <t
                            t-set="todoHasAssignees"
                            t-value="record.user_ids.raw_value.length &gt; 1" />
                        <div
                            t-att-class="{'opacity-50': ['done', 'canceled'].includes(record.state.raw_value)}">
                            <field
                                name="name"
                                widget="name_with_subtask_count"
                                class="fw-bold fs-5" />
                            <field
                                name="date_end"
                                widget="remaining_days"
                                t-if="record.date_end.raw_value" />
                            <field
                                name="tag_ids"
                                widget="many2many_tags"
                                options="{'color_field': 'color'}" />
                            <field
                                name="displayed_image_id"
                                widget="attachment_image"
                                t-if="record.displayed_image_id.value" />
                        </div>
                        <div class="d-flex pt-2">
                            <div
                                class="d-flex"
                                t-att-class="{'opacity-50': ['done', 'canceled'].includes(record.state.raw_value)}">
                                <field name="priority" widget="priority" class="me-2" />
                                <field name="activity_ids" widget="kanban_activity" />
                            </div>
                            <div class="d-flex ms-auto">
                                <div
                                    t-att-class="{'opacity-50': ['done', 'canceled'].includes(record.state.raw_value), 'o_todo_hide_avatar': !todoHasAssignees}">
                                    <field
                                        name="user_ids"
                                        widget="many2many_avatar_user"
                                        readonly="True" />
                                </div>
                                <field name="state" widget="todo_done_checkmark" />
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="project_task_view_todo_tree" model="ir.ui.view">
        <field name="name">project.task.todo.list</field>
        <field name="model">project.task</field>
        <field name="arch" type="xml">
            <list
                string="To-dos"
                default_group_by="triage_id"
                editable="bottom"
                multi_edit="1"
                open_form_view="True"
                js_class="todo_list">
                <field
                    name="state"
                    widget="todo_done_checkmark"
                    width="20px"
                    nolabel="1" />
                <field name="name" />
                <field
                    name="user_ids"
                    widget="many2many_avatar_user"
                    optional="show"
                    required="1" />
                <field name="priority" widget="priority" width="70px" optional="hide" />
                <field name="date_end" widget="remaining_days" optional="show" />
                <field name="activity_ids" widget="list_activity" optional="show" />
                <field
                    name="tag_ids"
                    widget="many2many_tags"
                    optional="show"
                    options="{'color_field': 'color', 'no_create_edit': True}" />
                <field name="triage_id" string="Stage" optional="hide" />
            </list>
        </field>
    </record>

    <record id="project_task_view_todo_form" model="ir.ui.view">
        <field name="name">project.task.view.todo.form</field>
        <field name="model">project.task</field>
        <field name="arch" type="xml">
            <form string="To-do" class="o_todo_form_view" js_class="todo_form">
                <field name="html_field_history_metadata" invisible="1" />
                <field name="company_id" invisible="1" />
                <field name="project_id" invisible="1" />
                <header>
                    <field
                        name="triage_id"
                        widget="statusbar"
                        domain="[('user_id','=',uid)]"
                        options="{'clickable': '1', 'fold_field': 'fold'}" />
                    <field name="active" invisible="1" />
                </header>
                <sheet class="o_todo_form_sheet_bg">
                    <widget
                        name="web_ribbon"
                        title="Archived"
                        invisible="active"
                        class="todo_archived"
                        bg_color="text-bg-danger" />
                    <div class="d-flex justify-content-between align-items-center">
                        <h1 class="d-flex w-100">
                            <field
                                name="name"
                                placeholder="Task Title..."
                                widget="text"
                                default_focus="1"
                                options="{'line_breaks': False}"
                                class="o_task_name text-truncate w-md-75 w-100 pe-2" />
                        </h1>
                        <div
                            class="d-flex justify-content-end align-items-center px-1"
                            invisible="not active">
                            <field
                                name="priority"
                                widget="priority_switch"
                                class="h3 pe-2" />
                            <field
                                name="state"
                                widget="todo_done_checkmark"
                                options="{'autosave': False}"
                                class="o_task_state_widget" />
                        </div>
                        <div
                            class="d-flex justify-content-start align-items-center w-100 w-md-50 w-lg-25"
                            invisible="active">
                            <field
                                name="priority"
                                widget="priority_switch"
                                class="h3 pe-2" />
                            <field
                                name="state"
                                widget="todo_done_checkmark"
                                options="{'autosave': False}"
                                class="o_task_state_widget" />
                        </div>
                    </div>
                    <group>
                        <group>
                            <field
                                name="user_ids"
                                placeholder="Assignees"
                                widget="many2many_avatar_user"
                                required="1" />
                        </group>
                        <group>
                            <field
                                name="tag_ids"
                                widget="many2many_tags"
                                options="{'color_field': 'color', 'no_create_edit': True}"
                                class="me-2" />
                            <field
                                name="date_end"
                                decoration-danger="date_end &lt; current_date" />
                        </group>
                    </group>
                    <field
                        name="description"
                        type="html"
                        placeholder="Type Here..."
                        options="{'resizable': false, 'collaborative': true}"
                        class="oe_description" />
                </sheet>
                <widget name="todo_chatter_panel" />
            </form>
        </field>
    </record>

    <record id="project_task_view_todo_quick_create_form" model="ir.ui.view">
        <field name="name">project.task.view.todo.quick.create.todo</field>
        <field name="model">project.task</field>
        <field name="priority">1000</field>
        <field name="arch" type="xml">
            <form class="o_form_project_tasks">
                <group>
                    <field
                        name="display_name"
                        string="To-do Title"
                        placeholder="e.g. Send Invitations"
                        help="Use these keywords in the title to streamline your to-dos:&#10;&#10;                             #tags: Add tags to categorize the to-do&#10;                             @user: Assign the to-do to a specific user&#10;                             !: Mark the to-do as medium priority&#10;                             !!: Mark the to-do as high priority&#10;                             !!!: Mark the to-do as urgent&#10;&#10;                             Ensure you follow the correct format and order. For example: &quot;Send invitations #event @Mitchell !&quot;"
                        required="1" />
                    <field
                        name="date_end"
                        decoration-danger="date_end &lt; current_date" />
                </group>
            </form>
        </field>
    </record>

    <record id="project_task_view_todo_conversion_form" model="ir.ui.view">
        <field name="name">project.task.view.todo.conversion.form</field>
        <field name="model">project.task</field>
        <field name="priority">999</field>
        <field name="arch" type="xml">
            <form string="Convert to Task" js_class="todo_conversion_form">
                <sheet>
                    <group>
                        <field name="company_id" invisible="1" />
                        <field name="project_id" default_focus="1" required="1" />
                        <field
                            name="user_ids"
                            widget="many2many_avatar_user"
                            options="{'no_open': True}"
                            class="o_task_user_field" />
                        <field
                            name="tag_ids"
                            placeholder="Choose tags from the selected project"
                            widget="many2many_tags"
                            context="{'project_id': project_id}"
                            options="{'color_field': 'color', 'no_create_edit': True}" />
                    </group>
                </sheet>
                <footer>
                    <button
                        name="action_convert_to_task"
                        type="object"
                        string="Convert to Task"
                        class="btn-primary" />
                    <button special="cancel" string="Discard" />
                </footer>
            </form>
        </field>
    </record>

    <record id="project_task_view_todo_calendar" model="ir.ui.view">
        <field name="name">project.task.calendar</field>
        <field name="model">project.task</field>
        <field name="arch" type="xml">
            <calendar
                string="To-dos"
                mode="month"
                color="triage_id"
                date_start="date_end"
                event_open_popup="true"
                hide_time="true"
                quick_create="1"
                scales="day,week,month,year"
                show_unusual_days="True">
                <field name="name" />
                <field name="priority" widget="priority" />
                <field
                    name="tag_ids"
                    widget="many2many_tags"
                    options="{'color_field': 'color'}"
                    invisible="not tag_ids" />
                <field
                    name="personal_triage_id"
                    string="Personal Stage"
                    options="{'no_open': True}"
                    invisible="not personal_triage_id" />
            </calendar>
        </field>
    </record>

    <record id="project_task_view_todo_activity" model="ir.ui.view">
        <field name="name">project.task.view.todo.activity</field>
        <field name="model">project.task</field>
        <field name="priority">1000</field>
        <field name="arch" type="xml">
            <activity string="To-dos">
                <field name="user_ids" />
                <templates>
                    <div
                        class="w-100"
                        t-name="activity-box"
                        style="display: inline-grid; grid-template-columns: auto max-content;">
                        <div>
                            <span t-att-title="record.name.value">
                                <field
                                    name="name"
                                    display="full"
                                    class="w-100 o_text_block align-middle" />
                            </span>
                        </div>
                        <field
                            name="user_ids"
                            widget="many2many_avatar_user"
                            readonly="True" />
                    </div>
                </templates>
            </activity>
        </field>
    </record>

    <record id="project_task_view_todo_search" model="ir.ui.view">
        <field name="name">project.task.view.todo.search</field>
        <field name="model">project.task</field>
        <field name="priority">1000</field>
        <field name="arch" type="xml">
            <search string="Todos">
                <field name="name" />
                <field name="tag_ids" />
                <field name="user_ids" />
                <field name="triage_ids" string="Stage" />
                <separator />
                <filter
                    name="open_tasks"
                    string="Open"
                    domain="[('is_closed', '=', False)]" />
                <filter
                    name="closed_tasks"
                    string="Closed"
                    domain="[('is_closed', '=', True)]" />
                <filter
                    name="closed_on"
                    string="Closed On"
                    domain="[('is_closed', '=', True)]"
                    date="date_last_stage_update" />
                <separator />
                <filter
                    name="date_end"
                    string="Deadline"
                    domain="[('date_end', '!=', False)]"
                    date="date_end" />
                <separator />
                <filter
                    name="active_false"
                    string="Archived"
                    domain="[('active', '=', False)]" />
                <filter
                    name="filter_activities_my"
                    string="My Activities"
                    domain="[('activity_user_id', '=', uid)]"
                    invisible="1" />
                <separator invisible="1" />
                <filter
                    name="activities_overdue"
                    string="Late Activities"
                    help="Show all records whose next activity date is past"
                    domain="[('my_activity_date_deadline', '&lt;', 'today')]"
                    invisible="1" />
                <filter
                    name="activities_today"
                    string="Today Activities"
                    domain="[('my_activity_date_deadline', '=', 'today')]"
                    invisible="1" />
                <filter
                    name="activities_upcoming_all"
                    string="Future Activities"
                    domain="[('my_activity_date_deadline', '&gt;', 'today')]"
                    invisible="1" />
                <group>
                    <filter
                        name="groupby_priority"
                        string="Priority"
                        context="{'group_by': 'priority'}" />
                    <filter
                        name="tags"
                        string="Tags"
                        help="By assigned tags"
                        context="{'group_by':'tag_ids'}" />
                    <filter
                        name="user_ids"
                        string="Assignees"
                        context="{'group_by': 'user_ids'}" />
                    <filter
                        name="stage"
                        string="Stage"
                        help="By personal stages"
                        context="{'group_by':'triage_id'}" />
                    <filter
                        name="deadline"
                        string="Deadline"
                        context="{'group_by': 'date_end'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="project_task_action_todo" model="ir.actions.act_window">
        <field name="name">To-dos</field>
        <field name="res_model">project.task</field>
        <field name="path">to-do</field>
        <field name="view_mode">activity,calendar,form,kanban,list</field>
        <field name="search_view_id" ref="project_task_view_todo_search" />
        <field
            name="domain">[('user_ids', 'in', [uid]), ('project_id', '=', False), ('parent_id', '=', False)]</field>
        <field
            name="context">{
            'search_default_open_tasks': 1,
            'list_view_ref': 'project_todo.project_task_view_todo_tree',
            'default_project_id': False,
            'show_todo_mail_helper': True,
            'show_task_options': False,
        }</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No to-do found. Let's create one!
            </p><p>
                Keep your work organized by using memos and to-do lists.
                Your to-do items are private by default, but you can choose to share them with others by adding them as assignees.
            </p>
        </field>
    </record>

    <record
        id="project_task_action_todo_kanban_view"
        model="ir.actions.act_window.view">
        <field name="view_mode">kanban</field>
        <field name="view_id" ref="project_task_view_todo_kanban" />
        <field name="act_window_id" ref="project_task_action_todo" />
    </record>

    <record id="project_task_action_todo_form_view" model="ir.actions.act_window.view">
        <field name="view_mode">form</field>
        <field name="view_id" ref="project_task_view_todo_form" />
        <field name="act_window_id" ref="project_task_action_todo" />
    </record>

    <record id="project_task_action_todo_tree_view" model="ir.actions.act_window.view">
        <field name="view_mode">list</field>
        <field name="view_id" ref="project_task_view_todo_tree" />
        <field name="act_window_id" ref="project_task_action_todo" />
    </record>

    <record
        id="project_task_action_todo_calendar_view"
        model="ir.actions.act_window.view">
        <field name="view_mode">calendar</field>
        <field name="view_id" ref="project_task_view_todo_calendar" />
        <field name="act_window_id" ref="project_task_action_todo" />
    </record>

    <record
        id="project_task_action_todo_activity_view"
        model="ir.actions.act_window.view">
        <field name="view_mode">activity</field>
        <field name="view_id" ref="project_task_view_todo_activity" />
        <field name="act_window_id" ref="project_task_action_todo" />
    </record>

    <record id="project_task_action_convert_todo_to_task" model="ir.actions.act_window">
        <field name="name">Convert to Task</field>
        <field name="res_model">project.task</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'dialog_size': 'medium'}</field>
    </record>

    <record
        id="project_task_action_convert_todo_to_task_form_view"
        model="ir.actions.act_window.view">
        <field name="view_mode">form</field>
        <field name="view_id" ref="project_task_view_todo_conversion_form" />
        <field name="act_window_id" ref="project_task_action_convert_todo_to_task" />
    </record>

</odoo>
