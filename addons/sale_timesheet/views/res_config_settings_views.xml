<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.sale.timesheet</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="hr_timesheet.res_config_settings_view_form" />
        <field name="priority" eval="1" />
        <field name="arch" type="xml">
            <xpath expr="//div[@name='section_leaves']" position="before">
                <block title="Billing">
                    <setting
                        id="time_billing_setting"
                        string="Time Billing"
                        help="Sell services and invoice time spent">
                        <button
                            name="%(sale_timesheet.product_template_action_default_services)d"
                            type="action"
                            string="Configure your services"
                            icon="oi-arrow-right"
                            class="btn-link" />
                    </setting>
                    <setting
                        name="invoice_policy"
                        help="Timesheets taken into account when invoicing your time">
                        <field name="invoice_policy" widget="upgrade_boolean" />
                    </setting>
                </block>
            </xpath>
        </field>
    </record>

</odoo>
