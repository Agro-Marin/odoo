<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_property_form" model="ir.ui.view">
        <field name="name">res.partner.pos.form.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="priority" eval="4" />
        <field name="arch" type="xml">
            <xpath expr="//div[@name='anchor_buttons_pos']" position="before">
                <button
                    class="oe_stat_button"
                    type="object"
                    name="action_view_pos_order"
                    groups="point_of_sale.group_pos_user"
                    context="{'default_partner_id': id}"
                    invisible="pos_order_count == 0"
                    icon="fa-shopping-bag">
                    <field
                        string="PoS Orders"
                        name="pos_order_count"
                        widget="statinfo" />
                </button>
            </xpath>
            <!-- Folded into Sales rather than kept as a one-field column of
                its own: every point-of-sale user on this database also holds
                a sales group, so the field keeps its audience. -->
            <xpath expr="//group[@name='sale']" position="inside">
                <field name="barcode" groups="point_of_sale.group_pos_user" />
            </xpath>
        </field>
    </record>

    <record id="res_partner_action_edit_pos" model="ir.actions.act_window">
        <field name="name">Edit Partner</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="base.view_partner_form" />
        <field name="target">new</field>
    </record>

</odoo>
