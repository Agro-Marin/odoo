<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_stock_scrap_search" model="ir.ui.view">
        <field name="name">stock.scrap.search</field>
        <field name="model">stock.scrap</field>
        <field name="arch" type="xml">
            <search string="Search Scrap">
                <field name="name" string="Reference" />
                <field name="product_id" />
                <field name="location_id" />
                <field name="scrap_location_id" />
                <field name="create_date" />
                <group>
                    <filter
                        name="product"
                        string="Product"
                        context="{'group_by':'product_id'}" />
                    <filter
                        name="location"
                        string="Location"
                        context="{'group_by':'location_id'}" />
                    <filter
                        name="scrap_location"
                        string="Scrap Location"
                        context="{'group_by':'scrap_location_id'}" />
                    <filter
                        name="transfer"
                        string="Transfer"
                        context="{'group_by':'picking_id'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="view_stock_scrap_list" model="ir.ui.view">
        <field name="name">stock.scrap.list</field>
        <field name="model">stock.scrap</field>
        <field name="arch" type="xml">
            <list multi_edit="1" sample="1" decoration-info="state == 'draft'">
                <field
                    name="company_id"
                    column_invisible="True"
                    readonly="state == 'done'" />
                <field name="name" decoration-bf="1" />
                <field name="date_done" />
                <field name="product_id" readonly="1" />
                <field name="scrap_qty" readonly="state == 'done'" />
                <field
                    name="product_uom_id"
                    widget="many2one_uom"
                    groups="uom.group_uom"
                    readonly="state == 'done'" />
                <field
                    name="location_id"
                    options="{'no_create': True}"
                    groups="stock.group_stock_multi_locations"
                    readonly="state == 'done'" />
                <field
                    name="scrap_location_id"
                    options="{'no_create': True}"
                    groups="stock.group_stock_multi_locations"
                    readonly="state == 'done'" />
                <field
                    name="company_id"
                    groups="base.group_multi_company"
                    readonly="1" />
                <field
                    name="state"
                    widget="badge"
                    decoration-muted="state == 'draft'"
                    decoration-success="state == 'done'" />
            </list>
        </field>
    </record>

    <record id="view_stock_scrap_kanban" model="ir.ui.view">
        <field name="name">stock.scrap.kanban</field>
        <field name="model">stock.scrap</field>
        <field name="arch" type="xml">
            <kanban sample="1" class="o_kanban_mobile">
                <templates>
                    <t t-name="card">
                        <div class="row">
                            <field name="name" class="col-6 fw-bolder" />
                            <div class="col-6 fw-bolder text-end">
                                <i
                                    class="fa-regular fa-clock"
                                    role="img"
                                    aria-label="Date"
                                    title="Date" />
                                <field name="date_done" />
                            </div>
                        </div>
                        <field name="product_id" />
                        <div class="row">
                            <field name="scrap_qty" class="col-6" />
                            <div class="col-6">
                                <field
                                    name="state"
                                    class="float-end badge text-bg-secondary" />
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_stock_scrap_form" model="ir.ui.view">
        <field name="name">stock.scrap.form</field>
        <field name="model">stock.scrap</field>
        <field name="arch" type="xml">
            <form string="Scrap">
                <header>
                    <button
                        name="action_validate"
                        type="object"
                        string="Validate"
                        context="{'not_unlink_on_discard': True}"
                        invisible="state != 'draft'"
                        class="oe_highlight"
                        data-hotkey="v" />
                    <field
                        name="state"
                        widget="statusbar"
                        statusbar_visible="draft,done" />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            name="action_get_stock_picking"
                            type="object"
                            string="Stock Operation"
                            icon="fa-cogs"
                            invisible="not picking_id"
                            class="oe_stat_button" />
                        <field
                            name="picking_id"
                            invisible="1"
                            readonly="state == 'done'" />
                        <button
                            name="action_get_stock_move_lines"
                            type="object"
                            icon="fa-exchange"
                            invisible="not move_ids"
                            class="oe_stat_button">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Product Moves</span>
                            </div>
                        </button>
                        <field name="move_ids" invisible="1" />
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field
                                name="product_id"
                                context="{'default_is_storable': True}"
                                readonly="state == 'done'" />
                            <field name="tracking" invisible="1" />
                            <label for="scrap_qty" />
                            <div class="o_row">
                                <field
                                    name="scrap_qty"
                                    readonly="state == 'done' or tracking == 'serial'" />
                                <field
                                    name="product_uom_id"
                                    widget="many2one_uom"
                                    force_save="1"
                                    groups="uom.group_uom"
                                    readonly="state == 'done' or tracking == 'serial'" />
                            </div>
                            <field
                                name="lot_id"
                                context="{'default_product_id': product_id}"
                                groups="stock.group_production_lot"
                                invisible="not product_id or tracking == 'none'"
                                readonly="state == 'done'"
                                required="tracking != 'none'" />
                            <field name="should_replenish" readonly="state == 'done'" />
                            <field
                                name="scrap_reason_tag_ids"
                                widget="many2many_tags"
                                options="{'no_create_edit': True}" />
                        </group>
                        <group>
                            <field
                                name="company_id"
                                invisible="1"
                                readonly="state == 'done'" />
                            <field
                                name="package_id"
                                groups="stock.group_tracking_lot"
                                readonly="state == 'done'" />
                            <field
                                name="owner_id"
                                groups="stock.group_tracking_owner"
                                readonly="state == 'done'" />
                            <field
                                name="location_id"
                                force_save="1"
                                options="{'no_create': True, 'no_open': True}"
                                groups="stock.group_stock_multi_locations"
                                readonly="state == 'done'" />
                            <field
                                name="scrap_location_id"
                                force_save="1"
                                options="{'no_create': True, 'no_open': True}"
                                groups="stock.group_stock_multi_locations"
                                readonly="state == 'done'" />
                            <field name="origin" />
                            <field name="date_done" invisible="state == 'draft'" />
                            <field
                                name="picking_id"
                                invisible="not picking_id"
                                readonly="state == 'done'" />
                            <field
                                name="company_id"
                                groups="base.group_multi_company"
                                readonly="state == 'done'" />
                        </group>
                    </group>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="view_stock_scrap_form2" model="ir.ui.view">
        <field name="name">stock.scrap.form2</field>
        <field name="model">stock.scrap</field>
        <field name="arch" type="xml">
            <form string="Scrap">
                <group>
                    <group>
                        <field name="state" invisible="1" />
                        <field
                            name="product_id"
                            domain="[('id', 'in', context.get('product_ids', []))]"
                            options="{'no_create': True}"
                            readonly="state == 'done'" />
                        <field name="tracking" invisible="1" />
                        <label for="scrap_qty" />
                        <div class="o_row">
                            <field name="scrap_qty" readonly="tracking == 'serial'" />
                            <field
                                name="product_uom_id"
                                widget="many2one_uom"
                                groups="uom.group_uom"
                                readonly="tracking == 'serial'" />
                        </div>
                        <field
                            name="lot_id"
                            context="{'default_product_id': product_id}"
                            groups="stock.group_production_lot"
                            invisible="not product_id or tracking == 'none'"
                            readonly="state == 'done'"
                            required="tracking != 'none'" />
                        <field
                            name="scrap_reason_tag_ids"
                            widget="many2many_tags"
                            options="{'no_create_edit': True}" />
                    </group>
                    <group>
                        <field
                            name="picking_id"
                            invisible="1"
                            readonly="state == 'done'" />
                        <field
                            name="package_id"
                            groups="stock.group_tracking_lot"
                            readonly="state == 'done'" />
                        <field
                            name="owner_id"
                            groups="stock.group_tracking_owner"
                            readonly="state == 'done'" />
                        <field
                            name="company_id"
                            invisible="1"
                            readonly="state == 'done'" />
                        <field
                            name="location_id"
                            options="{'no_open': True, 'no_create': True}"
                            groups="stock.group_stock_multi_locations"
                            readonly="state == 'done'" />
                        <field
                            name="scrap_location_id"
                            options="{'no_open': True, 'no_create': True}"
                            groups="stock.group_stock_multi_locations"
                            readonly="state == 'done'" />
                        <field name="should_replenish" readonly="state == 'done'" />
                    </group>
                </group>
                <footer>
                    <button
                        name="action_validate"
                        type="object"
                        string="Scrap Products"
                        invisible="state != 'draft'"
                        class="btn-primary"
                        data-hotkey="q" />
                    <button
                        special="cancel"
                        string="Discard"
                        class="btn-secondary"
                        data-hotkey="x" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_stock_scrap" model="ir.actions.act_window">
        <field name="name">Scrap Orders</field>
        <field name="res_model">stock.scrap</field>
        <field name="path">scraps</field>
        <field name="view_mode">list,form,kanban,pivot,graph</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Scrap products
            </p>
            <p>
                Scrapping a product will remove it from your stock. The product will
                end up in a scrap location that can be used for reporting purpose.
            </p>
        </field>
    </record>

</odoo>
