<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record model="res.groups.privilege" id="res_groups_privilege_project">
        <field name="name">Project</field>
        <field name="sequence">3</field>
        <field name="category_id" ref="base.module_category_services"/>
    </record>

    <record id="group_project_user" model="res.groups">
        <field name="name">User</field>
        <field name="comment">User can user the your employees' schedule</field>
        <field name="sequence">10</field>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        <field name="privilege_id" ref="res_groups_privilege_project"/>
    </record>

    <record id="group_project_manager" model="res.groups">
        <field name="name">Administrator</field>
        <field name="comment">Administrator can manage the employees' schedule</field>
        <field name="sequence">20</field>
        <field name="privilege_id" ref="res_groups_privilege_project"/>
        <field name="implied_ids" eval="[(4, ref('project.group_project_user')), (4, ref('mail.group_mail_canned_response_admin'))]"/>
        <field name="user_ids" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
    </record>

    <record id="group_project_stages" model="res.groups">
        <field name="name">Use Stages on Project</field>
    </record>

    <record id="group_project_recurring_tasks" model="res.groups">
        <field name="name">Use Recurring Tasks</field>
    </record>

    <record id="group_project_task_dependencies" model="res.groups">
        <field name="name">Use Task Dependencies</field>
    </record>

    <record id="group_project_milestone" model="res.groups">
        <field name="name">Use Milestones</field>
    </record>

<data noupdate="1">

    <record model="ir.rule" id="project_comp_rule">
        <field name="name">Project: multi-company</field>
        <field name="model_id" ref="model_project_project"/>
        <field name="domain_force">[('company_id', 'in', company_ids + [False])]</field>
    </record>

    <record model="ir.rule" id="project_phase_rule">
        <field name="name">Project Phase: multi-company</field>
        <field name="model_id" ref="model_project_phase"/>
        <field name="domain_force">[('company_id', 'in', company_ids + [False])]</field>
    </record>

    <record model="ir.rule" id="project_project_manager_rule">
        <field name="name">Project: project manager: see all</field>
        <field name="model_id" ref="model_project_project"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <record model="ir.rule" id="project_public_members_rule">
        <field name="name">Project: employees: following required for follower-only projects</field>
        <field name="model_id" ref="model_project_project"/>
        <field name="domain_force">['|',
                                        ('privacy_visibility', 'in', ['employees', 'portal']),
                                        ('message_partner_ids', 'in', [user.partner_id.id])
                                    ]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <record model="ir.rule" id="task_comp_rule">
        <field name="name">Project/Task: multi-company</field>
        <field name="model_id" ref="model_project_task"/>
        <field name="domain_force">[('company_id', 'in', company_ids + [False])]</field>
    </record>

    <record model="ir.rule" id="task_visibility_rule">
        <field name="name">Project/Task: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_task"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
        <field name="domain_force">[
            '|',
                '&amp;',
                    ('project_id', '!=', False),
                    '|',
                        ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
                        ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                '|',
                    ('message_partner_ids', 'in', [user.partner_id.id]),
                    # to subscribe check access to the record, follower is not enough at creation
                    ('user_ids', 'in', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('base.group_user'))]"/>
    </record>

    <record model="ir.rule" id="project_manager_all_project_tasks_rule">
        <field name="name">Project/Task: project manager: see all tasks linked to a project or its own tasks</field>
        <field name="model_id" ref="model_project_task"/>
        <field name="domain_force">[
            '|', ('project_id', '!=', False),
                 ('user_ids', 'in', user.id),
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <record model="ir.rule" id="workflow_step_manager_rule">
        <field name="name">Workflow Step: manager sees all</field>
        <field name="model_id" ref="model_project_workflow_step"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <record model="ir.rule" id="triage_visibility_rule">
        <field name="name">Triage: see own buckets</field>
        <field name="model_id" ref="model_project_triage"/>
        <field name="domain_force">[('user_id', 'in', (False, user.id))]</field>
    </record>

    <record model="ir.rule" id="triage_own_write_rule">
        <field name="name">Triage: write own buckets</field>
        <field name="model_id" ref="model_project_triage"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="perm_read" eval="False"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="report_project_task_user_report_comp_rule">
        <field name="name">Task Analysis multi-company</field>
        <field name="model_id" ref="model_report_project_task_user"/>
        <field name="domain_force">[('company_id', 'in', company_ids + [False])]</field>
    </record>

    <record id="ir_rule_project_task_triage_my" model="ir.rule">
        <field name="name">Project: See my own task triage</field>
        <field name="model_id" ref="project.model_project_task_triage"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
    </record>

    <record model="ir.rule" id="task_visibility_rule_project_user">
        <field name="name">Project/Task: project users: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_task"/>
        <field name="perm_read" eval="False"/>
        <field name="domain_force">[
            '|',
                '&amp;',
                    ('project_id', '!=', False),
                    '|',
                        ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
                        ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                '|',
                    ('message_partner_ids', 'in', [user.partner_id.id]),
                    # to subscribe check access to the record, follower is not enough at creation
                    ('user_ids', 'in', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record id="ir_rule_private_task" model="ir.rule">
        <field name="name">Project: See private tasks</field>
        <field name="model_id" ref="project.model_project_task"/>
        <field name="domain_force">[
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|', '|', ('project_id', '!=', False),
                      ('parent_id', '!=', False),
                 ('user_ids', 'in', user.id),
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

     <!-- Portal -->
    <record id="project_project_rule_portal" model="ir.rule">
        <field name="name">Project: portal users: portal and following</field>
        <field name="model_id" ref="project.model_project_project"/>
        <field name="domain_force">[
            '&amp;',
                ('privacy_visibility', 'in', ['invited_users', 'portal']),
                ('message_partner_ids', 'child_of', [user.partner_id.commercial_partner_id.id]),
        ]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
    </record>

    <record id="project_collaborator_rule_portal" model="ir.rule">
        <field name="name">Project/Collaborator: portal users: can only see his own collobaroration in shared projects</field>
        <field name="model_id" ref="project.model_project_collaborator"/>
        <field name="domain_force">[
            ('project_id.privacy_visibility', 'in', ['invited_users', 'portal']),
            ('partner_id', '=', user.partner_id.id),
        ]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
    </record>

    <record id="project_task_rule_portal" model="ir.rule">
        <field name="name">Project/Task: portal users: can only see a task if he's a collaborator of the project and a follower of the task</field>
        <field name="model_id" ref="project.model_project_task"/>
        <field name="domain_force">[
            ('project_id.privacy_visibility', 'in', ['invited_users', 'portal']),
            ('active', '=', True),
            '|',
                ('message_partner_ids', 'child_of', [user.partner_id.commercial_partner_id.id]),
                ('project_id.collaborator_ids', 'any', [
                    ('partner_id', '=', user.partner_id.id),
                    ('limited_access', '=', False),
                ]),
        ]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
    </record>

    <record id="project_task_rule_portal_project_sharing" model="ir.rule">
        <field name="name">Project/Task: portal users: portal user can edit with project sharing feature</field>
        <field name="model_id" ref="project.model_project_task"/>
        <field name="active">0</field>
        <field name="domain_force">[
            ('project_id.privacy_visibility', 'in', ['invited_users', 'portal']),
            ('active', '=', True),
            '|',
                '&amp;',
                    ('message_partner_ids', 'child_of', [user.partner_id.commercial_partner_id.id]),
                    ('project_id.collaborator_ids.partner_id', 'in', [user.partner_id.id]),
                ('project_id.collaborator_ids', 'any', [
                    ('partner_id', '=', user.partner_id.id),
                    ('limited_access', '=', False),
                ]),
        ]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="False"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <record model="ir.rule" id="update_comp_rule">
        <field name="name">Project/Updates: multi-company</field>
        <field name="model_id" ref="model_project_update"/>
        <field name="domain_force">['|', ('project_id.company_id', 'in', company_ids), ('project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="update_visibility_rule">
        <field name="name">Project/Update: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_update"/>
        <field name="domain_force">[
        '|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                '|',
                    ('user_id', '=', user.id),
                    ('project_id.user_id', '=', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('base.group_user'))]"/>
    </record>

    <record model="ir.rule" id="report_project_task_user_rule">
        <field name="name">Tasks Analysis: project visibility User</field>
        <field name="model_id" ref="model_report_project_task_user"/>
        <field name="domain_force">[
        '|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                '|',
                    ('task_id.message_partner_ids', 'in', [user.partner_id.id]),
                    ('user_ids', 'in', user.id),
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="report_project_task_manager_rule">
        <field name="name">Tasks Analysis: project visibility Manager</field>
        <field name="model_id" ref="model_report_project_task_user"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <record id="update_visibility_project_admin" model="ir.rule">
        <field name="name">Project updates : Project user can see all project updates</field>
        <field name="model_id" ref="project.model_project_update"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <record model="ir.rule" id="burndown_chart_project_user_rule">
        <field name="name">Burndown chart: project visibility User</field>
        <field name="model_id" ref="model_project_task_burndown_chart_report"/>
        <field name="domain_force">[
        '|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('user_ids', 'in', user.id),
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="burndown_chart_project_manager_rule">
        <field name="name">Burndown chart: project visibility User</field>
        <field name="model_id" ref="model_project_task_burndown_chart_report"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <record model="ir.rule" id="milestone_comp_rule">
        <field name="name">Project/Milestone: multi-company</field>
        <field name="model_id" ref="model_project_milestone"/>
        <field name="domain_force">['|', ('project_id.company_id', 'in', company_ids), ('project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="milestone_visibility_rule">
        <field name="name">Project/Milestone: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_milestone"/>
        <field name="domain_force">[
        '|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('project_id.user_id', '=', user.id),
        ]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <record id="milestone_visibility_project_admin" model="ir.rule">
        <field name="name">Project/Milestone: Project manager can see all project milestones</field>
        <field name="model_id" ref="project.model_project_milestone"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('project.group_project_manager'))]"/>
    </record>

    <record id="project_milestone_rule_portal_project_sharing" model="ir.rule">
        <field name="name">Project/milestone portal users: portal user can read with project sharing feature</field>
        <field name="model_id" ref="project.model_project_milestone"/>
        <field name="domain_force">[
            ('project_id.privacy_visibility', 'in', ['invited_users', 'portal']),
            ('project_id.collaborator_ids.partner_id', 'in', [user.partner_id.id]),
        ]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
    </record>

    <record id="mail_plan_rule_group_project_manager_task" model="ir.rule">
        <field name="name">Manager can manage project/task plans</field>
        <field name="groups" eval="[(4, ref('group_project_manager'))]"/>
        <field name="model_id" ref="mail.model_mail_activity_plan"/>
        <field name="domain_force">[('res_model', 'in', ('project.project', 'project.task'))]</field>
        <field name="perm_read" eval="False"/>
    </record>

    <record id="mail_plan_templates_rule_group_project_manager_task" model="ir.rule">
        <field name="name">Manager can manage project/task plan templates</field>
        <field name="groups" eval="[(4, ref('group_project_manager'))]"/>
        <field name="model_id" ref="mail.model_mail_activity_plan_template"/>
        <field name="domain_force">[('plan_id.res_model', 'in', ('project.project', 'project.task'))]</field>
        <field name="perm_read" eval="False"/>
    </record>

</data>

<!-- ============================================================
     PM-layer child models: multi-company + project-visibility rules
     (mirror project.task / project.update). Without these, any internal
     project user could read/mutate risks, gates, benefits, baselines,
     history, retrospectives and sprints of projects they cannot see.
     ============================================================ -->

    <!-- project.risk -->
    <record model="ir.rule" id="risk_comp_rule">
        <field name="name">Project/project.risk: multi-company</field>
        <field name="model_id" ref="model_project_risk"/>
        <field name="domain_force">['|', ('project_id.company_id', 'in', company_ids), ('project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="risk_visibility_rule">
        <field name="name">Project/project.risk: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_risk"/>
        <field name="domain_force">['|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('project_id.user_id', '=', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="risk_manager_rule">
        <field name="name">Project/project.risk: manager full access</field>
        <field name="model_id" ref="model_project_risk"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <!-- project.gate -->
    <record model="ir.rule" id="gate_comp_rule">
        <field name="name">Project/project.gate: multi-company</field>
        <field name="model_id" ref="model_project_gate"/>
        <field name="domain_force">['|', ('project_id.company_id', 'in', company_ids), ('project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="gate_visibility_rule">
        <field name="name">Project/project.gate: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_gate"/>
        <field name="domain_force">['|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('project_id.user_id', '=', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="gate_manager_rule">
        <field name="name">Project/project.gate: manager full access</field>
        <field name="model_id" ref="model_project_gate"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <!-- project.gate.criterion -->
    <record model="ir.rule" id="gate_criterion_comp_rule">
        <field name="name">Project/project.gate.criterion: multi-company</field>
        <field name="model_id" ref="model_project_gate_criterion"/>
        <field name="domain_force">['|', ('gate_id.project_id.company_id', 'in', company_ids), ('gate_id.project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="gate_criterion_visibility_rule">
        <field name="name">Project/project.gate.criterion: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_gate_criterion"/>
        <field name="domain_force">['|',
            ('gate_id.project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('gate_id.project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('gate_id.project_id.user_id', '=', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="gate_criterion_manager_rule">
        <field name="name">Project/project.gate.criterion: manager full access</field>
        <field name="model_id" ref="model_project_gate_criterion"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <!-- project.benefit -->
    <record model="ir.rule" id="benefit_comp_rule">
        <field name="name">Project/project.benefit: multi-company</field>
        <field name="model_id" ref="model_project_benefit"/>
        <field name="domain_force">['|', ('project_id.company_id', 'in', company_ids), ('project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="benefit_visibility_rule">
        <field name="name">Project/project.benefit: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_benefit"/>
        <field name="domain_force">['|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('project_id.user_id', '=', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="benefit_manager_rule">
        <field name="name">Project/project.benefit: manager full access</field>
        <field name="model_id" ref="model_project_benefit"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <!-- project.baseline -->
    <record model="ir.rule" id="baseline_comp_rule">
        <field name="name">Project/project.baseline: multi-company</field>
        <field name="model_id" ref="model_project_baseline"/>
        <field name="domain_force">['|', ('project_id.company_id', 'in', company_ids), ('project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="baseline_visibility_rule">
        <field name="name">Project/project.baseline: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_baseline"/>
        <field name="domain_force">['|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('project_id.user_id', '=', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="baseline_manager_rule">
        <field name="name">Project/project.baseline: manager full access</field>
        <field name="model_id" ref="model_project_baseline"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <!-- project.baseline.line -->
    <record model="ir.rule" id="baseline_line_comp_rule">
        <field name="name">Project/project.baseline.line: multi-company</field>
        <field name="model_id" ref="model_project_baseline_line"/>
        <field name="domain_force">['|', ('project_id.company_id', 'in', company_ids), ('project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="baseline_line_visibility_rule">
        <field name="name">Project/project.baseline.line: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_baseline_line"/>
        <field name="domain_force">['|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('project_id.user_id', '=', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="baseline_line_manager_rule">
        <field name="name">Project/project.baseline.line: manager full access</field>
        <field name="model_id" ref="model_project_baseline_line"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <!-- project.history -->
    <record model="ir.rule" id="history_comp_rule">
        <field name="name">Project/project.history: multi-company</field>
        <field name="model_id" ref="model_project_history"/>
        <field name="domain_force">['|', ('project_id.company_id', 'in', company_ids), ('project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="history_visibility_rule">
        <field name="name">Project/project.history: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_history"/>
        <field name="domain_force">['|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('project_id.user_id', '=', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="history_manager_rule">
        <field name="name">Project/project.history: manager full access</field>
        <field name="model_id" ref="model_project_history"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <!-- project.retrospective -->
    <record model="ir.rule" id="retrospective_comp_rule">
        <field name="name">Project/project.retrospective: multi-company</field>
        <field name="model_id" ref="model_project_retrospective"/>
        <field name="domain_force">['|', ('project_id.company_id', 'in', company_ids), ('project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="retrospective_visibility_rule">
        <field name="name">Project/project.retrospective: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_retrospective"/>
        <field name="domain_force">['|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('project_id.user_id', '=', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="retrospective_manager_rule">
        <field name="name">Project/project.retrospective: manager full access</field>
        <field name="model_id" ref="model_project_retrospective"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <!-- project.retrospective.action -->
    <record model="ir.rule" id="retrospective_action_comp_rule">
        <field name="name">Project/project.retrospective.action: multi-company</field>
        <field name="model_id" ref="model_project_retrospective_action"/>
        <field name="domain_force">['|', ('project_id.company_id', 'in', company_ids), ('project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="retrospective_action_visibility_rule">
        <field name="name">Project/project.retrospective.action: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_retrospective_action"/>
        <field name="domain_force">['|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('project_id.user_id', '=', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="retrospective_action_manager_rule">
        <field name="name">Project/project.retrospective.action: manager full access</field>
        <field name="model_id" ref="model_project_retrospective_action"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

    <!-- project.sprint -->
    <record model="ir.rule" id="sprint_comp_rule">
        <field name="name">Project/project.sprint: multi-company</field>
        <field name="model_id" ref="model_project_sprint"/>
        <field name="domain_force">['|', ('project_id.company_id', 'in', company_ids), ('project_id.company_id', '=', False)]</field>
    </record>

    <record model="ir.rule" id="sprint_visibility_rule">
        <field name="name">Project/project.sprint: employees: follow required for follower-only projects</field>
        <field name="model_id" ref="model_project_sprint"/>
        <field name="domain_force">['|',
            ('project_id.privacy_visibility', 'in', ['employees', 'portal']),
            '|',
                ('project_id.message_partner_ids', 'in', [user.partner_id.id]),
                ('project_id.user_id', '=', user.id)
        ]</field>
        <field name="groups" eval="[(4,ref('project.group_project_user'))]"/>
    </record>

    <record model="ir.rule" id="sprint_manager_rule">
        <field name="name">Project/project.sprint: manager full access</field>
        <field name="model_id" ref="model_project_sprint"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4,ref('project.group_project_manager'))]"/>
    </record>

</odoo>
