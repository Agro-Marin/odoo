<?xml version="1.0" encoding="utf-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="project.ProjectRightSidePanelSection">
        <div class="o_rightpanel_section" t-att-name="props.name" t-if="props.show">
            <div class="d-flex align-items-center justify-content-between gap-1 pe-3" t-att-class="props.headerClassName" t-if="props.header">
                <div class="o_rightpanel_title flex-grow-1 ps-3 py-3 py-md-2" t-if="props.slots.title" t-attf-class="{{ env.isSmall ? 'd-flex align-items-center' : '' }}" t-att-class="{ 'cursor-pointer': env.isSmall and props.canBeClosed }" t-on-click="toggleSection">
                    <span t-if="env.isSmall and props.canBeClosed"><i t-attf-class="fa-solid fa-fw {{state.isClosed ? 'fa-caret-right' : 'fa-caret-down'}}"/></span>
                    <h3 class="m-0 lh-lg"><t t-slot="title"/></h3>
                </div>
                <t t-slot="header"/>
            </div>
            <div class="o_rightpanel_data" t-if="props.showData" t-att-class="props.dataClassName" t-attf-class="{{state.isClosed ? 'o_rightpanel_hidden d-none' : ''}}">
                <t t-slot="default"/>
            </div>
        </div>
    </t>

</templates>
