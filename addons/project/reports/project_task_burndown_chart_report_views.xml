<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="project_task_burndown_chart_report_view_search" model="ir.ui.view">
        <field name="name">project.task.burndown.chart.report.view.search</field>
        <field name="model">project.task.burndown.chart.report</field>
        <field name="arch" type="xml">
            <search string="Burndown Chart">
                <field name="tag_ids" />
                <field name="user_ids" />
                <field name="step_id" />
                <field name="is_closed" />
                <field name="project_id" />
                <field name="milestone_id" groups="project.group_project_milestone" />
                <field
                    name="partner_id"
                    filter_domain="[('partner_id', 'child_of', self)]" />
                <separator />
                <filter
                    name="my_tasks"
                    string="My Tasks"
                    domain="[('user_ids', 'in', uid)]" />
                <filter
                    name="unassigned"
                    string="Unassigned"
                    domain="[('user_ids', '=', False)]" />
                <separator />
                <filter
                    name="filter_date"
                    string="Date"
                    date="date"
                    default_period="year,year-1" />
                <filter
                    name="filter_last_stage_update"
                    date="date_last_status_change" />
                <filter name="filter_date_deadline" date="date_end" />
                <filter
                    name="last_month"
                    string="Last Month"
                    domain="[('date','&gt;=', 'today -30d')]"
                    invisible="1" />
                <separator />
                <filter
                    name="open_tasks"
                    string="Open Tasks"
                    domain="[('is_closed', '=', 'open')]" />
                <filter
                    name="closed_tasks"
                    string="Closed Tasks"
                    domain="[('is_closed', '=', 'closed')]" />
                <group>
                    <filter
                        name="date"
                        string="Date"
                        context="{'group_by': 'date:week'}" />
                    <filter
                        name="stage"
                        string="Stage (Burndown Chart)"
                        context="{'group_by': 'step_id'}" />
                    <filter
                        name="is_closed"
                        string="Is Closed (Burn-up Chart)"
                        context="{'group_by': 'is_closed'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="project_task_burndown_chart_report_view_graph" model="ir.ui.view">
        <field name="name">project.task.burndown.chart.report.view.graph</field>
        <field name="model">project.task.burndown.chart.report</field>
        <field name="arch" type="xml">
            <graph
                type="line"
                string="Burndown Chart"
                sample="1"
                disable_linking="1"
                js_class="burndown_chart">
                <field name="date" string="Date" interval="week" />
                <field name="step_id" />
                <field name="is_closed" />
            </graph>
        </field>
    </record>

    <record
        id="action_project_task_burndown_chart_report"
        model="ir.actions.act_window">
        <field name="name">Burndown Chart</field>
        <field name="res_model">project.task.burndown.chart.report</field>
        <field name="path">burndown-chart</field>
        <field name="view_mode">graph</field>
        <field
            name="search_view_id"
            ref="project_task_burndown_chart_report_view_search" />
        <field name="domain">[('project_id', '!=', False)]</field>
        <field
            name="context">{'search_default_project_id': active_id, 'search_default_date': 1, 'search_default_stage': 1, 'search_default_filter_date': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_empty_folder">
                No data yet!
            </p>
            <p>Analyze how quickly your team is completing your project's tasks and check if everything is progressing according to plan.</p>
        </field>
    </record>

</odoo>
