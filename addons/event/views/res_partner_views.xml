<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
        <record id="res_partner_view_tree" model="ir.ui.view">
            <field name="name">view.res.partner.form.event.inherited</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form" />
            <field name="priority" eval="6" />
            <field name="arch" type="xml">
                <xpath expr="//div[@name='anchor_buttons_events']" position="before">
                    <button
                        name="action_event_view"
                        type="object"
                        icon="fa-ticket"
                        groups="event.group_event_user"
                        invisible="event_count == 0"
                        class="oe_stat_button">
                        <field name="event_count" string="Events" widget="statinfo" />
                    </button>
                </xpath>
            </field>
        </record>
    </data>

</odoo>
