<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
        <record id="account_return_kanban_view" model="ir.ui.view">
            <field name="name">account.return.kanban</field>
            <field name="model">account.return</field>
            <field name="arch" type="xml">
                <kanban
                    options="{'allowed_categories': ['account_return']}"
                    js_class="account_return_kanban"
                    on_create="account.action_create_account_return">
                    <field name="state" />
                    <field name="check_ids" />
                    <field name="is_completed" />
                    <field name="type_external_id" />
                    <field name="report_opened_once" />
                    <field name="is_report_set" />
                    <field name="has_move_entries" />
                    <field name="is_main_company_active" />
                    <field name="show_companies" />
                    <field name="show_amount_to_pay" />
                    <field name="date_submission" />
                    <field name="days_to_deadline" />
                    <field name="return_type_category" />
                    <field name="manually_created" />
                    <field name="visible_states" />
                    <field name="active" invisible="1" />

                    <t t-name="card" class="p-0 w-100">
                        <widget
                            name="web_ribbon"
                            title="Archived"
                            invisible="not context.get('in_checks_view') or active"
                            bg_color="bg-danger" />
                        <div class="w-100 p-2 px-3">
                            <div
                                class="alert alert-warning text-center"
                                role="alert"
                                invisible="not context.get('in_checks_view') or not show_companies_mismatch_warning">
                                Your active companies do not match this return's companies. The results shown for the checks might be incomplete.
                            </div>
                            <div class="row h-full position-relative">
                                <div
                                    class="col-12"
                                    t-att-class="{'col-lg-3': record.return_type_category.raw_value != 'audit', 'col-lg-5': record.return_type_category.raw_value == 'audit'}">
                                    <div class="row">
                                        <div class="o_field_widget">
                                            <field
                                                name="name"
                                                class="oe_state_value pe-2 fs-5" />
                                            <field
                                                name="unresolved_check_count"
                                                invisible="context.get('in_checks_view') or unresolved_check_count &lt;= 0"
                                                class="oe_state_value pe-1 text-danger" />
                                            <span invisible="context.get('in_checks_view') or unresolved_check_count &lt;= 0" class="oe_stat_text ps-0 text-danger">Pending</span>
                                            <field
                                                name="resolved_check_count"
                                                invisible="context.get('in_checks_view') or unresolved_check_count != 0 or resolved_check_count &lt;= 0"
                                                class="oe_state_value pe-1 text-success" />
                                            <span invisible="context.get('in_checks_view') or unresolved_check_count != 0 or resolved_check_count &lt;= 0" class="oe_stat_text ps-0 text-success">Passed</span>
                                        </div>
                                    </div>
                                    <div
                                        id="date_wrapper"
                                        class="row o_field_widget"
                                        t-att-class="{'text-black': (record.days_to_deadline.raw_value &gt;= 0) or record.date_submission.raw_value or record.is_completed.raw_value, 'text-danger': (record.days_to_deadline.raw_value &lt; 0) and !record.date_submission.raw_value and !record.is_completed.raw_value}"
                                        invisible="return_type_category == 'audit'">
                                        <t t-if="!record.date_submission.raw_value">
                                            <span class="oe_stat_text pe-1 text-black">Deadline: </span><field name="date_deadline" class="oe_stat_value ps-0" />
                                        </t>
                                        <t t-else="">
                                            <span class="oe_stat_text pe-1 text-black-50">Submitted on: </span><field name="date_submission" class="oe_stat_value ps-0 text-black-50" />
                                        </t>
                                    </div>

                                    <div class="row" invisible="not show_companies">
                                        <field
                                            name="company_ids"
                                            widget="many2many_tags" />
                                    </div>
                                </div>

                                <div
                                    id="account_tax_return_amount"
                                    class="col-12 col-lg-2 px-lg-0 d-flex align-items-center text-end"
                                    invisible="return_type_category == 'audit'">
                                    <field
                                        name="amount_to_pay_currency_id"
                                        invisible="1" />
                                    <div
                                        id="return_amount_to_pay"
                                        invisible="not show_amount_to_pay or not is_main_company_active or return_type_category == 'audit'">
                                        <h2
                                            class="o_account_return_amount o_field_widget">
                                            <field name="period_amount_to_pay" />
                                        </h2>
                                        <span
                                            class="oe_stat_text text-black-50 fs-5 fw-bold"
                                            t-att-class="{'invisible': record.total_amount_to_pay.raw_value == record.period_amount_to_pay.raw_value}"
                                            invisible="context.get('in_checks_view') and total_amount_to_pay == period_amount_to_pay">
                                            Balance <field name="total_amount_to_pay" />
                                        </span>
                                    </div>
                                </div>

                                <!-- States bar -->
                                <div class="col-12 col-lg-3 o_account_return_state">
                                    <t
                                        t-foreach="record.visible_states.raw_value"
                                        t-as="state"
                                        t-key="state.name">
                                        <div
                                            class="state-container"
                                            t-att-class="{'active': state.active}">
                                            <div class="state-bubble" />
                                            <span
                                                class="state-label"
                                                t-out="state.label" />
                                        </div>
                                    </t>
                                </div>

                                <!-- Buttons -->
                                <div class="col-12 col-lg-4 o_account_return_buttons">
                                    <field
                                        name="activity_ids"
                                        widget="kanban_activity"
                                        invisible="context.get('in_checks_view')"
                                        class="ms-1" />

                                    <button
                                        name="action_view_attachments"
                                        type="object"
                                        invisible="context.get('in_checks_view') or message_attachment_count &lt;= 0 or not is_main_company_active">
                                        <i
                                            class="fa-solid fa-paperclip fa-lg"
                                            title="Open Attachments" />
                                        <sup><field name="message_attachment_count" /></sup>
                                    </button>

                                    <t
                                        t-if="!record.report_opened_once.raw_value and record.show_submit_button.raw_value and record.is_report_set.raw_value">
                                        <button name="action_view_report" type="object" class="btn btn-primary"><field name="report_name" /></button>
                                        <button name="action_submit" type="object" invisible="not context.get('in_checks_view') or is_completed or not is_main_company_active" class="btn btn-secondary">Submit</button>
                                    </t>
                                    <t t-else="">
                                        <button name="action_view_report" type="object" invisible="not is_report_set" class="btn btn-secondary"><field name="report_name" /></button>
                                        <button name="action_submit" type="object" invisible="not context.get('in_checks_view') or is_completed or not show_submit_button or not is_main_company_active" class="btn btn-primary">Submit</button>
                                    </t>

                                    <t t-if="0">
                                    </t>
                                    <t id="default_buttons" t-else="">
                                        <button name="action_validate" type="object" invisible="not context.get('in_checks_view') or next_state != 'reviewed' or is_completed or not is_main_company_active or unresolved_check_count or not active" class="btn btn-primary">Validate</button>
                                        <button name="action_pay" type="object" invisible="not context.get('in_checks_view') or next_state != 'paid' or is_completed or not is_main_company_active" class="btn btn-primary">Pay</button>
                                    </t>

                                    <div
                                        class="dropdown show dropdown-sm"
                                        invisible="not is_main_company_active">
                                        <button
                                            type="button"
                                            class="btn border-0"
                                            data-self-handled="dropdown">
                                            <i
                                                class="fa-solid fa-ellipsis-v"
                                                title="Dropdown Menu" />
                                        </button>

                                        <div
                                            class="dropdown-menu"
                                            aria-labelledby="dropdownMenuButton"
                                            role="menu">
                                            <a
                                                invisible="is_completed or not active"
                                                class="dropdown-item"
                                                type="object"
                                                name="action_mark_completed"
                                                string="Mark as Completed" />
                                            <a
                                                invisible="not has_move_entries or state not in ('reviewed', 'submitted', 'paid')"
                                                class="dropdown-item"
                                                type="object"
                                                name="action_view_entry"
                                                string="View Entry" />
                                            <a
                                                invisible="context.get('in_checks_view') or not active or state != 'new' or is_completed"
                                                class="dropdown-item"
                                                type="object"
                                                name="action_archive"
                                                string="Archive" />
                                            <a
                                                invisible="active"
                                                class="dropdown-item"
                                                type="object"
                                                name="action_unarchive"
                                                string="Unarchive" />
                                            <a invisible="context.get('in_checks_view') or not (manually_created and state == 'new')" class="dropdown-item" type="delete" groups="account.group_account_user">Delete</a>

                                            <!-- Reset should be last-->
                                            <a
                                                invisible="state == 'new' and not is_completed or not is_tax_return"
                                                class="dropdown-item"
                                                type="object"
                                                name="action_reset_tax_return_common"
                                                string="Reset" />
                                            <a
                                                invisible="state == 'new' and not is_completed or not is_ec_sales_list_return"
                                                class="dropdown-item"
                                                type="object"
                                                name="action_reset_2_states"
                                                string="Reset" />
                                            <a
                                                invisible="state == 'new' and not is_completed or return_type_category != 'audit'"
                                                class="dropdown-item"
                                                type="object"
                                                name="action_reset_custom_return"
                                                string="Reset" />
                                            <a
                                                invisible="state == 'new' and not is_completed or type_external_id != 'account.annual_corporate_tax_return_type'"
                                                class="dropdown-item"
                                                type="object"
                                                name="action_reset_annual_closing"
                                                string="Reset" />
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </kanban>
            </field>
        </record>
        <record id="account_return_calendar_view" model="ir.ui.view">
            <field name="name">account.return.calendar</field>
            <field name="model">account.return</field>
            <field name="arch" type="xml">
                <calendar
                    string="Tax Return"
                    mode="year"
                    create="False"
                    edit="False"
                    delete="False"
                    color="type_id"
                    date_start="date_deadline"
                    quick_create="0">
                    <field name="name" />
                    <field name="type_id" filters="1" />
                </calendar>
            </field>
        </record>
        <record id="account_return_search_view" model="ir.ui.view">
            <field name="name">account.return</field>
            <field name="model">account.return</field>
            <field name="arch" type="xml">
                <search>
                    <field name="name" />
                    <field name="date_to" />
                    <field name="date_from" />
                    <field name="date_deadline" />
                    <field name="type_id" />
                    <field name="state" />
                    <field name="date_submission" />
                    <filter
                        name="todo_returns"
                        string="To Do"
                        domain="[('is_completed', '=', False), ('date_to', '&lt;', 'today')]" />
                    <filter
                        name="done_returns"
                        string="Done"
                        domain="[('is_completed', '=', True)]" />
                    <separator />
                    <filter
                        name="date_deadline_filter"
                        string="Deadline"
                        date="date_deadline" />
                    <separator />
                    <filter
                        name="filter_archived"
                        string="Archived"
                        domain="[('active', '=', False)]" />
                    <filter
                        name="groupby_date_to"
                        string="Date"
                        context="{'group_by': 'date_to:month'}" />
                    <filter
                        name="groupby_type_id"
                        string="Return Type"
                        context="{'group_by': 'type_id'}" />
                    <separator />
                    <filter
                        name="filter_report_id"
                        string="Return type"
                        domain="[('type_id.report_id.id', '=', context.get('filter_report_id'))]"
                        invisible="1" />
                    <separator />
                    <filter
                        name="filter_activities_my"
                        string="My Activities"
                        domain="[('activity_user_id', '=', uid)]"
                        invisible="1" />
                    <separator invisible="1" />
                    <filter
                        name="activities_overdue"
                        string="Late Activities"
                        help="Show all records whose next activity date is past"
                        domain="[('my_activity_date_deadline', '&lt;', 'today')]"
                        invisible="1" />
                    <filter
                        name="activities_today"
                        string="Today Activities"
                        domain="[('my_activity_date_deadline', '=', 'today')]"
                        invisible="1" />
                    <filter
                        name="activities_upcoming_all"
                        string="Future Activities"
                        domain="[('my_activity_date_deadline', '&gt;', 'today')]"
                        invisible="1" />
                </search>
            </field>
        </record>
        <record id="action_server_open_view_account_return" model="ir.actions.server">
            <field name="name">Open Tax Return</field>
            <field name="model_id" ref="model_account_return" />
            <field name="state">code</field>
            <field name="code">action = model.action_view_tax_return_view()</field>
        </record>
        <record id="action_view_account_return" model="ir.actions.act_window">
            <field name="name">Tax Return</field>
            <field name="res_model">account.return</field>
            <field name="path">tax-return</field>
            <field name="view_mode">kanban,calendar</field>
            <field name="view_id" ref="account.account_return_kanban_view" />
            <field name="search_view_id" ref="account.account_return_search_view" />
            <field
                name="domain">[('return_type_category', '=', 'account_return')]</field>
            <field
                name="context">{'search_default_groupby_date_to': 1, 'search_default_todo_returns': 1, 'max_number_opened_groups': 100000}</field>
        </record>
        <record id="action_create_account_return" model="ir.actions.act_window">
            <field name="name">Generate Return</field>
            <field name="res_model">account.return.creation.wizard</field>
            <field name="view_id" ref="account.return_creation_wizard_form" />
            <field name="target">new</field>
            <field name="context">{'open_account_return_on_save': True}</field>
        </record>
        <menuitem
            id="menu_action_account_return"
            name="Tax Returns"
            parent="account.menu_account_closing"
            action="action_server_open_view_account_return"
            sequence="67" />
    </data>

</odoo>
