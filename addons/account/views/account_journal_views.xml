<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_journal_tree" model="ir.ui.view">
        <field name="name">account.journal.list</field>
        <field name="model">account.journal</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <list string="Account Journal">
                <field name="sequence" widget="handle" />
                <field name="name" />
                <field name="type" />
                <field
                    name="journal_group_ids"
                    widget="many2many_tags"
                    optional="hide"
                    readonly="1" />
                <field
                    name="currency_id"
                    optional="hide"
                    groups="base.group_multi_currency" />
                <field name="code" optional="show" />
                <field name="default_account_id" optional="show" />
                <field name="active" optional="hide" />
                <field
                    name="company_id"
                    optional="hide"
                    groups="base.group_multi_company" />
                <field
                    name="company_id"
                    groups="!base.group_multi_company"
                    column_invisible="True" />
            </list>
        </field>
    </record>

    <record id="view_account_journal_form" model="ir.ui.view">
        <field name="name">account.journal.form</field>
        <field name="model">account.journal</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <form string="Account Journal">
                <field name="company_id" invisible="1" />
                <field name="bank_statements_source" invisible="1" />
                <sheet>
                    <div name="button_box" class="oe_button_box">
                        <button
                            name="%(action_account_moves_all_a)d"
                            type="action"
                            icon="fa-book"
                            context="{'search_default_journal_id':id}"
                            class="oe_stat_button">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Journal Entries</span>
                            </div>
                        </button>
                    </div>
                    <widget
                        name="web_ribbon"
                        title="Archived"
                        invisible="active"
                        bg_color="text-bg-danger" />
                    <div class="oe_title">
                        <label for="name" />
                        <field name="name_placeholder" force_save="1" invisible="1" />
                        <h1>
                            <field
                                name="name"
                                options="{'placeholder_field': 'name_placeholder'}"
                                required="not type" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="active" invisible="1" />
                            <field name="type" />
                            <field name="code" placeholder="e.g. INV" />
                        </group>
                        <group>
                            <field
                                name="company_id"
                                options="{'no_create': True}"
                                groups="base.group_multi_company" />
                            <field name="country_code" invisible="1" />
                        </group>
                    </group>
                    <notebook>
                        <page name="bank_account" string="Journal Entries">
                            <group>
                                <group>
                                    <label
                                        for="default_account_id"
                                        string="Bank Account"
                                        groups="account.group_account_readonly"
                                        invisible="type != 'bank'" />
                                    <label
                                        for="default_account_id"
                                        string="Journal Account"
                                        groups="account.group_account_readonly"
                                        invisible="type != 'credit'" />
                                    <label
                                        for="default_account_id"
                                        string="Cash Account"
                                        groups="account.group_account_readonly"
                                        invisible="type != 'cash'" />
                                    <label
                                        for="default_account_id"
                                        string="Default Income Account"
                                        groups="account.group_account_readonly"
                                        invisible="type != 'sale'" />
                                    <label
                                        for="default_account_id"
                                        string="Default Expense Account"
                                        groups="account.group_account_readonly"
                                        invisible="type != 'purchase'" />
                                    <label
                                        for="default_account_id"
                                        string="Default Account"
                                        help="If set, this account is used to automatically balance entries."
                                        groups="account.group_account_readonly"
                                        invisible="type != 'general'" />
                                    <field
                                        name="default_account_id"
                                        placeholder="Create new account"
                                        nolabel="1"
                                        options="{'no_quick_create': True}"
                                        groups="account.group_account_readonly"
                                        invisible="not type or type in ('sale', 'purchase', 'general')"
                                        required="(id and type in ('bank', 'cash', 'credit'))" />
                                    <field
                                        name="default_account_id"
                                        nolabel="1"
                                        options="{'no_quick_create': True}"
                                        groups="account.group_account_readonly"
                                        invisible="type not in ('sale', 'purchase', 'general')"
                                        required="type in ('sale', 'purchase')" />
                                    <field
                                        name="suspense_account_id"
                                        options="{'no_quick_create': True}"
                                        groups="account.group_account_readonly"
                                        invisible="type not in ('bank', 'cash', 'credit')"
                                        required="type in ('bank', 'cash', 'credit')" />
                                    <field
                                        name="non_deductible_account_id"
                                        options="{'no_quick_create': True}"
                                        groups="account.group_account_readonly"
                                        invisible="type != 'purchase'" />
                                    <field
                                        name="profit_account_id"
                                        groups="account.group_account_readonly"
                                        invisible="type not in ('cash', 'bank')" />
                                    <field
                                        name="loss_account_id"
                                        groups="account.group_account_readonly"
                                        invisible="type not in ('cash', 'bank')" />
                                    <field
                                        name="refund_sequence"
                                        invisible="type not in ['sale', 'purchase']" />
                                    <field
                                        name="payment_sequence"
                                        invisible="type not in ('bank', 'cash', 'credit')" />
                                    <field
                                        name="invoice_template_pdf_report_id"
                                        options="{'no_create': True, 'no_edit': True}"
                                        invisible="not display_invoice_template_pdf_report_id" />
                                    <field
                                        name="currency_id"
                                        options="{'no_create': True}"
                                        groups="base.group_multi_currency" />
                                </group>
                                <group
                                    name="bank_account_number"
                                    invisible="type != 'bank'">
                                    <field name="company_partner_id" invisible="1" />
                                    <field
                                        name="bank_account_id"
                                        string="Bank Account Number"
                                        context="{'default_partner_ids': [company_partner_id]}" />
                                    <field
                                        name="bank_id"
                                        invisible="not bank_account_id" />
                                    <field
                                        name="bank_statements_source"
                                        widget="radio"
                                        groups="account.group_account_basic"
                                        required="type == 'bank'" />
                                </group>
                                <group name="bank_source" invisible="type != 'credit'">
                                    <label
                                        for="bank_statements_source"
                                        string="Transaction Feeds"
                                        groups="account.group_account_readonly"
                                        invisible="type != 'credit'" />
                                    <field
                                        name="bank_statements_source"
                                        widget="radio"
                                        nolabel="1"
                                        groups="account.group_account_basic"
                                        required="type == 'credit'" />
                                </group>
                            </group>
                        </page>
                        <page
                            name="page_incoming_payments"
                            id="inbound_payment_settings"
                            string="Incoming Payments"
                            invisible="type not in ['cash', 'bank', 'credit']">
                            <field name="available_payment_method_ids" invisible="1" />
                            <field
                                name="inbound_payment_channel_ids"
                                context="{'default_payment_type': 'inbound'}">
                                <list string="Payment Methods" editable="bottom">
                                    <field
                                        name="available_payment_method_ids"
                                        column_invisible="True" />
                                    <field
                                        name="payment_type"
                                        column_invisible="True" />
                                    <field name="company_id" column_invisible="True" />
                                    <field name="sequence" widget="handle" />
                                    <field
                                        name="payment_method_id"
                                        options="{'no_create': True, 'no_open': True}" />
                                    <field name="name" />
                                    <field
                                        name="payment_account_id"
                                        string="Outstanding Receipts accounts"
                                        placeholder="No payment journal entries"
                                        options="{'no_quick_create': True}"
                                        groups="account.group_account_readonly" />
                                </list>
                            </field>
                        </page>
                        <page
                            name="page_outgoing_payments"
                            id="outbound_payment_settings"
                            string="Outgoing Payments"
                            invisible="type not in ['cash', 'bank', 'credit']">
                            <field
                                name="outbound_payment_channel_ids"
                                context="{'default_payment_type': 'outbound'}">
                                <list string="Payment Methods" editable="bottom">
                                    <field
                                        name="available_payment_method_ids"
                                        column_invisible="True" />
                                    <field
                                        name="payment_type"
                                        column_invisible="True" />
                                    <field name="company_id" column_invisible="True" />
                                    <field name="sequence" widget="handle" />
                                    <field
                                        name="payment_method_id"
                                        options="{'no_create': True, 'no_open': True}" />
                                    <field name="name" />
                                    <field
                                        name="payment_account_id"
                                        string="Outstanding Payments accounts"
                                        placeholder="No payment journal entries"
                                        options="{'no_quick_create': True}"
                                        groups="account.group_account_readonly" />
                                </list>
                            </field>
                            <field name="selected_payment_method_codes" invisible="1" />
                            <group name="outgoing_payment" />
                        </page>
                        <page name="advanced_settings" string="Advanced Settings">
                            <group>
                                <group
                                    string="Automation"
                                    groups="account.group_account_manager">
                                    <field
                                        name="restrict_mode_hash_table"
                                        groups="account.group_account_readonly"
                                        invisible="type not in ['sale', 'purchase', 'general']" />
                                    <field
                                        name="is_self_billing"
                                        invisible="type != 'purchase'" />
                                </group>
                                <group
                                    name="group_email_alias"
                                    string="Emails"
                                    invisible="type not in ('general', 'sale', 'purchase')">
                                    <field name="display_alias_fields" invisible="1" />
                                    <div
                                        class="o_row"
                                        colspan="2"
                                        invisible="display_alias_fields">
                                        <a
                                            type="action"
                                            name="%(action_open_settings)d"
                                            class="btn btn-link"
                                            role="button">
                                            <i
                                                class="oi oi-fw o_button_icon oi-arrow-right" />
                                            Configure Alias Domain </a>
                                    </div>
                                    <div
                                        class="o_row"
                                        colspan="2"
                                        dir="ltr"
                                        invisible="not display_alias_fields">
                                        <label for="alias_name" string="Email Alias" />
                                            <field name="alias_name" placeholder="alias" />@ <field name="alias_domain_id" placeholder="e.g. mycompany.com" options="{'no_create': True, 'no_open': True}" />
                                    </div>
                                    <field
                                        name="incoming_einvoice_notification_email"
                                        placeholder="e.g. finance@example.com; accountant@example.com"
                                        class="w-100" />
                                </group>
                                <group
                                    name="group_edi_config"
                                    string="Electronic Data Interchange"
                                    invisible="1" />
                                <group
                                    name="group_access_control"
                                    string="Access Control">
                                    <field
                                        name="allowed_account_ids"
                                        widget="many2many_tags"
                                        options="{'no_create': True}" />
                                    <field
                                        name="allowed_user_ids"
                                        widget="many2many_tags"
                                        options="{'no_create': True}" />
                                </group>
                                <group
                                    string="Payment Communications"
                                    invisible="type != 'sale'">
                                    <field name="invoice_reference_type" />
                                    <field
                                        name="invoice_reference_model"
                                        invisible="invoice_reference_type == 'none'" />
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="account_journal_view_kanban" model="ir.ui.view">
        <field name="name">account.journal.kanban</field>
        <field name="model">account.journal</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile">
                <templates>
                    <t t-name="card" class="row g-0">
                        <div class="col-6">
                            <field name="name" class="fw-bolder" />
                        </div>
                        <div class="col-6 ">
                            <field name="type" class="float-end" />
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_account_journal_search" model="ir.ui.view">
        <field name="name">account.journal.search</field>
        <field name="model">account.journal</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <search string="Search Account Journal">
                <field
                    name="name"
                    string="Journal"
                    filter_domain="['|', ('name', 'ilike', self), ('code', 'ilike', self)]" />
                <field name="activity_user_id" string="Activities of" />
                <field name="activity_type_id" string="Activity type" />
                <filter
                    name="dashboard"
                    string="Favorites"
                    domain="[('show_on_dashboard', '=', True)]" />
                <separator />
                <filter name="sales" string="Sales" domain="[('type', '=', 'sale')]" />
                <filter
                    name="purchases"
                    string="Purchases"
                    domain="[('type', '=', 'purchase')]" />
                <filter
                    name="liquidity"
                    string="Liquidity"
                    domain="[('type', 'in', ('cash', 'bank', 'credit'))]" />
                <filter
                    name="miscellaneous"
                    string="Miscellaneous"
                    domain="[('type', 'not in', ['sale', 'purchase', 'cash', 'bank', 'credit'])]" />
                <separator />
                <filter
                    name="inactive"
                    string="Archived"
                    domain="[('active', '=', False)]" />
                <separator />
                <group name="group_by">
                    <filter
                        name="group_by_type"
                        string="Type"
                        context="{'group_by': 'type'}" />
                    <filter
                        name="group_by_journal_group"
                        string="Ledger Group"
                        context="{'group_by': 'journal_group_ids'}" />
                    <filter
                        name="group_by_currency"
                        string="Currency"
                        context="{'group_by': 'currency_id'}"
                        groups="base.group_multi_currency" />
                    <filter
                        name="group_by_company"
                        string="Company"
                        context="{'group_by': 'company_id'}"
                        groups="base.group_multi_company" />
                </group>
            </search>
        </field>
    </record>

    <record id="action_account_journal_form" model="ir.actions.act_window">
        <field name="name">Journals</field>
        <field name="res_model">account.journal</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Add a journal
            </p>
            <p>
                A journal is used to record transactions of all accounting data
                related to the day-to-day business.
            </p>
        </field>
        <field
            name="view_ids"
            eval="[Command.clear(),                 Command.create({'view_mode': 'list', 'view_id': ref('view_account_journal_tree')}),                 Command.create({'view_mode': 'kanban', 'view_id': ref('account_journal_view_kanban')}),                 Command.create({'view_mode': 'form', 'view_id': ref('view_account_journal_form')})]" />
    </record>

    <record id="view_account_journal_group_tree" model="ir.ui.view">
        <field name="name">account.journal.group.list</field>
        <field name="model">account.journal.group</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="company_id" column_invisible="True" />
                <field name="sequence" widget="handle" />
                <field name="name" placeholder="e.g. GAAP, IFRS, ..." />
                <field
                    name="excluded_journal_ids"
                    widget="many2many_tags_journals"
                    options="{'no_create': True}" />
                <field
                    name="company_id"
                    placeholder="Visible to all"
                    options="{'no_create': True}"
                    groups="base.group_multi_company" />
            </list>
        </field>
    </record>

    <record id="view_account_journal_group_form" model="ir.ui.view">
        <field name="name">account.journal.group.form</field>
        <field name="model">account.journal.group</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <form string="Multi-ledger">
                <sheet>
                    <group>
                        <field name="company_id" invisible="1" />
                        <field name="name" placeholder="e.g. GAAP, IFRS, ..." />
                        <field
                            name="excluded_journal_ids"
                            widget="many2many_tags_journals"
                            options="{'no_create': True}" />
                        <field name="sequence" groups="base.group_no_one" />
                        <field name="company_id" groups="base.group_multi_company" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_account_journal_group_list" model="ir.actions.act_window">
        <field name="name">Multi-ledger</field>
        <field name="res_model">account.journal.group</field>
        <field name="path">multi-ledger</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_multi_ledger" />
            <h2 class="d-md-block">Ledger group allows managing multiple accounting standards.</h2>
            <p>
                Create as many ledger groups as needed to maintain separate ledgers for local GAAP,
                IFRS, or fiscal
                adjustments, ensuring compliance with diverse regulations.
            </p>
        </field>
    </record>

</odoo>
