<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_users_identitycheck_view_form" model="ir.ui.view">
        <field name="model">res.users.identitycheck</field>
        <field name="arch" type="xml">
            <form string="Password Confirmation">
                <sheet>
                    <div invisible="auth_method != 'password'">
                        <h3>
                            <strong>Let's first confirm it's you. Enter your current password:</strong>
                        </h3>
                        <div>
                            <field
                                name="password"
                                placeholder="Enter your password"
                                password="True"
                                required="auth_method == 'password'"
                                class="o_field_highlight col-10 col-md-6 px-0"
                                autocomplete="current-password" />
                        </div>
                        <a href="/web/reset_password/" class="btn btn-link ps-0" role="button">Forgot
                            password?</a>
                    </div>
                </sheet>
                <footer>
                    <button
                        name="run_check"
                        id="password_confirm"
                        type="object"
                        string="Confirm Password"
                        context="{'password': password}"
                        invisible="auth_method != 'password'"
                        class="btn btn-primary"
                        data-hotkey="q" />
                    <button
                        special="cancel"
                        string="Discard"
                        class="btn btn-secondary"
                        data-hotkey="z" />
                </footer>
            </form>
        </field>
    </record>

</odoo>
