{
  "expected": {},
  "note": "Zero reds. test_invoicing_order_with_promotions asserted that a draft invoice leaves its quantity invoiceable, which 18caece7fd6 makes true again (qty_invoiced counts every non-cancelled invoice, as upstream); test_change_reward_on_confirmed_order no longer errors. Measured solo on a loyalty+sale_loyalty database at the narrow addons path.",
  "run_spec": "-d <db with loyalty,sale_loyalty installed> --test-enable --no-http --stop-after-init --test-tags /sale_loyalty",
  "suite": "/sale_loyalty",
  "tests_total": 104,
  "verified_at": "18caece7fd6"
}
