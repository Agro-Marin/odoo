{
  "expected": {},
  "note": "Green. Kept because the suite is worth a recorded zero: it sat at 2 of 11 behind a stale selector, and both are browser tours, which fail for environmental reasons as readily as real ones. Three repairs got here: 1e2bdac3e2a (a[data-event-id] and .fc-daygrid-day-events, neither rendered since FullCalendar v7), 944f49cdafe (web's useSpecialData hung a cold many2one statusbar -- what step 1 was hiding), and the drop position: the run:'drag_and_drop ...' string form defaults to position:'top', which hoot resolves to one pixel ABOVE the target, so the dragged event landed in the previous week's row.",
  "run_spec": "-i maintenance --test-enable --test-tags '/maintenance'",
  "suite": "/maintenance",
  "tests_total": 12,
  "verified_at": "ea6447faade"
}
