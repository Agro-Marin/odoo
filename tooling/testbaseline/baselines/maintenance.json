{
  "expected": {
    "maintenance/TestCalendarWithRecurrence.test_dblclick_event_from_calendar": "FAIL",
    "maintenance/TestCalendarWithRecurrence.test_drag_and_drop_calendar_event": "FAIL"
  },
  "note": "Two tours, root-caused to two SEPARATE faults, neither in maintenance. (1) test_dblclick_event_from_calendar stops at step 2 because the maintenance.request FORM never renders -- reproduced standalone at /odoo/maintenance-requests/<id>, still 0 .o_form_view after 25s, while res.partner and maintenance.equipment forms open fine in the same session. project.task's form hangs identically, so it is not this module. The hang is useSpecialData's useRecordObserver deferred never resolving inside statusbar_field.setupRelationData, which runs only for a MANY2ONE statusbar; the searchRead it needs returns in 10ms plain and 15ms through the disk cache on the same page, so the RPC is not it. All four files involved (record_observer.js, special_data.js, reactive.js, statusbar_field.js) were last touched by bf19a83c9ab; bisecting there is blocked because that tree will not boot against today's sibling repos (_get_asset_params). (2) test_drag_and_drop_calendar_event now completes all five tour steps and fails on the Python assertion: the event lands Aug 8 instead of Aug 15, right column and one row short, identically for a drop on the day cell and on .fc-daygrid-day-top -- the tour helper's drag_and_drop against FullCalendar v7 pointer handling. The stale a[data-event-id] and .fc-daygrid-day-events selectors that used to mask both were fixed in 1e2bdac3e2a.",
  "run_spec": "-i maintenance --test-enable --test-tags '/maintenance'",
  "suite": "/maintenance",
  "tests_total": 11,
  "verified_at": "1e2bdac3e2a"
}
