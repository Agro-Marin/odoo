{
  "expected": {
    "maintenance/TestCalendarWithRecurrence.test_dblclick_event_from_calendar": "FAIL",
    "maintenance/TestCalendarWithRecurrence.test_drag_and_drop_calendar_event": "FAIL"
  },
  "note": "Two tours, two separate faults, neither in maintenance. (1) test_dblclick_event_from_calendar stops at step 2 because a COLD open of the maintenance.request form never renders -- 0 .o_form_view after 25s; the SECOND open of the same form renders in 1.3s. project.task behaves identically, res.partner and maintenance.equipment do not. Isolated cold, one variable per browser, by patching StatusBarField.setupRelationData before the first open: no-op 1261ms OK; useSpecialData(async()=>[]) 1280ms OK; useSpecialData using the PLAIN orm service 1233ms OK; useSpecialData(async (orm)=>orm.searchRead(...)) -- the wrapped ormWithCache the hook passes in -- HUNG 13000ms. So the blocker is the ormWithCache wrapper in special_data.js on a cold cache, and the second open works only because the first populated it. Ruled out with timings: the RPC itself (10ms), every orm.cache shape cold incl. {disk,always,callback} (6-13ms), get_views prewarm (31ms, does not help), markItemsStale, and effect() failing to invoke immediately (it does, and returns a disposer). Reached only for a many2one statusbar -- setupRelationData is guarded by fieldDefinition.type === 'many2one'. All four files last moved in bf19a83c9ab; bisecting is blocked because that tree will not boot against today's siblings (_get_asset_params). (2) test_drag_and_drop_calendar_event completes all five tour steps and fails the Python assertion: Aug 8 instead of Aug 15, right column one row short, identical for a drop on the day cell and on .fc-daygrid-day-top -- the tour helper's drag_and_drop against FullCalendar v7 pointer handling. Stale selectors that masked both were fixed in 1e2bdac3e2a.",
  "run_spec": "-i maintenance --test-enable --test-tags '/maintenance'",
  "suite": "/maintenance",
  "tests_total": 11,
  "verified_at": "2fa34973ea3"
}
