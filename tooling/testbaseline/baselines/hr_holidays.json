{
  "expected": {
    "hr_holidays/Subtest TestHolidaysCalendar.test_hours_time_off_request_calendar_view (login='enguerran')": "FAIL",
    "hr_holidays/TestHrLeaveTypeTour.test_hr_leave_type_tour": "FAIL"
  },
  "note": "3 -> 2. TestPartner.test_res_partner_to_store PASSES now and is banked out; the first floor and the workspace CLAUDE.md's known-defects row both still said 3.\n\nTHE THIRD FAILURE THIS SET CARRIED IS NOT A THIRD FAILURE, IT IS A FLAKE.\nThree full runs, same run-spec, a fresh database each:\n\n  run 1   3 failed of 302   includes TestTimeOffAllocationTour.test_time_off_allocation_warning_tour\n  run 2   2 failed of 302   does not\n  run 3   2 failed of 302   does not, and is the run this floor is taken from\n\nand TestTimeOffAllocationTour passes in isolation (with TestTimeOffCardTour, 0\nfailed of 2). It is order- or contention-dependent inside the full run, not a\nregression, and it is deliberately NOT recorded as expected: banking a flake makes\nthe run that catches it read as clean, which is the opposite of what this file is\nfor. Runs 1 and 2 predate f066721cb65; run 3 is after it, and the set did not move.\n\nThe two that remain are both tours and reproduce in every full run.\n\nREFUTED, so nobody re-walks them. Both remaining tours fail with isTourReady\nfalsy, which reads as the tour never registering, and it is none of:\n\n  - a missing bundle. hr_holidays' manifest puts static/tests/tours/**/* in\n    web.assets_tests and static/src/**/* in web.assets_backend; every tour file\n    is covered by one of them.\n  - a name mismatch. The six names registered in JS and the six the tests ask\n    for are the same six.\n  - the start URL. time_off_card_tour and time_off_graph_view_tour start at '/'\n    and pass; hr_leave_type_tour starts at '/web' and fails, but '/web' and\n    '/odoo' are the SAME route -- web/controllers/home.py declares\n    ['/web', '/odoo', '/odoo/<path:subpath>', ...] on one handler -- so the\n    fork's move to /odoo is not what breaks it.\n  - infrastructure. No ENOSPC, no PoolTimeout, no 'too many clients' and no\n    'Failed to fetch' in any run, and 3 of the 5 tour tests pass alongside.",
  "run_spec": "-i hr_holidays --test-enable --test-tags '/hr_holidays' --stop-after-init, fresh database",
  "suite": "/hr_holidays",
  "tests_total": 302,
  "verified_at": "c109fb733be"
}
