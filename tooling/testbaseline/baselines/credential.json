{
  "expected": {},
  "note": "green. No CI lane runs this module (guidelines \u00a79.6) -- it is run by hand, so a baseline is the only thing that notices a regression",
  "run_spec": "-u credential --test-enable --test-tags /credential, with ODOO_API_ENCRYPTION_KEY set",
  "suite": "/credential",
  "tests_total": 228,
  "verified_at": "45a2ae2d337"
}
