{
  "expected": {
    "hr_expense/TestExpensesAccessRights.test_expense_access_rights_user": "FAIL",
    "hr_expense/TestExpensesMailImport.test_import_expense_from_email": "ERROR",
    "hr_expense/TestUi.test_expense_manager_can_always_set_employee": "FAIL",
    "hr_expense/TestUi.test_no_zero_amount_expense_in_expense": "FAIL"
  },
  "note": "Seven of the eleven recorded at c51631833cd now pass and are dropped; the two TestExpensesStates ones and TestExpenses.test_expense_main_flow no longer die inside account_move.unlink, and four of the five TestExpensesMailImport ones no longer hit the empty-product singleton in _get_product_accounts. Not fixed here -- they were already green at HEAD before this change, measured in a detached worktree, so the stale entries were costing every reader a '7 newly-passing' line and no clean verdict. What this change did fix is a TWELFTH failure the old recording never saw: TestExpenses.test_expense_multi_currencies died with AccessError in _compute_from_product, which reads product_id.supplier_taxes_id for a boolean while an expense user holds no account.tax ACL; it now reads through sudo(), as purchase does for the same field. The four left are the three browser tours (they need the live conf's addons-path, and a worktree needs node_modules symlinked) and TestExpensesMailImport.test_import_expense_from_email, the one mail-import case still reaching _get_product_accounts with an empty product.template. Total moved 51 -> 53 with the two TestExpenseProductTaxAccess cases added alongside that fix.",
  "run_spec": "-i hr_expense --test-enable --test-tags '/hr_expense'",
  "suite": "/hr_expense",
  "tests_total": 53,
  "verified_at": "1f682f8218d"
}
