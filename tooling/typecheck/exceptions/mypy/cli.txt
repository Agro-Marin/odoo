# mypy / cli: files NOT yet clean under mypy.ini — the
# EXCEPTIONS to a default-deny gate, so this list may only shrink.
# Everything else under odoo/cli/ is locked at zero, including
# files added after this list was written. Per-file ceilings for the
# entries below are in budgets/mypy-cli.json.
#
