{
  "count": 21,
  "note": "24 -> 21, and all three are views/ tests that asserted on a class no non-test file declares -- the one assertion shape a wrong selector cannot be told from a passing test, which is why this gate exists.\n\n  kanban_view.test.js:6733   .o_kanban_example_background   ->  .o_kanban_stages_nocontent + .o_kanban_examples\n  settings_upgrade_boolean_field.test.js:53,100   .o_field .badge  ->  .o_field_widget .badge\n\nThe kanban one is the more interesting: 'o_kanban_example_background' (singular) exists nowhere in the workspace -- the real classes are o_kanban_examples* -- and the affordance the test meant to exclude lives inside the 'No stages yet' block, kanban_renderer.xml:98. So the assertion now says what it meant: the one nocontent shown here is the RECORD helper, not the stages one, and the See examples button is not rendered. Two assertions replace one because the test was distinguishing two nocontent blocks and only naming one.\n\nThe settings pair asserted '.o_field .badge'. There is no bare .o_field in this tree; the wrapper field.js:382 puts on a field is o_field_widget. Both matched nothing and could not fail whatever the enterprise badge did.\n\nviews/ now has zero vacuous assertions. Verified: @web/views/kanban/kanban_view 360 passed, @web/views/settings 62 passed."
}
