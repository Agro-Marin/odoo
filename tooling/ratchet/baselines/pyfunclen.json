{
  "count": 4391,
  "note": "4403 -> 4391, and every unit of it is base.ir_actions_server._run_action_webhook, 92 -> 57 lines. d3967e48968 moved the send path behind api_transport's endpoint model and left the action resolving an endpoint and delegating to it, so the function's 12 units of excess over the 80-line budget are gone and it is off the offender list entirely.\n\nThe floor before this one recorded the same function taking the gate red in the other direction: 9e285c3d53b grew it 88 -> 92 and did not re-floor. The commit that shrank it did not re-floor either, so exact mode has been failing at HEAD ever since, only now on the improvement side. One function exercised both halves of the ratchet inside four days; this closes it.\n\nThe excess did not escape the scope. The gate measures odoo/ and the extraction landed in addons/api_transport, which it does not read, but that file's longest function is __call__ at 45 lines and its excess over the budget is 0 -- nothing was relocated past the gate.\n\nMeasured with tooling/architecture/py_function_length.py --count on a clean tree at d4458a315b7; ir_actions_server.py is the only core .py file changed since the last floor move. 120 functions remain over 80 lines, none over 250, longest 223 (modules/loading.py::load_module_graph)."
}
