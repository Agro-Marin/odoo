{
  "count": 3413,
  "note": "ir_ui_view.py: 3435 -> 3413, and the whole of the -22 is one traversal stopping being written twice.\n\n_postprocess_view and _check_view walked the same tree threading the same two\nscopes down it, in 35 structurally identical lines apiece. `_arch_scope` is now\nthe one place that is built:\n\n  _postprocess_view   83 -> 58\n  _check_view         91 -> 72\n  _arch_scope           new, 64 -- under the ceiling on its own\n\nso this file stops contributing offenders to the gate entirely (2 -> 0) while\nadding a function. That is the shape the gate is meant to reward: it ratchets\nEXCESS LINES over 80, not offender count, so splitting a long function raises\nthe count of functions and lowers the number.\n\nNot a behaviour change, and not argued: both phases were driven over 45 real\nviews and every node_info dict either builds was recorded and compared --\n1338 dicts over 90 phase runs, identical byte for byte.\n\npyfunclen_addons is red at this HEAD by +338 and is left alone: measured with\nand without this change it reads 21503 either way, so it is not this commit to\nbank."
}
