{
  "count": 3361,
  "note": "3394 -> 3361. Only 17 of the 33 are this change's. 3394 -> 3378 was already standing at HEAD (66b6c590afe measures 3378 on a pristine detached worktree), left by an earlier change that shrank a function and did not re-floor; banked here so the gate is not left red in the falling direction, not claimed.\n\nThe 17 are ir.module.module's two entries in this file: button_upgrade (96 lines) split into _upgrade_cascade / _drop_unchanged_from_upgrade / _uninstalled_dependency_names, and _button_immediate_function (81) into _lock_against_concurrent_module_operations / _has_pending_module_operation. _compute_description_html also loses its inner closure to a module-level localize_description_images and two named readers.\n\nMeasured on a detached worktree at HEAD with only this change's files applied, not in the shared workspace checkout, which carries other sessions' uncommitted work. NOTE: another session had 3357 in flight in its working tree when this landed, measured against the same 3394 parent for a different set of splits. Whoever commits second re-derives; neither number is wrong, they measure different trees."
}
