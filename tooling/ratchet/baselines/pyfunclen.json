{
  "count": 4414,
  "note": "4432 -> 4414, re-measured on the tree left by rebasing this branch onto origin/19.0-marin.\n\n  origin/19.0-marin        4432   (its committed floor, 4432, reproduces)\n  this branch, pre-rebase  4373   (against its own committed 4373 -- also reproduces)\n  merged                   4414\n\nThe -18 is orm/models/mixins/unlink.py::unlink, 101 -> 70 lines and out of the counted set: its two logging tails are now _log_unlinked_ids (the ID-list info line and its truncated variant) and _log_unlink_profile (the debug timing line and the aggregate record_unlink). Pure code motion; the only substitution is len(self) -> len(deleted_ids) for the record count, and deleted_ids IS self.ids, taken before the delete. _delete_sql (125) is now that file's only entry.\n\n4432 - 18 = 4414. The two trees do not compose to their pre-rebase numbers by arithmetic (4373 + origin's ESM growth), and this is the measurement rather than that sum."
}
