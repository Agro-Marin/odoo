{
  "count": 4039,
  "note": "RE-MEASURED AT e1867684efb WITH ONLY THIS BRANCH'S CHANGE APPLIED, in a detached worktree, because the shared checkout carries another session's ~100 uncommitted files. 4048 -> 4039, and the tree had drifted to 4057 in between, so the move is two things and both are accounted for here.\n\nThe +9 that landed since 4048 was set (d115f0f0556), unit by unit, from diffing --json between that commit and HEAD:\n  +2  ir_cron._run_job (96) renamed to _run_job_within_budget and grew to 98\n  +0  ir_fields.for_model -> _get_converter_record, 91 either side\n  +0  ir_model_fields._instantiate_attrs -> _prepare_field_attrs, 96 either side\n  +1  ir_module._button_immediate_function crossed the threshold at 81\n  +6  tools/pdf convert_to_pdfa 92 -> 98\nNone of it is this branch's; three of the five are rename churn from a naming pass.\n\nThe -18 is this branch's, and is why the floor drops rather than rises: convert_to_pdfa was doing four separable things at 98 lines, and the fontTools DescendantFont width block is the one that lifts out whole -- nothing it binds is read after it. Extracted as _restate_descendant_font_widths(pages), which leaves the caller under 80 and off the offender list entirely, so the whole 18 comes back, not just the 6 it had grown by. The diff is a pure move plus the call, the def, and `return` in place of `else:`. Verified against the suite that asserts PDF/A structure: account_edi_ubl_cii, 0 failed of 59 tests, and no \"Error while converting to PDF/A\" in the log."
}
