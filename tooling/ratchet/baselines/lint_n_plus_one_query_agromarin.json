{
  "count": 60,
  "measured_at": "979d01bcccdc9a01f6efc2daa16af76f7ae90d4d",
  "measured_root": "agromarin",
  "note": "product_docs_xiuman_data/labels.py had four search() calls inside loops: one per entry of pairing.NEW_PRODUCTS in ensure_products, and three per category name inside a loop over folders that was itself inside a loop over categories. All four hoisted above their loops and indexed by name, with the label dict written back after a create so a category in two folders still finds the record the first folder made. 64 -> 60. The floor this replaces was 61, i.e. agromarin HEAD was already red by three before this session -- confirmed by measuring a clean archive of agromarin HEAD, which reads 64 as well.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRE-STAMPED AFTER THE 2026-09-07 SYNCS, AND 67 -> 61. Two syncs landed in this repository today; the first replayed agromarin onto origin, so c4e07c06d ceased to exist and all three of this scope's stamps read ORPHANED-BASE, which makes the architecture lane exit 2 asking to be re-measured rather than comparing. Re-measured in a clean detached worktree of d2054a34a, laid out beside a clean odoo worktree as that lane lays it out.\n\nTHE NUMBER MOVED AS WELL AS THE STAMP, AND NOT BECAUSE OF EITHER REPLAY. The first sync brought 180 commits carrying seven new reports and taking this gate to 69, red. One was a real N+1 -- approval.request._compute_asset_id searched stock.lot once per request -- and is batched now. Six were false positives of two shapes the gate cannot see: a loop over MODEL NAMES in agro_base, where the query count is bounded by the model list and each search is already batched across the ids, and a per-company memo in approval_account and approval_stock; those six carry a reasoned noqa. Five older findings had gone in the same range, which is why the visible drift was +2 where the repair is -7. Measured 67 at the pre-sync tip 474753152, 69 at that merge, 62 after the repair, and 61 here after a second sync of 206 commits retired one more."
}
