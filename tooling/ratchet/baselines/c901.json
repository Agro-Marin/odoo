{
  "count": 45,
  "note": "Field._get_cache_miss drops below the threshold: its three copies of \"try the prefetch batch, fall back to the single record\" became one _batch_then_single helper taking the exception tuple and the re-raise policy as arguments, which removes six branches from the method. No behaviour change -- each call site passes exactly the catch set it had, including the compute branch keeping its lack of a single-record re-raise. Verified 0 failed of 4896 tests on -i base,test_orm."
}
