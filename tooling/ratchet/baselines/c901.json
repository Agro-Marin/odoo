{
  "count": 42,
  "note": "modules/loading.py: 43 -> 42, and the whole of the -1 is one offender leaving the list.\n\n`ruff check odoo/ --no-cache --select C901 --output-format=concise`, HEAD against HEAD plus this change, diffed:\n\n  odoo/modules/loading.py::load_module_graph  35 > 20  -> gone\n\nIt was 221 lines and thirty-eight branches, all of them re-deciding what\n`update_operation` had already decided at the top. The steps now have names --\nresolve_operation, run_pre_migration, import_python_module, run_pre_init_hook,\nload_models, load_data_and_demo, run_post_migration, mark_loaded,\nrun_post_init_hook, stamp_installed, run_at_install_tests, report_cost -- and\neach decides for itself whether it applies, the way _ModuleLoader's phases\nalready did after 4fc8d5f671d. No new offender: nothing the split produced is\nover the ceiling on its own."
}
