{
  "count": 40,
  "note": "42 -> 40, and the two units are two different commits' work, banked together because an exact ratchet cannot be moved halfway.\n\n  odoo/addons/base/models/ir_ui_view.py::_check_attributes  25 > 20  -> gone\n  odoo/addons/base/models/ir_qweb.py::_compile_expr_tokens  37 > 20  -> gone\n\nThe first is c1c6623ee6e's, which took it under the ceiling and did not bank the\nfloor, so the gate has been red in the falling direction ever since. The second\nis this change: _compile_expr_tokens was three passes over one token list\nsharing nothing but the list, and is now _collect_expr_argument_names,\n_fold_expr_brackets and _emit_expr_tokens, with _collect_lambda_argument_names,\n_collect_loop_target_names, _emit_lambda_parameters and _emit_expr_name under\nthem. No new offender: nothing the split produced is over the ceiling on its own.\n\nEquivalence was measured rather than argued. Every t-out, t-esc, t-raw, t-if,\nt-elif, t-value, t-foreach, t-att, t-att-*, t-options and t-args expression in\nthe four repositories -- 18661 distinct, compiled at both settings of\nraise_on_missing, 37322 pairs, 368 of which raise -- produced character-identical\noutput before and after, exception types and messages included."
}
