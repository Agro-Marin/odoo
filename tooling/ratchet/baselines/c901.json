{
  "count": 43,
  "note": "http/routing.py: 44 -> 43, and the whole of the -1 is one offender leaving the list.\n\n`ruff check odoo/ --no-cache --select C901 --output-format=concise` in two trees,\nthis commit and its parent:\n\n  parent   44   (its floor, 44, reproduces -- unlike pyfunclen, this one was in\n                 sync with its tree)\n  this     43\n\ncomm returns one removal and no additions:\n\n  odoo/http/routing.py::_generate_routing_rules is too complex (21 > 20)  -> gone\n\nSame split as the pyfunclen note, measured on the same pair. The 21 branches\nwere three nested closures plus the per-method routing fold sharing one scope;\nthe fold is now _merge_routing, the tree walk _get_controllers, and neither the\ngenerator nor any extraction reaches the ceiling."
}
