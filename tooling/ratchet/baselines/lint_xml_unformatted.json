{
  "count": 3362,
  "note": "3370 -> 3362. Four newly-added survey demo files arrived non-canonical, which is the increase this ratchet exists to refuse; formatting them alone would have left the module split between two conventions, so the whole of addons/survey/demo was canonicalised -- 9 files, `_pretty_xml.py addons/survey/demo`. The formatter re-parses its own output and refuses a change `_xml_identity.is_faithful` cannot confirm, so this is a whitespace change by construction."
}
