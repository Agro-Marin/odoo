{
  "count": 3295,
  "note": "account: 71 XML data files put into canonical format by the repo s own fixer.\n\n3341 -> 3295. This one is measured, not composed: at 52a60efa3c0 the gate reads 3366 and with only this change 3295, both in a detached worktree.\n\nTWENTY-FIVE OF THAT DROP IS NOT THIS CHANGE. HEAD measures 3366 against its own committed floor of 3341, so the gate was already red by 25 before this branch existed -- somebody landed 25 unformatted files and never banked them. The gate is exact, so a floor of 3341 + this change s -71 = 3270 would fail against the real 3295. Banking 3295 is the only value that is true of the tree; the 25 are named here rather than hidden, and no one has examined which files they are.\n\nOrder matters when running the fixers: _sort_xml_records.py rewrites attribute order and leaves indentation drift behind it, so _pretty_xml.py has to run last or it reports the files it just formatted.\n\nCommand: odoo-bin -d <db> -i test_lint --test-enable --test-tags /test_lint"
}
