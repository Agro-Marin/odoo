{
  "count": 3201,
  "measured_at": "24f92b0f744fe7664a897896eb4ab25bf1f647ae",
  "note": "Re-measured at 24f92b0f744 after a rebase orphaned the previous stamp b7b596cd004; moved 3199 -> 3201, UP by two. Read through the test_lint lane's own reader (odoo-bin --addons-path=odoo/addons,addons -i test_lint --test-enable --test-tags /test_lint on a fresh database, where test_pretty_xml reported 3201 against the floor), and attributed by running the same formatter sweep over the same core_data_files selection standalone at both commits (3199 and 3201, to the unit) and diffing the sets. Three files joined and one left. Joined: addons/stock/report/picking_templates.xml, formatted before e38766a8fa6 '[FIX] stock: fix QWeb render crash from stale reserved_uom_qty field' and not after it; odoo/addons/base/security/base_security.xml, formatted before e6ae905d756 (the employee-is-a-role refactor) and not after it, and 63a06d2ca21 left it so; addons/l10n_mx/data/res_partner_identifier_type_data.xml, formatted as 86f75926034 added it and not after 9087b29a12a '[ADD] l10n_mx: the IMSS social security number as an identifier type' -- which is the rebased twin of the very commit the old stamp names, so the old floor was measured a moment before its own commit broke it. Left: addons/stock_account/views/stock_picking_views.xml, deleted whole by a7572f47b78. l10n_tr_nilvera_einvoice's res_partner_category_data.xml -> res_partner_tag_data.xml is the same unformatted file under e6ae905d756's rename, not a move. Each of the three joiners is one _pretty_xml.py run away from leaving again, and every one came in through the rebase without meeting this floor.\n\nRe-measured after rebasing this branch onto origin/19.0-marin (214 upstream commits). Neither side's floor described the merged tree: both had driven counts down independently from a common base, so every one of these had to be re-derived rather than merged. Measured at HEAD in the workspace checkout; mypy per section 9.3 in a detached worktree with mypy==2.3.0 alone."
}
