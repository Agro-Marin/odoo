{
  "count": 229,
  "measured_at": "4e1fcdae6684469a205482d34794b6be507e6068",
  "measured_root": "",
  "note": "WIDENED SCAN, NOT A WIDENED TREE: 229 newly VISIBLE names, 0 new offenders. naming_vocabulary.py (84573c1c65f8) asks four questions it could not ask before, each argued in \u00a72.4 and measured over every scope before landing: (1) domain_tail_under_get -- a _get_<what>_domain is \u00a72.4.1 free-standing domain in the tail-marked spelling that section retired, exempt only on a return annotation naming a non-domain type (a -> str hostname, a -> Self alias-domain record); in measure() and deliberately NOT in classify, so the core gate, which reads classify as its leading kind and pins _get_x_domain as accepted, does not inherit it -- 158; (2) the necessity modals should/need/needs/must/want/wants/requires are ABOLISHED -> _is_ (\u00a72.4.8) -- 42, and the three the shared table reached in core-gate scopes were renamed in the same commit; (3) generate joins the producer verbs (\u00a72.4.12), printing _create_ for a create-only body and _update_ otherwise -- 18; (4) reshaped_receiver, a _get_/_check_/_set_/_prepare_/_resolve_ whose body is one return self.filtered/sorted/grouped/sudo/with_*(...) (\u00a72.4.22) -- 11. base itself reads 0 under the widened gate: thirty-three names renamed there in 84573c1c65f8. Measured in a clean detached worktree at 4e1fcdae6684 (231 on the checkout; 229 at the tip after odoo-77 prepare_config_warning and the strip pass). Exact, so the next batch to drain any of the 229 re-banks in its own commit; the decomposition above is the map.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REPAIRED AFTER A SECOND REBASE ON 2026-09-10, NUMBER UNCHANGED. Two origin commits (06c63323ec84 orm query memo, 7c98abdeeb30 exchange) landed while the sync was being verified; replaying onto them rewrote 7aa312dfe3fc, which this stamp named, into f32b27003eac. Re-measured with the gate own runner at f32b27003eac + 1 in a clean checkout and it reads what the floor already said. Only the provenance moves; the note below is the attribution.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REPAIRED AFTER THE 2026-09-10 SYNC, NUMBER UNCHANGED AT ZERO -- AND IT WAS ONE ON THE MERGED TREE UNTIL 7aa312dfe3fc. Origin's 7a4fc89f51b0 arrived carrying _append_description_variants in purchase_requisition; renamed _add_description_variants at its three bindings in the same commit as the other sync repairs. Measured 0 with naming_vocabulary.py --roots odoo --count at 7aa312dfe3fc, tree clean.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nBACK TO ZERO. The 159 newly-visible names of e195be1f7a41 are swept in b34a816dd849 (140 Python renames over 240 files, read one by one against the rows), the two website insert_* names went in b5f9bed0b06d, the three names 145dff9562e2 made visible in files that batch was already holding (_prepare_request, _prepare_survey_in_progress_data, _prepare_badges_domain) rode in it as odoo-ff hunks, and the two producers-without-a-product that batch itself introduced (_get_lines_to_defer, _get_company_based_kpi) are _update_* in 3bf82b219115. Measured in a clean detached worktree at 3bf82b219115: 0. Exact, and a contract again -- a new offender anywhere under a manifest, controllers and migrations included, is red.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n160 = 157 + 3, TWO MOVES IN ONE STAMP, neither a regression. (1) 159 -> 157 arrived with b5f9bed0b06d (website's insert_record / insert_attachment renamed to create_*), two units nobody banked, so this floor had been red under exact mode since that commit; measured with each commit's own gate in a detached worktree: 159 at e195be1f7a41, d125fe46759d, 366986f0dcd3 and 05444f900744; 157 at b5f9bed0b06d, 28a7fa9ad54e and 1ad4b4cbb623. (2) 157 -> 160 is the SCAN growing, not the tree: 145dff9562e2's two rules -- prepare_* with a _domain tail is the Domain row, and a producer prefix on a body that never returns a value but stores or writes is the Mutation row -- reach exactly three names: api_transport/tools/api_client.py _prepare_request, survey/controllers/main.py _prepare_survey_in_progress_data, website_profile/controllers/main.py _prepare_badges_domain. All three are ALREADY RENAMED in another session's uncommitted widened-scope sweep, which carries those files (agreed 2026-09-09 evening); when it lands this reads 157 again and that landing re-banks. Every other name the two rules reach in addons/ (twelve, 366986f0dcd3) was renamed ahead of them. Measured in a clean detached lane at 145dff9562e2.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nWIDENED SCAN, NOT A WIDENED TREE: 159 newly VISIBLE names, 0 new offenders. naming_vocabulary.py (e195be1f7a41) now governs every file under a __manifest__.py (controllers/, tools/, report/, utils/, migrations/, a module top-level utils.py) instead of models/ and wizard/ only, and carries \u00a72.4.20 synonyms in ABOLISHED (populate, tweak, prune, sweep, seed, scan, detect, determine, calculate, synchronize/synchronise, assemble, craft, forge). Measured in a clean detached worktree at e195be1f7a41. The floor was already red at 19 against 0 before that change -- eighteen account report names (build_result_dict, _inject_*_into_xlsx_sheet, _validate_companies*, assign_sum ...) arrived with the 2026-09-09 sync, plus hr_version.drop_values_from_other_companies -- so 0 was not the tree either. Banked at the reading so that four sessions can sweep it down in batches under an exact floor, each batch re-banking in its own commit. Decomposition: controllers 66, models 40, tools 32, other 21; by verb verify 31, validate 29, build 17, fetch 13, calculate 13, ensure 9, populate 7, delete 7, synchronize 7, make 6, inject 5, assign 4, insert 3, drop 2, scan 2, determine 2, prune 1, synchronise 1.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-banked at a reachable commit after this branch was rebased onto origin/19.0-marin. --list had flagged it as banked at a commit that is not in HEAD history: the rebase replaced every commit on this side, so each measured_at named an object no longer reachable. Re-measured on the merged tree rather than carried over. The eight addon rings took it to zero, and _validate_hook on res.partner.identifier.type -- added upstream while this was out of tree -- was the last unit. Neither side's figure described the merged tree: 89 was upstream's, 0 was this one, and both were driven down from a1b37ae209c without seeing the other."
}
