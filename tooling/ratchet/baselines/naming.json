{
  "count": 115,
  "note": "An override does not choose its name -- the base does. The gate counted one as its own violation, which double-counted the debt (fixing the base fixes every override with it) and invited a rename that silently unhooks the override: the method keeps compiling, super() still resolves, and the behaviour just stops being reached. 130 -> 115; the 15 removed are all super()-calling overrides. Workspace-wide the same shape covered 52 of 368 definitions."
}
