{
  "count": 145,
  "note": "account.reconcile.model.line._validate_amount -> _check_amount, 146 -> 145. \u00a72.4.8: validate is an abolished spelling and _check_ is the canonical for an @api.constrains hook, which this is. The hook also stopped raising UserError for ValidationError, per \u00a72.7 and the review checklist -- no functional difference (ValidationError subclasses UserError and _validate_fields does not branch on the type), but the two rules point the same way and the rename is what the gate counts. Measured in a detached worktree at HEAD with only this changes files copied in: the shared tree carries another sessions work that leaves fieldhooks at 285 against a committed 282, red before this branch and not banked here."
}
