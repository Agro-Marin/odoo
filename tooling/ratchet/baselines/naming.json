{
  "count": 262,
  "note": "Deleting the dead db/schema.py::fix_foreign_key (-1), whose `fix_` verb \u00a72.4 abolishes in favour of `_update_*`. Removed in the same sweep as libs/set_expression.py::get_disjoint_ids for having no caller anywhere in odoo/, enterprise/, agromarin/ or design-themes/; this floor was missed in that commit and is corrected here, in the commit that measured it."
}
