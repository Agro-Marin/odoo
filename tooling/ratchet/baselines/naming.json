{
  "count": 95,
  "note": "base_import._validate_import_paths -> _check_import_paths. One definition, the abolished verb `validate` -> the canonical _check_* per coding_guidelines.rst 2.4. Three references in one file, no caller outside it.\n\n  96 -> 95"
}
