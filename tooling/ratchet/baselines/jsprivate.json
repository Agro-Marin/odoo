{
  "count": 42,
  "measured_at": "1ad4d29c24e501c111c14dc20de05869963fc07e",
  "measured_root": "",
  "note": "STAMP REFRESHED, NUMBER UNCHANGED. The old stamp named a commit the 2026-09-02 sync replayed away, so `--list` flagged it ORPHANED-BASE and architecture.yml exited 2 asking to be re-measured rather than comparing anything -- neither green nor red. Re-measured in a clean detached worktree of 1ad4d29c24e and it reads exactly what the floor already said, so only the stamp moves.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n244 -> 42 in 289e8a01272, every unit a rename: the 201 accesses that named a member a *_contract.js declared private (record.js 99, relational_model.js 54, static_list.js 32, action_service.js 21, dynamic_list.js 9) now reach the same member spelled public -- the underscore twin of a mutex-guarded public method became *Locked (updateLocked, saveLocked, loadLocked, discardLocked, switchModeLocked, checkValidityLocked, getChangesLocked, applyCommandsLocked, multiSaveLocked, resequenceLocked), everything else simply lost the underscore, and _values readers use the savedData getter that already existed. The 42 left are static_list's three INTERNAL_COLLABORATORS (command engine, sort, utils) reaching its working state; 43 -> 42 because static_list_sort.js now calls notifyParentUpdate() rather than the _onUpdate callback field. Zero declared, zero undeclared, zero writes."
}
