{
  "count": 251,
  "note": "253 -> 251 (-2). Audit U3 proposal 4 step 2: loadState migrated onto the shared NavigationTracker clock. load_state.js's read+increment of the foreign private am._loadStateGeneration (the one access the action-service contract deliberately left counted as debt) is deleted with the counter itself; loadState now mints on am.navigation like every other navigation entry point. Command: python tooling/architecture/js_private_access.py --count"
}
