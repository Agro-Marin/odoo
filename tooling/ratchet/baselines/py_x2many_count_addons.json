{
  "count": 22,
  "note": "23 -> 22, and the whole of the -1 is one compute.\n\nsurvey.quota._compute_quota_usage ran `search_count` once per record inside its\nloop; it now takes a single `_read_group` over survey.user_input.line keyed by\nsuggested_answer_id and reads each quota's count out of the result. Landed in\n8b936132f46 and left unbanked, which an exact ratchet fails on the same as a\nregression.\n\nOffender sets diffed name by name between a detached worktree at 765bc0c4761 --\nthe commit that set 23 -- and one at HEAD, with line numbers normalised out so a\nfile that merely moved does not read as two units:\n\n  removed  addons/survey/models/survey_quota.py  _compute_quota_usage\n  added    none\n\n`addons/web` contributes nothing to this gate and did not before."
}
