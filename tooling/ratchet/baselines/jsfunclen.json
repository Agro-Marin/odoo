{
  "count": 30,
  "measured_at": "8ee29cf681816841c210d01d6f8a115c0e18d52c",
  "measured_root": "",
  "note": "32 -> 30 at 8ee29cf6818, measured in a detached worktree of that commit: web/search's EmbeddedActions.saveNewAction (98 lines) and ControlPanel.setup (96) split by responsibility; search/ now has no function over 90 lines.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nMeasured 32 in a detached worktree at d2430ad22ad (parent 70208245853 read 45). All 13 units are web/static/src/core functions split in that commit: py_interpreter evaluate/_applyBinaryOp/pyStringFormat, py_parser parseInfix/parsePrefix, domain compileCondition, rpc_cache read, sortable onDragStart, nested_sortable onDrag, position/utils computePosition, file_upload_service upload, localization_service start, construct_expression_from_tree, user._makeUser.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n46 -> 45 at 17a9f555940, measured in a detached worktree of that commit with node_modules symlinked and nothing uncommitted: View.loadView in web/static/src/views/view.js (222 lines) split into resolveViewSelection, loadViewDescriptions, parseViewArch, resolveUseSampleModel, buildControllerProps, buildWithSearchProps and mergeViewDisplay, none over 90. The baseline itself is banked from the workspace checkout in a follow-up commit so the floor sits at the tip that lowered it.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n47 -> 46 at 668a9a62ea1, measured in a detached worktree of that commit: web/search's _fillPropertyFieldSearchItems (98 lines) fell under 90 once its stale-item retirement moved into the shared _forgetSearchItems helper.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n52 -> 47, same cause as the eslint move: the branch was replayed onto a new base and the floor\nis re-measured against it, not re-earned.\n\nMeasured with js_function_length.py in two detached worktrees, node_modules symlinked, nothing\nuncommitted in either:\n\n    origin/19.0-marin, no branch work   52\n    the same 9 commits replayed on it   47\n\nThe -5 is unchanged and is the same five splits the superseded note named: loadESMBundle in\ncore/assets.js, _rpc and its fetch continuation in core/network/rpc.js, computeAggregates, and\nuseDynamicPlaceholder. Origin independently reached 52, which is what the old note recorded as\nthis branch HEAD, so the two lines of work overlapped in the floor without overlapping in the\ncode.\n\nThis gate ratchets the OFFENDER COUNT, not excess lines, so splitting a long function lowers it\n-- and with `exact` mode that improvement fails the build unless banked. Banked here."
}
