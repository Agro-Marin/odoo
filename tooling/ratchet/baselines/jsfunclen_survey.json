{
  "count": 3,
  "measured_at": "afc037e38cabbfe1dcdbf3cb6142db0c657d66d8",
  "measured_root": "",
  "note": "STAMPED, NOT MOVED. The floor carried no `measured_at` at all, so it was a number nobody could reproduce: the guard that refuses a stamp this branch never had cannot fire on a floor with no stamp, and an unstamped floor may never have been true of any tree. Re-measured on a clean detached worktree at the stamped commit -- python tooling/architecture/js_function_length.py --addon survey --count -- and it reads 3, equal to the floor. The count does not move; only the stamp does.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nFunction-length budget raised 80 -> 90, matching the Python gate, so one number serves both trees. RE-MEASURED, not improved.\n\nNote the unit: this gate ratchets the OFFENDER COUNT, not excess lines as pyfunclen does, so raising the budget lowers it by dropping functions below the threshold rather than by forgiving lines. 13 of web's 70 offenders are between 81 and 90 lines. Measured in a detached worktree at HEAD with node_modules symlinked, only this change applied."
}
