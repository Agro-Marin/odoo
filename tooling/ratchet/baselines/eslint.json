{
  "count": 24333,
  "note": "24335 -> 24333 (-2). Both are pre-existing errors in files this commit already edits: a simple-import-sort/imports violation in tests/core/utils/numbers.test.js and one prettier/prettier missing comma in the same file, cleared by running eslint --fix over the touched files. Not a lint sweep -- the bulk autofix is still worth its own commit, per the note on the previous floor."
}
