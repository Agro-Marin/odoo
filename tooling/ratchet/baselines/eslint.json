{
  "count": 23510,
  "note": "23513 -> 23510. Three errors, one per file, all simple-import-sort/imports (\"Run autofix to sort these imports!\") at line 2, and all three fixed by 05333f0bea8 [REF] mail: strip comments and docstrings module-wide:\n\n  addons/mail/static/src/core/common/thread.js                 1 -> 0\n  addons/mail/static/src/discuss/call/common/call.js           1 -> 0\n  addons/mail/static/src/views/web/activity/activity_record.js 1 -> 0\n\nA side effect, not a fix anyone aimed at: each file had a comment sitting between its imports, which splits the block the sorter reasons about, so removing the comment left one already-sorted block. That commit landed no JS intent at all and moved this gate anyway, which is why an exact ratchet is worth having in the falling direction too -- nothing else would have noticed.\n\nMeasured on clean worktrees at 40933dbcf84 (the commit whose note set 23513, which re-measures at exactly 23513) and at HEAD, both with the one node_modules symlinked, using the command lint.yml prescribes: npx eslint . --format=json, summing errorCount. Warnings unchanged at 2 in both."
}
