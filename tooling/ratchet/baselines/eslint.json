{
  "count": 23224,
  "note": "23513 -> 23224, on the tree left by rebasing this branch onto origin/19.0-marin, and MEASURED there rather than carried over from either side.\n\nTwo disjoint moves, both against the same 23513 base:\n\n  origin       23513 -> 23510   three simple-import-sort/imports errors, one per file, cleared as a side effect of stripping the comments that sat between imports in mail\n  this branch  23513 -> 23227   x2ManyCommands moved to core/network/commands.js, 33 files, eslint --fix sorting their imports on the way through\n\nThe file sets do not intersect, so the composition 23510 - 286 = 23224 is exact, and the measurement confirms it to the unit. Measured the way lint.yml prescribes, on the rebased tree with this checkout's node_modules: npx eslint . --format=json, then [.[].errorCount] | add -> 23224."
}
