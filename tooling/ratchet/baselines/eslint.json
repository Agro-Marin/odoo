{
  "count": 24050,
  "note": "24278 (origin) / 24111 (replayed) -> 24050, re-measured after the rebase. Neither committed number was measured against this tree: the remote's predated the web architecture wave, the branch's predated the remote's 187 commits. Measured as CI does: npx eslint . --format=json, summed errorCount, with the committed toolchain. warningCount is 2, unchanged and ungated."
}
