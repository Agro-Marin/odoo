{
  "count": 23963,
  "note": "23969 -> 23963 (-6). Attributable in full to two calendar files, measured per-file against their HEAD content in the same tree (npx eslint <file>, counting error lines):\n\n- attendee_calendar_controller.js 18 -> 13: deleteRecord's dead `user.partnerId === record.attendeeId` re-test and the action_unlink_event call lost their arguments when the dead `attendee_id` parameter went away, and the call collapsed onto one line.\n- calendar_form_controller.js 22 -> 21: the same call site, now passing the recurrence policy the user actually chose in the dialog instead of the inline radio field.\n\nThe other four touched files are error-neutral by construction: calendar_notification_service.js kept its 2 pre-existing errors (the markup import and the re-poll floor were written to the repo's prettier config), attendee_calendar_model.js 13 -> 13, many2many_attendee.js 7 -> 7, and the new calendar_notification.test.js case is clean. Nothing was reformatted that this change did not touch.\n\nNOT measured with a full `npx eslint .` on the shared checkout: it carries other sessions' in-progress mrp/project/orm work, so the tree-wide number there is not HEAD's. The delta above is the one this change is responsible for."
}
