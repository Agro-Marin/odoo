{
  "count": 23565,
  "note": "auth_portal (ex auth_totp_portal + auth_passkey_portal) cleared to zero and onboarded to the whitelist: 23835 -> 23565 (-270).\n\nStacks on 437b6a8dd71's barcodes move (23963 -> 23835, -128). The two are independently measured against the same 23963 at d9fb99febd4 and touch disjoint trees -- addons/barcodes there, addons/auth_portal here, and that commit touches no auth_* file -- so the deltas compose: 23963 - 128 - 270 = 23565. Re-applied because the barcodes update landed on top of this one in the shared checkout and recomputed from 23963, dropping it.\n\nMeasured per-module against the pair's HEAD content (npx eslint <dir>): 270 across auth_totp_portal + auth_passkey_portal before, 0 in auth_portal after.\n\nThree steps, in order:\n- npx eslint --fix: 237 prettier findings, the whole of the pre-existing formatting debt. Neither module had ever been prettier-formatted; both were linted by js.configs.recommended + prettier, repo-wide.\n- Added \"addons/auth_portal\" to COMMUNITY_MODULES, beside the addons/portal it depends on. That grants globals.browser, which retires all 23 no-undef (document / location / setTimeout -- browser globals, never real defects), and applies the full ruleset: it surfaced 7 simple-import-sort (auto-fixed) and 5 eqeqeq.\n- By hand: the 5 eqeqeq are \"amount != 0\" / \"!= 1\" in the passkey tours, comparing a NodeList length to a number literal, so !== is the same comparison. The 8 no-case-declarations are fromField()'s four switch cases in totp_enable.js, each now a block. Every one of those cases ends in a return, so scoping them changes nothing that runs; default needs no block and did not get one.\n\nBehaviour verified, not assumed: /auth_portal is 0 failed, 0 errors of 8 tests after the reformat -- the four passkey portal tours, the TOTP portal tour (which is what drives fromField / fixupViewBody, the restructured switch), the revoke-hook test and the two invite-URL tests.\n\nNOT measured with a full \"npx eslint .\": the shared checkout carries other sessions' work, so the tree-wide number there is not HEAD's."
}
