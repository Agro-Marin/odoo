{
    "count": 24122,
    "note": "24333 -> 24122 (-211 against the recorded floor; -242 against HEAD, which had already drifted to 24364 without a floor update). The drop is this change's own: 37 module faces plus the @web/core/l10n/translation -> @web/core/translation move rewrote ~3300 import statements, and eslint --fix-type layout with prettier/prettier disabled re-sorted them. No file that was non-conforming before this change was reformatted -- only the 19 that this change itself broke, plus the new face files."
}
