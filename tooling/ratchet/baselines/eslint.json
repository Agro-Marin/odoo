{
  "count": 23835,
  "note": "addons/barcodes onboarded to the full ESLint ruleset and driven to zero: 23963 -> 23835 (-128), measured against the committed floor at HEAD d9fb99febd4. Counted per-directory, HEAD content in a clean worktree vs this change in the shared one (npx eslint addons/barcodes, counting error lines): 128 before, 0 after. Per-directory because the shared checkout carries other sessions' work, so a tree-wide `npx eslint .` there is neither HEAD nor HEAD-plus-this. The module was absent from COMMUNITY_MODULES, so only the repo-wide js.configs.recommended + prettier base reached it: 99 prettier/prettier and 22 no-undef (document, MouseEvent, CustomEvent, setTimeout -- browser globals the whitelist supplies). Adding it to the whitelist silenced the no-undef and enabled the full ruleset, which found 15 more (14 simple-import-sort, 1 prefer-const), all autofixed. No behaviour change: prettier reflow, import order, one let -> const in a test helper."
}
