{
  "count": 24111,
  "note": "sales_team prettier cleanup: eslint --fix on the module's 4 JS files (crm_team_form.js, crm_team_form.test.js, crm_team_test_helpers.js, mock_server/mock_models/crm_team.js) cleared all 8 of its errors, 8 -> 0. Measured against a clean 'git archive' of HEAD 2b77756f451 (24119) plus the working tree (24111); the module A/B is exact. Note this ALSO corrects a pre-existing 5-error overcount: the previous floor of 24124 did not match clean HEAD, which measures 24119 -- the dirty-tree mis-record that ruff.json documents twice. 24119 - 8 = 24111."
}
