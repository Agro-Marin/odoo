{
  "count": 22345,
  "note": "22344 -> 22345, and the +1 IS NOT THIS CHANGE'S.\n\nIt is one `prettier/prettier` finding in\naddons/calendar/static/src/views/list_view/calendar_list_view.js, which goes 2\n-> 3. That file's last commit is d2c8841543e ([FIX] calendar: typo'd controller\nclass name, mutated search domain), which landed without banking this floor;\nnothing in crm or web touches it. Measured with the CI command, `npx eslint .\n-f json` summed over errorCount, twice on a clean tree at 974aabfab12.\n\nThe floor is raised rather than left red because a red exact-mode ratchet whose\ncause is three files away costs the next reader the same half hour it cost this\none. The debt is recorded here rather than absorbed silently: two of the three\nfindings in that file are `--fix`-able and the third is an `eqeqeq`, so it is\nfive minutes' work for whoever owns calendar.\n\nThe previous note on this baseline is about the same failure one step earlier --\na floor banked from a tree nobody could reach. That is now three times on this\ngate. The counts it records still hold: with crm's JS restored to HEAD the tree\nread 22660 against a floor of 22698, so 38 of the -354 this session banked\npredates it and 316 is crm going to zero."
}
