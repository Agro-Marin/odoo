{
  "count": 23079,
  "note": "ONE UNIT, AND IT WAS PRE-EXISTING. 23080 -> 23079. website_livechat/static/tests/website_livechat_test_helpers.js carried a simple-import-sort/imports error at HEAD; routing that helper through defineLivechatModels() rewrote its import block and eslint --fix cleared it. Measured CI-shaped (npx eslint . --format=json, jq [.[].errorCount]|add) in a detached worktree at HEAD with only the six changed files copied in -- the shared checkout had five mail/static/tests JS files dirty from another session, and a repo-wide reading there would have banked their work as mine. Worktree: HEAD 23080, HEAD+mine 23079."
}
