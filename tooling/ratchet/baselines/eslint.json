{
  "count": 24129,
  "note": "24122 -> 24129 (+7). NOT a regression: the floor was recorded 8 too low and exact-mode has failed every build since. Re-measured from a clean tree with the repo's pinned toolchain at three points: the merge base 3394fbe3405 = 24333, the commit that recorded 24122 (the architecture refactor, whose own -211 claim stands) = 24130, and HEAD = 24129. So the refactor's drop was real but its floor was measured against something other than the tree it committed -- the same dirty-working-tree mis-measurement ruff.json's note records twice (546 -> 541 -> 539). The -1 from 24130 to 24129 is this branch's own: hoisting the odoo.isReady read out of the payload literal in error_beacon.js retired one error while merging the cause-chain beacon with the phase/dedup options. Command: npx eslint . --format=json --output-file=/tmp/eslint.json && jq '[.[].errorCount] | add // 0' /tmp/eslint.json"
}
