{
  "count": 24047,
  "note": "24050 -> 24047. Onboarded addons/resource to COMMUNITY_MODULES and cleared its debt: its static/src was previously matched only by js.configs.recommended + prettier, so it lacked globals.browser (window/document tripped no-undef) and was exempt from simple-import-sort et al. All 19 findings under the full ruleset were autofixable; the module is now clean. Measured as CI does: npx eslint . --format=json, summed errorCount, with the committed toolchain. warningCount is 2, unchanged and ungated."
}
