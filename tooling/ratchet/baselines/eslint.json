{
  "count": 24335,
  "note": "npx eslint . --format=json, sum of errorCount; eslint 10.6.0 (package-lock pin). 123299 -> 27890 (-95409) -> 24338 -> 24335 now. The -3552 is NOT this series' work: mainline measured 24338 against the committed floor of 27890 both before this series and after its first five commits, so the floor had simply never been re-measured since it was written on 19.0-t24263-suniagajose @ 7d8d534ed9c3. Locked in because the ratchet runs in exact mode, where an unrecorded improvement fails the build exactly like a regression. Of the 24338, the large majority remain prettier/prettier; the whole set is autofixable with 'npx eslint . --fix' and is worth doing as its own commit rather than smeared across feature work."
}
