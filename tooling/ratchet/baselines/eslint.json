{
  "count": 22338,
  "note": "REBASE RESOLUTION: two sides each moved this exact floor, so neither number described the merged tree.\n\n  22698  merge-base (764c731009f)\n  22345  origin/19.0-marin, banked by 82c627b2f72 -- crm's JS going to zero, plus the +1 in\n         addons/calendar/static/src/views/list_view/calendar_list_view.js that commit's note\n         records as not its own\n  22693  this branch before the rebase, banked by the sale audit: -7 prettier findings in\n         sale/static/tests/sale_order_line_field.test.js, the only eslint debt in `sale`\n\nThe two deltas are disjoint -- crm and calendar on one side, one file under `sale` on the\nother -- so the merged tree is 22345 - 7. Confirmed by measurement rather than by that\narithmetic: `npx eslint . -f json` summed over `errorCount`, the CI command, on the\nrebased tree.\n\nThe three prior notes on this baseline are each about a floor banked from a tree nobody\ncould reach. This resolution is the same hazard in its rebase form: taking either side\nverbatim leaves the gate red, in exact mode, in whichever direction was not measured."
}
