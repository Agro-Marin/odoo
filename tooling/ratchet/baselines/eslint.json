{
  "count": 23969,
  "note": "23970 -> 23969 (-1). Measured as CI does on a CLEAN worktree at HEAD (the shared checkout carries other sessions' in-progress work): npx eslint . --format=json, summed errorCount, committed toolchain (eslint 10.6.0). warningCount is 2, unchanged and ungated.\n\nThe -1 is entirely attributable: base_import's ImportAction dropped a `prettier/prettier` error when `onWillStart` was rewritten to stop calling `actionService.currentAction`/`currentController`, which the web architecture redesign had made private. Clean HEAD measured 23970 immediately before, matching the previous floor exactly, so no unattributed drift was absorbed here.\n\nThe rest of that file's 8 remaining prettier errors were left alone on purpose: 6 of base_import's 7 JS files are prettier-dirty under the repo's own .prettierrc (printWidth 88, trailingComma all), so the committed style predates that config. Reformatting lines this change did not touch would have buried a behavioural fix in unrelated churn."
}
