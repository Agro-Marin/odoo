{
  "count": 22344,
  "note": "22698 -> 22344, of which 316 is crm and 38 is not.\n\ncrm goes to zero: 314 formatting findings cleared by `npx eslint addons/crm --fix` over 20 files, plus the two `no-unused-vars` it cannot fix -- a one-position cycle has no position to compute, so `cyclePos: (x) => 1` in fill_temporal_service.js drops its parameter.\n\nThe other 38 predate this change and are the same failure the previous note is in capitals about: measured with the CI command, `npx eslint . -f json` summed over errorCount, the tree reads 22660 with crm's JS restored to HEAD. So the floor was 38 above a tree nobody could reach, for the third time on this gate. Do not read the -354 as one change's work.\n\nThe formatting pass is verified inert: `./hoot '@crm'` gave the same 13 failed / 12 passed with a character-identical failure list before and after it. Those 13 are fixed separately in the same commit."
}
