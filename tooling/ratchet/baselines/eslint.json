{
  "count": 22159,
  "measured_at": "b0184c33f4aeb08ac1eb044252e24f7070025603",
  "note": "22171 -> 22159, and the whole of the -12 is prettier/prettier inside the hr_* comment strip banked with it. Measured with lint.yml own command (npx eslint . --format=json, then the errorCount sum) on both sides: 22171 in a detached worktree at HEAD b0184c33f4a, which reproduces the standing floor exactly, and 22159 in the workspace checkout holding the strip. Attributed per file, and every unit is accounted for: hr_holidays/graph/hr_holidays_graph_model.js -1, hr_holidays/graph/hr_holidays_graph_renderer.js +1, hr_holidays/tours/time_off_allocation_warning_tour.js -1, hr_homeworking_calendar/calendar_homeworking.test.js -6, hr_org_chart/tours/hr_employee_test.js -1, hr_timesheet/graph_model_patch.js -3, hr_timesheet/project_task_analysis_graph_model.js -1. Net -12. The lone +1 is a prettier wrap that a removed trailing comment had been holding open; it is left as a finding rather than reformatted, because reformatting that file is not part of a comment strip. No rule other than prettier/prettier moved in either direction."
}
