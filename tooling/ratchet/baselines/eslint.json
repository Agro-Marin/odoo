{
  "count": 23550,
  "note": "event_barcode.js reformatted while removing its duplicated scanner branch: 23565 -> 23550 (-15). Measured per-file, HEAD content vs this change in the same tree (npx eslint <file>, counting error lines): 18 before, 3 after. The 3 that remain are no-undef on the browser global Audio -- addons/event is not in COMMUNITY_MODULES, so only the repo-wide recommended+prettier base reaches it and it gets no browser globals. Not onboarded here: that is its own change, and this commit touches one of its files. An eslint --fix run had reformatted all of addons/event; the ten files unrelated to this change were reverted after confirming with git diff -w that they carried nothing but quote-style and re-wrapping."
}
