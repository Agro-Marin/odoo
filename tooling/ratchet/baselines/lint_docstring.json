{
  "count": 32,
  "measured_at": "ea94c1e88c542d7f5d2f059397cf1d670c18c71b",
  "measured_root": "",
  "note": "STAMP REPAIRED AFTER THE SECOND SYNC OF 2026-09-07, NUMBER UNCHANGED. Five commits landed on origin/19.0-marin during verification and the replay onto them orphaned this stamp again -- the fourth and fifth time in two days that a rebase has invalidated a recorded commit reference. Graded by the run on the replayed tree: -i test_lint --test-enable --test-tags /test_lint on a fresh database, 0 failed, 0 error(s) of 315 tests, 315 started against 315 reported, with this class in the log by name. Exact-mode, so passing IS the measurement. Only the provenance moves.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMPED FOR THE FIRST TIME, NUMBER UNCHANGED. This floor carried an EMPTY measured_at, so --list could report it neither fresh nor orphaned -- it was unverified rather than clean, and had been silently exempt from the freshness check every other floor answers to.\n\nGraded by the same run that re-stamped lint_record_reference: `-i test_lint --test-enable --test-tags /test_lint` on a fresh database, 0 failed, 0 error(s) of 315 tests, with TestDocstring in the log by name. Exact-mode, so passing is the measurement that it reads 32. The count is not touched; what changes is that from here on a rebase can tell you when this floor stops meaning anything.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nCarried over unchanged from the Python literal. This gate reads the INSTALLED REGISTRY, so its count is a function of what is installed: at CI scope it measures 1, on a fuller install 32. That is why it is the one gate here invoked `exact=False` -- a lower reading is not an improvement to bank, it is a smaller install. Do not floor it at a CI-scope reading."
}
