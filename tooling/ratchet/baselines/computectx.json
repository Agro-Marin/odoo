{
  "count": 100,
  "note": "account: ten computes that read self.env.uid / self.env.user or call get_lang/format_* now declare it with @api.depends_context.\n\nFive for lang (_compute_payment_term_details, _compute_abnormal_warnings, _compute_example_previews, _compute_lang, _compute_new_values) and five for uid. Undeclared, each of these was cached under a key that ignored the very thing it read.\n\n110 -> 100. The floor was 112 when this work started and another session banked 110 while it ran; 100 is that 110 minus the ten measured here in a detached worktree at HEAD carrying only this change.\n\nCommand: python tooling/architecture/compute_context_deps.py --count"
}
