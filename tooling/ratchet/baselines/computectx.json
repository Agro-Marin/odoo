{
  "count": 56,
  "measured_at": "cad752ac96c3fb59ee6233d6202b3b9ef3977d61",
  "measured_root": "",
  "note": "DOWN ONE, WITH THE FOLD'S TWO OFFENDERS FIXED RATHER THAN BANKED. The 57 banked in 50d84a707910 reads 59 on that commit's tree: its fix, 09e57a0c7fc9, was measured before the previous sync put e99015f75bc1's fold underneath it (that sync rewrote it into 5eec2b535ff9), and the fold brought account.return._compute_visible_states and account.report.send._compute_mail_lang, both reading the language undeclared. Set diff against 1d872ce5b036 (floor 59, reads 59) accounts for everything else: the crm repair and the stock_picking_batch relocation 57's note already names. cad752ac96c3 declares @api.depends_context(\"lang\") on both. The -1 is purchase_requisition's _compute_price_unit_and_date_commitment_and_name, which 7a4fc89f51b0 replaced with _get_requisition_line, taking its get_lang read out of the compute. 57 + 2 - 2 - 1 = 56. Measured in a clean worktree of cad752ac96c3.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nWas 59, red at 63 since 8f4a84e81517 while ratchet.py --list called it clean, because the stamp resolves (CLAUDE.md \u00a74: an un-orphaned floor is not a verified floor). THE TREE MOVED AND THE SCAN DID NOT, measured rather than argued: the CURRENT scanner reads 59 against the tree at the old stamp and 63 against HEAD, the comparison \u00a74 says otherwise cannot be made after the fact. Set diff 6 added / 2 removed, but one pair is _compute_show_allocation RELOCATING from addons/stock_picking_batch into addons/stock in 666d2ca6f4be and cancelling -- so the +4 is FIVE new offenders, one relocation, and one genuine repair somebody made without banking (crm._compute_application_statistics_hook). A delta cannot see a relocation; only a set diff can. All six live offenders fixed in 09e57a0c7fc9 rather than banked at 63, taking it two BELOW the old floor: five wanted @api.depends_context(\"uid\") or (\"lang\"), and the sixth, account_change_lock_date._compute_lock_date_exceptions, declared @api.depends_context(\"user\", ...) -- a key the ORM does not populate, 3 uses in the tree against 100+ of \"uid\" -- so its cache never varied by user while reading self.env.user.id. A wrong key is worse than a missing one: it reads as handled to the gate and to a reader. Verification: -i account,stock,resource --test-enable --http-port, 3,870 tests started, 0 failures, 0 setUpClass errors. Banked from a detached worktree at 09e57a0c7fc9 because the shared checkout carries 39 paths of other sessions uncommitted work inside this gate scan scope, and ratchet.py refuses a dirty tree.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRebase resolution set this to the replayed side (86); origin had 59 and the merged tree measures 59. compute_context_deps.py --count on a clean tree at this commit reads 59."
}
