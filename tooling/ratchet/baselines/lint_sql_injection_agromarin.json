{
  "count": 13,
  "note": "A HELPER IS ONE FINDING NOW, NOT ONE PER CALL SITE. 22 -> 13, and not one query changed. `_checker_sql._visit_functiondef` yielded once per recorded call site, all anchored at the same `def` line and column, so `marin/wizards/sale_oyb_wizard.py:736` -- one helper, called from five places, two of which `_callsites` records twice -- counted as TEN of the twenty-two. The message names every distinct call site instead of one row per site.\n\nTwo findings that really are on the same LINE are still two: they differ by column, which `Finding.col_offset` carries. `odoo` reads 37 on 35 lines for exactly that reason and is unchanged by this, as is `enterprise` at 8.\n\nMeasured with `tooling/lint/py_lint.py <repo> --count`, the tool this scope is gated by."
}
