{
  "count": 25,
  "measured_at": "afc037e38cabbfe1dcdbf3cb6142db0c657d66d8",
  "measured_root": "",
  "note": "STAMPED, NOT MOVED. The floor carried no `measured_at` at all, so it was a number nobody could reproduce: the guard that refuses a stamp this branch never had cannot fire on a floor with no stamp, and an unstamped floor may never have been true of any tree. Re-measured on a clean detached worktree at the stamped commit -- python tooling/architecture/py_function_length.py --addon survey --count -- and it reads 25, equal to the floor. The count does not move; only the stamp does.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nFunction-length budget raised 80 -> 90, one number for both trees. This floor is RE-MEASURED at the new budget, not improved: no function was split or shortened for it. Measured in a detached worktree at HEAD with only this change applied, so the shared dirty tree is not in the reading."
}
