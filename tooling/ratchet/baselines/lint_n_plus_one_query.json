{
  "count": 380,
  "note": "381 -> 380, and the unit is NOT this change's: a84073fc0c9 ([FIX] base: update_list ... read one group's users per group) batched a per-record query in base and did not re-floor, because the floor was first written minutes earlier against the commit before it. Banked here so the gate is not left red in the falling direction.\n\n381 was 369 + 12: `search_read` and `name_search` joined `_QUERY_METHODS`, and every one of the twelve is a `for x in <records>:` with a query inside. What was measured and rejected -- `while` (13, all worklist or paging loops issuing a batched query per iteration), comprehensions (4, all short-circuiting `any(...)`), `read` (42, dominated by `file.read()`), `_search` (15, mostly loops over a handful of domains) -- is recorded in _checker_batch.py beside the method set."
}
