{
  "count": 245,
  "note": "1572 -> 245, re-measured on the rebased tree.\n\n  origin/19.0-marin        1572   (its committed floor, 1572, reproduces)\n  this branch, pre-rebase   245\n  merged                    245\n\norigin contributes nothing here -- it changes no Python in addons/mail -- so the whole -1327 is this branch, and mail_mail.py carries the visible part of it: four functions over the budget and now none.\n\n_send_one 101 -> 63, by _begin_sending (the durable placeholder and its commit), _deliver_all (the recipient list and the fold of each outcome), _record_send_success / _record_send_failure, and _reraise_send_error for a caller that passed raise_exception.\n_prepare_outgoing_list 128 -> 47, by _prepare_recipient_groups and _link_instead_of_attach.\n_split_by_delayed_batch 106 -> 68, by _open_quota_minute, _schedule_delayed_batch and _split_off_sendable_copy.\nsend 84 -> 60, by _record_connect_failure and _close_smtp_session.\n\nThe rest is the module-wide extraction the same branch did across mixin_mail_thread.py, mail_message.py, mail_activity.py and the gateway."
}
