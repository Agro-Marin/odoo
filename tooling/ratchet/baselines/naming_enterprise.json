{
  "count": 340,
  "measured_at": "666b434e1f980ad66c185d7d5f5732a8424ad81c",
  "measured_root": "enterprise",
  "note": "416 -> 340: the 76 enterprise overrides and declarations of the tail-marked domain family renamed by 42db56e84ac3 / enterprise 666b434e1f9 (two of the 78 the note above counted were exempt by annotation once read). Measured with odoo at 25dd59a0ed18 and enterprise at 666b434e1f9, clean detached worktrees. Still owed: the 280 the earlier notes attribute, and 60 more the 2026-09-10 widening reached (modals, generate-without-product, one-shaping-call).\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n416: one of the 417 went with the odoo batch 631c4224cb10 (an override of a renamed name); nothing else moved. Measured with odoo at a93654d995d1 and enterprise at 6d8584da2e9, clean detached worktrees. Still owed: the 280 the previous notes attribute and the 78 domain-family names.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nWIDENED SCAN, NOT A WIDENED TREE: 280 -> 417, 137 newly VISIBLE names, 0 new offenders, in the four rules odoo 84573c1c65f8 added to naming_vocabulary.py (the naming floor note has the argument): _get_<what>_domain 78, necessity modals 11, generate without a product 44 (eleven of them the l10n_ch_hr_payroll generate_*_report buttons, one finding), one shaping call under a producer prefix 5. Measured with odoo at 4e1fcdae6684 and enterprise at d24123871fd, both clean detached worktrees. The 280 previously owed are still owed and the previous note says whose they are; the 137 are owed the same way.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REPAIRED AFTER THE 2026-09-10 SYNC, NUMBER UNCHANGED. enterprise 19.0-marin was rebased onto origin (25 local commits over 6) and odoo onto its own; naming_vocabulary.py --roots enterprise --count reads 280 with odoo at 7aa312dfe3fc and enterprise at its rebased tip, all three repos clean. Only the provenance moves; the 280 are still owed and the previous note says whose they are.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nDOWN 32 FROM 312, ALL PAID, NONE OF IT SCAN. 31 are enterprise 40756faa3dc renaming every name the producer-without-product rule (odoo 145dff9562e2) had banked as visible-not-new -- the l10n_in_reports sheet writers to _add_<section>_sheet, the account_online tokens to _update_*, the paid-booking event to _create_*, the Swissdec button to action_*, and so on; the commit message groups them by the operation each body actually does. The 32nd is mrp_mps _assign_mps_sequence, paid by enterprise e8d361d3e5a (the enterprise mrp sweep on 1ad4b4cbb623's terms) and unbanked. So the rule that widened this floor by 31 two commits ago has now been paid down by the same 31, and the floor is back to what the tree earns. Measured on a clean detached lane, odoo 22864448d1ed beside enterprise 40756faa3dc.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nWIDENED SCAN, NOT A WIDENED TREE, TWICE OVER: 312 = 275 + 6 + 31. (1) 275 -> 281 arrived with odoo 1ad4b4cbb623 (sum/calc/cal joined ABOLISHED and the auto<verb> row), measured with that commit's gate against enterprise 671b80ff4ba and 275 under the gate one commit earlier: six newly VISIBLE names, unbanked by the commit that widened. (2) 281 -> 312 is this commit's producer-without-product rule: a _get_ / _resolve_ / _prepare_ whose body never returns or yields a value and instead stores into something or writes records is the Mutation row. 31 names, 23 wearing _prepare_ and 8 _get_, all -> _update_: l10n_in_reports 11 (the gstr1 spreadsheet writer's _prepare_<section>_sheet(workbook) family), web_studio 2, account_online_synchronization 2 (_get_access_token / _get_refresh_token assign the token and return nothing), and one each in l10n_se_sie4_import, l10n_lu_reports (get_file_name stores into options), appointment_account_payment, l10n_co_dian, website_generator, pos_urban_piper, industry_fsm_sale, l10n_cl_edi, test_l10n_hk_hr_payroll_account, l10n_ch_hr_payroll, industry_fsm_stock, social_linkedin, l10n_be_hr_payroll, sale_renting, partner_commission, l10n_de_pos_cert. Zero of them are new offenders; the six the same rules found here that were renamed ahead of them (1a10fe02ba4: five _prepare_*_domain and _create_missing_uom_hours) are why it is 31 and not 37. Measured on a clean detached lane, odoo 1ad4b4cbb623 with this gate beside enterprise 671b80ff4ba. The 31 are owed; --json lists them.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nWIDENED SCAN, NOT A WIDENED TREE: 275 against a floor of 213, and the tree read 195 under the old gate at the same commit -- so this is 80 newly VISIBLE names and 18 fewer offenders than the floor said, not 62 new ones. The shared gate now governs every file under a manifest and carries \u00a72.4.20 synonyms (here calculate 19, determine 4, populate 3, detect 1, seed 1). Measured at enterprise 7634473eeab with the gate from odoo 5d1a36d538ac, clean detached worktrees side by side. Largest owners: ai 13, l10n_es_reports 10, web_studio 10, sale_amazon 8, account_online_synchronization 7, esg 7, sale_lazada 7; by verb validate 72, fetch 47, build 31, calculate 19, make 15, assign 14, fill 13, retrieve 13, ensure 13, delete 12, append 12.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nDOWN ONE FROM UPSTREAM 214, THE web_map UNIT THIS BRANCH CARRIES. The 2026-09-09 second sync replayed 55 enterprise commits onto a9a804b42d4, where upstream had just measured 214 after the project_enterprise dissolve moved two abolished verbs into community project. That 214 was measured WITHOUT this branch, whose web_map `_delete_coordinates` rename pays exactly one more unit; on the settled pair naming_vocabulary.py --roots enterprise --count reads 213. Resolved to upstream 214 during the replay as a placeholder, because no textual resolution of two floors measured on two different trees can be right, and re-measured here. This is the third time the same unit has had to be re-established across a sync: the commit that first paid it (7bbf7723f87) was dropped as superseded in the first sync, so the unit is paid and its commit is gone, and only this note carries that. odoo a5a69651ee5, enterprise 749c3caa2b4, both clean.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nDOWN TWO, AND THIS RESTORES REASONING A REBASE CONFLICT DISCARDED. The project_enterprise dissolve moved `_fetch_planning_overlap` and `_fetch_last_date_end_from_dependent_task_for_all_tasks` into community project, where naming is a contract at zero, so they were renamed `_get_planning_overlap_rows` and `_get_last_predecessor_date_end_per_task` rather than carried. Two abolished verbs leave this scope with them; nothing here was renamed in place. Pinned to that one commit: 216 at its parent, 214 at it, 214 at HEAD. During the 2026-09-09 sync both sides of the replay had banked this floor against their own tree -- 216 from upstream widening the scan, 153 from this branch under the old scan -- and the conflict was resolved to upstream 216 as a placeholder because no textual resolution could be right. This is the measurement that was owed.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-stamped after the 2026-09-08 sync rebased odoo and enterprise onto their origins, which rewrote every SHA and orphaned this floor's measured_at. Only the provenance moved: naming_vocabulary.py --roots enterprise --count reads 216 against the settled pair, the banked count unchanged.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nThe SCAN grew, not the tree. naming_vocabulary.py measure() implemented \u00a72.4.13's scope as a class-membership test, so three populations the section governs were counted by census() and gated by nothing: module-level functions and plain-class methods in an addon's models/ wizard/ wizards/ files, and functions nested inside either. odoo ca24a4b847bd put all three in the population. 155 -> 216 is 61 newly VISIBLE names -- 35 nested, 17 on plain classes, 9 at module level -- and NOT 61 new offenders; enterprise's tree is unchanged. The same widening found 26 in addons/ and 6 in agromarin, both swept to zero (689b6949126c, agromarin ac4033bba), so those floors held at 0 and only this one moves, enterprise having no sweep behind it yet. Measured on detached worktrees at odoo 1011731b50d8 and enterprise 68239d338a1, never from the shared checkout -- which for an hour reported 216 against HEAD's gate because it was reading an uncommitted measure(). A floor whose scan widened is indistinguishable from one whose tree regressed, and one whose scan NARROWED is indistinguishable from one somebody paid down, so this note says which: the instrument moved.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nDOWN ONE AFTER THE ENTERPRISE REBASE, RE-STAMPED AT 401369e7fe3. 156 before, 155 after 28 upstream commits arrived. CLAUDE.md section 4 predicts this direction and the reason: a floor carried across a rebase reads high, because the branch banked before upstream's improvements reached it. This is a count of specific offences -- methods spelled with section 2.4's abolished verbs -- rather than a whole-tree sum, so the decrease cannot be one improvement paying for a regression somewhere else. Re-measured in a clean detached worktree laid out beside a clean odoo worktree.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REFRESHED, NUMBER UNCHANGED. The old stamp named a commit the 2026-09-02 sync replayed away, so --list flagged it ORPHANED-BASE and the sibling architecture.yml exited 2 asking to be re-measured rather than comparing -- neither green nor red. Re-measured in a clean detached worktree of enterprise at f6fa11352db, laid out beside a clean odoo worktree exactly as the lane does, because these gates resolve the community fork as a sibling and reading one alone inflates the count: unresolved_calls_enterprise measured 11 pointed at the sibling on its own and 3 in the pair.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMPED AGAINST THE RIGHT REPOSITORY. The floor already carried a `measured_at`, but it was an ODOO commit for a gate that measures enterprise, so it resolved cleanly in the wrong checkout and said nothing about the tree that was counted -- `--list` rendered it STAMP-PREDATES-ROOT. Re-measured on a clean detached worktree of enterprise -- python odoo/tooling/architecture/naming_vocabulary.py --roots enterprise --count -- and it reads 156, equal to the floor. Re-banked with `--update --root enterprise`, which records the enterprise commit.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-banked at a reachable commit after this branch was rebased onto origin/19.0-marin. --list had flagged it as banked at a commit that is not in HEAD history: the rebase replaced every commit on this side, so each measured_at named an object no longer reachable. Re-measured on the merged tree rather than carried over. 159 -> 156. Upstream's 159 already carried the iap_extract retirement; the three further units are this branch -- helpdesk_sale_timesheet _confirm_linked_sale_orders, sale_planning _get_or_create_uom_hours, l10n_ec_edi _check_withhold_data_on_post."
}
