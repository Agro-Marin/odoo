{
  "count": 96,
  "note": "THE FLOOR WAS ALREADY STALE BEFORE THIS CHANGE. 104 -> 96, and the eight units split into two groups that were banked together only because the first was never banked on its own.\n\n  committed HEAD, measured in a clean worktree   100   (floor still read 104)\n  this change                                     96\n\nFOUR WERE ALREADY TRUE AT HEAD. A worktree checked out at ac6b7c05d45 and measured with the CI command reads 100 against a committed 104, so `prettier_scss` was failing in the improvement direction before this change touched anything -- an exact ratchet fails on an unbanked decrease exactly so this cannot sit unnoticed, and it had.\n\nFOUR ARE THIS CHANGE. The mask repair edits four files that were themselves on the unformatted list, and running the repo formatter over an edited file is not optional:\n\n  addons/web/static/src/scss/bootstrap_review.scss\n  addons/web/static/src/scss/pre_variables.scss\n  addons/web/static/src/scss/scheme_rules.scss\n  addons/web/static/src/views/settings/settings_form_view.scss\n\nAttribution is a per-file set difference against the clean worktree, not arithmetic: those four are the only entries the two lists differ by, and 100 - 4 = 96 agrees. The fifth file the change touches (secondary_variables.scss) was already formatted, and the sixth (account/static/src/scss/account_reconcile_model.scss) is deleted and was not on the list either, so neither moves the count."
}
