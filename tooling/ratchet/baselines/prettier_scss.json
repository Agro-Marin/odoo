{
  "count": 56,
  "note": "96 -> 56, and the whole of the -40 is addons/web: every .scss file the module owns that prettier would rewrite, rewritten.\n\nWhitespace only -- prettier does not move a declaration, and sass is\nwhitespace-insensitive in the places it touched. The reformat was verified where\nit could actually break something rather than by inspection: the asset & palette\nlane (TestSchemeDuplication, TestDarkSiblingScope, TestGreyRampToken,\nTestColorSchemeReads, TestBundlesAssemble, TestDarkBundles, TestTokenLayer,\nTestDesignSystem, TestWebBundleSize over the 21-module install asset_lint.yml\nnames) reads 0 failed, 0 error(s) of 50, and TestDarkBundles compiles the dark\nbackend bundle with Dart Sass on the reformatted sources.\n\n  addons/web        40 -> 0\n  everything else   56 -> 56\n\nMeasured with the CI command, `npx prettier --list-different \"**/*.scss\"`, on a\nclean worktree at 8b8a26297b0, which reads 96 -- the floor it replaces still\nreproduces."
}
