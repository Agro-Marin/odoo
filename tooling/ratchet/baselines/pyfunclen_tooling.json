{
  "count": 831,
  "note": "Rebase onto origin/19.0-marin. The floor was 879 and the composed tree measures 831: origin has since split several of tooling/ own long functions, and this branch adds payment_vocabulary already in its split form. An exact ratchet fails in both directions, so the fall is banked here rather than left to read as somebody else debt. Measured with py_function_length.py --addon tooling --count in a detached worktree at the rebased tip."
}
