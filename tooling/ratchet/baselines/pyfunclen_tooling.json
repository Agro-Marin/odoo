{
  "count": 536,
  "measured_at": "20258c0f8b08475469b55b3c2dc0aa97fa6a1711",
  "measured_root": "",
  "note": "Three gate CLI mains split; the number fell by shortening, not by banking a grown tree. 723 -> 536, and every unit of the 187 is one function dropping under the 90-line budget: js_private_access.main 171 -> 29 lines (81 back) into survey()/Survey, _build_parser, _report_doc_block, _print_json, _print_contracts and _print_report; mixin_coupling_check.main 160 -> 24 (70 back) into _build_parser, _explain_edge, _collect_reports, _print_json, _print_composition and _check_verdicts; js_public_surface.main 126 -> 53 (36 back) into _build_parser, _print_json, _print_report and _print_drift. 81 + 70 + 36 = 187. No other tooling function moved, and core/addons/tests measured 0/12192/0 on both sides. Behaviour-preserving, measured not argued: 25 invocations across the three tools (--help, bare, --count, --count-undeclared, --count-cross-tree, --cross-tree, --top 0, --check, --check-doc, --json, --explain hit and miss, --composition Field, --addon mail, invalid --addon) byte-identical in stdout, stderr and exit code against a worktree of the parent commit.\n\nPREVIOUS NOTE, kept as the record of how the floor got to 669:\n\nFunction-length budget raised 80 -> 90, one number for both trees. This floor is RE-MEASURED at the new budget, not improved: no function was split or shortened for it.\n\nTHIS FLOOR WAS ALREADY RED AT HEAD, THE FALLING WAY IS NOT WHY. Committed 817; the tree measured 840 at the old 80-line budget, verified in a detached worktree at HEAD with nothing applied, so architecture.yml was failing on this gate for everyone before this change. 669 is the reading at the new budget with the same worktree, and it supersedes both numbers."
}
