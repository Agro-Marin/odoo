{
  "count": 831,
  "note": "Set on the composed tree after rebasing onto origin, and 879 -> 831 is one function: js_extension_surface.py::main, which this branch grew from 128 lines to 169 when it added the disposition and explain worklists. Origin measured 879 at its own tip and this branch measured 920, so the floor described neither tree. main was a dispatcher, not a long procedure, so what came out of it is the branch: _build_parser, _print_dispositions, _print_overriders, _print_drift_report (itself splitting into _print_new_and_gone and _print_orphaned_and_unmarked), _print_drift_json and _run_doc_flags. main is 45 lines and nothing in the file is over 80, which is why the floor falls 48 past the 41 the regression cost rather than stopping at 879."
}
