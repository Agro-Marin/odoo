{
  "count": 0,
  "measured_at": "f654fe0d29f43b0ffc000b128601f9b5c00ca9f4",
  "measured_root": "",
  "note": "CONTRACT RESTORED, NOT A FLOOR MOVED. This is floored at 0 with a file rather than without one, so the zero is a won argument and not a gate born clean. The rebase brought it to 1: mail_template.py:583 passed %(name)r to _(), which E8504 refuses because %r leaks Python syntax into a user-facing sentence -- and, as odoo-13 observed, %r on a str picks its quote character from the content, so a value containing an apostrophe would change the sentence's punctuation under the translator's feet. Repaired in ca2754cb8af rather than banked upward, and re-stamped at f654fe0d29f. odoo-13's translation_catalog entry carries the new text.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REFRESHED, NUMBER UNCHANGED. Re-measured with 'py_lint.py odoo/odoo odoo/addons --count' in a clean detached worktree of ed0d70a60d6. The scope is confirmed rather than assumed: lint_sql_injection is NOT orphaned, its floor is 9, and the same command reads 9, so this invocation reproduces the odoo-scope floors the test_lint lane sets. The old stamp named a commit the 2026-09-02 sync replayed away, so architecture.yml exited 2 asking to be re-measured rather than comparing.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMPED, NOT MOVED. The floor carried no `measured_at` at all, so it was a number nobody could reproduce: the guard that refuses a stamp this branch never had cannot fire on a floor with no stamp, and an unstamped floor may never have been true of any tree. Re-measured on a clean detached worktree at the stamped commit -- odoo-bin -i test_lint --test-enable --test-tags /test_lint at CI scope -- and it reads 0, equal to the floor. The count does not move; only the stamp does.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n8 -> 0. NONE OF THIS IS MINE.\n\nMeasured at 3bbd93238b3 in a detached worktree with no working-tree state applied:\nthe rule reports 0 while its floor stands at 8, so the gate has been red in the\nfalling direction for as long as whatever fixed those eight has been in. base\ncontributes nothing to this rule either before or after this change.\n\nBanked here because this commit re-measures the same scan for six other rules and\nleaving one of them red would make the next reader diff a count nobody owns."
}
