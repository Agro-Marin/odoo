{
  "count": 0,
  "note": "8 -> 0, measured at the CI scope. The rule is now clean; with no baseline file a floor of zero is implied, but the file is kept so `ratchet.py --list` still shows the gate rather than dropping it from the roster."
}
