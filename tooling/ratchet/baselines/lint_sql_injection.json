{
  "count": 17,
  "note": "21 -> 17: odoo/orm reaches zero on this rule, all four by waiver, because none of the four interpolates anything a caller controls.\n\n  runtime/environment.py  cr.execute(query) sits two lines under isinstance(query, SQL).\n                          The checker cannot see a type test, and that test is the\n                          strongest guarantee this gate exists to ask for.\n  runtime/registry.py     executes LAG_SQL, a module constant.\n  models/mixins/write.py  casts with column_type[1], the field's own declared column type.\n  fields/temporal.py      formats a granularity looked up in READ_GROUP_NUMBER_GRANULARITY,\n                          a module-level dict, immediately after the raise that rejects\n                          any key not in it.\n\nUNION-MEASURED, not subtracted. The odoo/tools work that landed as ea183558595\ntook this floor 24 -> 21 and predicted 17 jointly; measured here at CI scope with\nonly this change applied it is 17, so the two scopes' findings are in fact\ndisjoint. That check is cheap and this gate is exact in both directions.\n\nMeasured -i test_lint at CI scope (--addons-path odoo/addons,addons) in a\ndetached worktree at ea183558595: 21 with nothing applied, 17 with only this\nchange's files copied in."
}
