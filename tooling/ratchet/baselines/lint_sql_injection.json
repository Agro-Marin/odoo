{
  "count": 37,
  "note": "37, against the 36 the Python literal claimed -- the gate was red by one at 598cf211cc2 and this is the tree's own reading. Note the unit: 37 findings on 35 distinct lines. `mixin_sql_report.py:191` and `base_partner_merge.py:398` each carry two violations, which `Finding` now separates by column.\n\nFirst baseline. The floor used to be a Python literal in the test module; twenty-four of test_lint's last forty commits changed nothing there but an integer and the comment above it, and five of the six gates red at 598cf211cc2 were floor drift rather than defects. `assert_ratchet` now takes a gate name and refuses an integer outright, so a floor cannot go back into Python. Measured at CI scope -- `--addons-path=odoo/addons,addons`, only test_lint installed -- against a `git archive` of HEAD carrying this change and nothing else, because the shared checkout holds other sessions' uncommitted work."
}
