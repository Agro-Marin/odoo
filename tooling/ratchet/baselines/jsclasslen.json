{
  "count": 7055,
  "measured_at": "c65b67c822e7fcc3cbf5140cd7c92a5c8cf8b0d1",
  "measured_root": "",
  "note": "RE-STAMP ONLY, re-measured and unmoved at 7055 on the merged tree. The second rebase onto origin/19.0-marin rewrote the commit this floor was stamped at, leaving it ORPHANED-BASE or UNCHECKED -- which makes it unfalsifiable, the failure mode that let jsclasslen hide a 79-line regression earlier in this session. Re-stamped at a commit this branch holds.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REFRESHED AFTER THE 2026-09-08 SYNC, NUMBER UNCHANGED. 19.0-marin was replayed onto origin/19.0-marin (10 commits, 2 conflicts resolved), so every SHA changed and this stamp was orphaned by the replay -- the flag working, not debt appearing. Re-derived at the landed tip 600e97b75fa in a CLEAN detached worktree, not the shared checkout, which carried 42 uncommitted paths of a peer session at the time; the gate reads exactly what the floor already said, so no unit of this figure was ever phantom and nothing here is attributable to the sync. The gate resolves its own ROOT from __file__ over odoo/ and addons/ and loads no sibling repo, so the reading is sibling-proof and the odoo-only worktree is a sound scope for it. node_modules was symlinked and esbuild verified executable before measuring -- without it this gate exits on a bare Node banner with no count, which reads as a broken gate rather than a missing dependency.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nHomeMenu is no longer an offender: 7092 -> 7055. addons/web/static/src/webclient/home_menu/home_menu.js reads 396 non-comment lines against the 400 budget, having been over it for this gate every day it has existed. The number moves by 37 for a 41-line change because the whole remaining excess goes, not a part of it. b0cbf29 moved the search box -- what is typed, where the focus goes, the window keydown listener, the IME composition flag and the FooterComponent the command palette is handed -- into useHomeMenuSearch() in home_menu_search.js, which is 129 lines and declares one 8-line class, so nothing moved into a scope this gate cannot read. Across the whole pass the component went 544 -> 396 with no feature dropped: a layout object, the arrow keys geometry, the badge rule both launchers shared, and now the search box. Measured with node_modules symlinked in a clean detached worktree at the tip and at its parent, which git confirms is the real predecessor: 7055 and 7092. NOT measured in the shared checkout, which carries an ended peer session comment sweep across addons/web.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nHomeMenu shed six non-comment lines: 7098 -> 7092, addons/web/static/src/webclient/home_menu/home_menu.js, read at 437 against its 400 budget. 6bd03d966d5 moved the drag handler arithmetic into orderAfterDrag(order, movedId, afterId) in home_menu_layout.js, beside the other pure layout rules, and gave it the guard the old comment already claimed: an app the order does not hold answers null rather than reaching splice(-1, 1), which removes the last entry and reorders an app nobody dragged. home_menu_layout.js is 217 lines and not an offender, so the delta is the component alone. Measured with node_modules symlinked in a clean detached worktree at 6bd03d966d5 and again at its parent f7accb8d6b1, which git confirms is the real predecessor: 7092 and 7098. NOT measured in the shared checkout, which carries an ended peer session comment sweep across addons/web.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nHomeMenu shed thirteen non-comment lines: 7111 -> 7098, addons/web/static/src/webclient/home_menu/home_menu.js, read at 443 against its 400 budget. 33268be677d moved the rule \"an app the user hid is not on the grid, and one they pinned comes first in the order they pinned it\" out of both launchers into shownApps(config, apps) and pinnedApps(config, apps) in home_menu_layout.js, which QuickLauncher had been writing out a second time in its own spelling. HomeMenu.pinnedApps also stopped re-filtering displayedApps to rebuild the shown set that shownApps had just computed, and _isShown lost its last caller. home_menu_layout.js grew but is 190 lines and not an offender, so the delta is the component alone. Measured with node_modules symlinked in a clean detached worktree at the tip and at its parent, which git confirms is the real predecessor: 7098 and 7111. NOT measured in the shared checkout, which carries an ended peer session comment sweep across addons/web.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nHomeMenu shed one more non-comment line: 7112 -> 7111, addons/web/static/src/webclient/home_menu/home_menu.js, read at 456 against its 400 budget. 1cc37120487 is again a net of two directions: making the keyboard-selection reset conditional on the grid actually changing shape ADDED lines, and deleting two members that were only indirection took more -- this.orm, assigned in setup and read once to hand to the layout object, and get maxIconNumber(), which returned the module constant APPS_PER_ROW to one caller in the same file with no override anywhere in the workspace. Measured with node_modules symlinked in a clean detached worktree at the tip and at its parent, which git confirms is the real predecessor: 7111 and 7112. NOT measured in the shared checkout, which carries an ended peer session comment sweep across addons/web.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nHomeMenu shed five more non-comment lines: 7117 -> 7112, all of it addons/web/static/src/webclient/home_menu/home_menu.js, read at 457 against its 400 budget. 2cc9f3804c1 is a net of two movements in opposite directions: the badge work (one appBadge value and one web.AppBadge template shared by both launchers, plus re-counting when the app list changes) ADDED six lines to the component, and deleting the isPinned/isHidden forwarders -- the template asks layout directly now, as it already asks subscription -- took eleven. Neither badges.js nor home_menu_layout.js is an offender, so nothing moved into a scope this gate cannot read. Measured with node_modules symlinked in a clean detached worktree at the tip and again at its parent, which git confirms is the real predecessor: 7112 and 7117. NOT measured in the shared checkout, which carries an ended peer session comment sweep across addons/web.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nHomeMenu shed 82 non-comment lines: 7199 -> 7117, all of it addons/web/static/src/webclient/home_menu/home_menu.js, which the gate reads at 462 against its 400 budget (was 544). 866ef83334c moved two responsibilities out of the component rather than shortening it in place: HomeMenuLayout (home_menu_layout.js) took the eleven members that decide which apps are pinned, which are hidden, what order they sit in and where that is written down, and grid_navigation.js took keyboardRows and _updateFocusedIndex as two pure functions over the shape on screen. Neither new module is an offender -- HomeMenuLayout is 155 lines including its docblocks, grid_navigation.js declares no class -- so the whole delta is the component getting smaller, not mass moving to a scope this gate does not read. Measured with node_modules symlinked in a clean detached worktree at 866ef83334c and again at its parent 6f696628b2d, which git confirms is the real predecessor: 7117 and 7199. NOT measured in the shared checkout, which carries an ended peer session comment sweep across addons/web; comment lines do not count here, but a stripped file still shifts class bodies and the checkout was not a sound tree to read.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n7209 -> 7199, and the whole -10 is c10fc9f6b91 (the app-launcher render fix), attributed by measuring its own parent rather than by subtracting two readings: c10fc9f6b91~1 reads 7209 and c10fc9f6b91 reads 7199, with the only other two commits in the range being a baseline note and a test file, both of which the gate does not scan. That session could not have banked it themselves -- this gate was hours old when they committed and is not in any runbook they would have read. Measured in a clean detached worktree of HEAD with node_modules symlinked; the shared checkout agrees at 7199 now that their work has landed, which is also the retraction of the earlier note's claim that the 7199 there was contamination: it was their real improvement, uncommitted at the time.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nCORRECTION TO THE NOTE BELOW, WHICH WAS WRONG. It claimed 'a comment sweep moves this floor and the drop is not an improvement'. It does not move it at all, and the sentence before that claim is the reason: the gate excludes comment-only lines, so deleting a comment block shrinks the class span and the excluded count by the same N, and the excess is unchanged. Measured against a 3626-line comment sweep across addons/web and addons/point_of_sale: 7209 in a clean worktree of HEAD before and after, 0 contribution. The 7199 the shared checkout read throughout is another session's uncommitted work on webclient/home_menu, not the sweep. Kept rather than replaced because a floor that loses the record of what it once asserted costs more than a tidy note gains -- and because the wrong claim was mine, repeated to two other sessions before anyone checked it. Count unchanged at 7209; this stamp moves the note, not the floor.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nNew gate, first floor. addons/web/static/src holds 33 classes over the 400-line budget for 7209 excess code lines, and no existing gate can see any of them: jsfunclen holds every JS function under 90 lines, and a class of short methods is invisible to a per-function budget -- the argument py_class_length.py was added for on the Python side, which had no client-side counterpart. Largest: FlowEditor 1267 over 63 methods (core/flow_editor/flow_editor.js), StaticList 1041, ListRenderer 963, CalendarModel 918, RelationalRecord 871. Unit is excess lines rather than offender count, so splitting one huge class into two large ones lowers the number rather than raising it. Comment-only lines are excluded, matching jsfunclen's eslint skipComments:true, so the two figures stay comparable -- which also means a comment sweep moves this floor and the drop is not an improvement. Measured in a clean detached worktree of fff4eb9a9f4, the commit that adds the gate, node_modules symlinked."
}
