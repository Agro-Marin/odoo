{
  "count": 85,
  "note": "87 -> 85, and both units are `sessionInfo` in addons/web/static/src/service_worker.js. It was declared `let sessionInfo = null` and assigned an object later, which under noImplicitAny is two errors, not one -- TS7034 where it is declared and TS7005 where it is read. 9886d8e3639 gave it `/** @type {any} */` in passing while doing something else, so the lane has been two units below its floor since, and an exact ratchet fails in the falling direction.\n\nPer lane at 8730c943415, measured on a clean detached worktree with the main checkout's node_modules:\n\n    /web/service-worker.js     web + mail   51 -> 49\n    /pos/service-worker.js     point_of_sale  3 -> 3\n    /event/service-worker.js   website_event_track  33 -> 33\n\nAttributed by diffing the lane's error set between 96f67b1067e -- the commit the previous note names as the seed, and which still resolves -- and HEAD. Three commits touched the lane's files in between; only that one moved the count."
}
