{
  "count": 0,
  "note": "1 -> 0. The last one was fields/_field_description.py: env._(self.falsy_value_label) is not extractable HERE, but the string is booked into the catalogue at the field declaration, which is where every producer spells it _lt() -- project, project_hr and project_task all do. The exception is sale_timesheet, whose falsy_value_label='Non-billable' is a bare str and therefore in no catalogue at all; that is a separate finding this gate cannot see and is left for its own change."
}
