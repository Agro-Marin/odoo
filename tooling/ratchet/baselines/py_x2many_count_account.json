{
  "count": 1,
  "note": "3 -> 1. account.payment loses its two hand-written counters to fields.Count. What remains is one search_count in a loop in account_secure_entries_wizard._compute_warnings, which counts draft entries over a date domain rather than an x2many field -- there is no collection for a Count to name, so this floor stops at 1 rather than 0."
}
