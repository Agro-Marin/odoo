{
  "count": 0,
  "note": "account: the one counter was search_count() inside _compute_warnings loop in account_secure_entries_wizard. The per-wizard body of that compute is now _get_warnings(), so the loop carries no query.\n\n1 -> 0.\n\nCommand: python tooling/architecture/py_x2many_count.py --count --addon account"
}
