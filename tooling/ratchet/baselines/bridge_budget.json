{
  "count": 92,
  "measured_at": "5fb6ea70f5636edb49e0849d7c8aae816b088b6f",
  "measured_root": "",
  "note": "NOT three bridges folded -- three that crossed the 60-pyLOC budget when ruff format reflowed them, and are therefore no longer counted. Named so they are not lost: l10n_be_pos_restaurant (59 pyLOC before), l10n_it_edi_sale (59), l10n_sa_pos (49). Each is still an auto-installed bridge with 2+ triggers and still a candidate to fold into a parent; what changed is the measurement, not the code. A future pass that wants them back should raise MAX_PYLOC rather than expect this floor to find them.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REFRESHED AFTER THE 2026-09-04 REBASE, NUMBER UNCHANGED. 19.0-marin was replayed onto origin, so every SHA changed and the stamps written hours earlier were orphaned again -- the same mechanism, and the flag working. Re-measured at b67fa612706 in a clean detached worktree, and it reads what the floor already said. The invocation is confirmed rather than assumed: lint_sql_injection is not orphaned, its floor is 9, and the same run reads 9. Scope trap worth recording, because I hit it: run from the REPO ROOT, where 'odoo/' means the core package and the lint roots are 'odoo addons'; run one level up and ruff reports 3,117 findings from the bundled addons against a hard-zero floor.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nUP ONE, AND THE JUDGEMENT IS THE POINT RATHER THAN THE NUMBER. The gate is a count of auto_install bridges under 60 pyLOC, and its own docstring says a fold is a decision per module taken by reading the list, with the floor as where that decision is recorded.\n\nThe new entry is documents_speech, 18 pyLOC over documents and speech. It stays a bridge because there is nowhere to fold it: into documents makes documents depend on speech, into speech makes speech depend on documents, and both are inversions of a dependency that must not exist. That is the same shape the docstring already exempts for an OPL-1 dependency kept out of an LGPL-3 parent.\n\nEntered at a9fce94c98e and unseen until now because this floor was ORPHANED-BASE, so architecture.yml exited 2 asking to be re-measured rather than comparing. Re-measured in a clean detached worktree of 16691613dd8.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nOpened at 94: every auto_install module in addons/ with two or more triggers and under 60 pyLOC (odoo/addons holds none). 21 of the 94 ship no Python at all. Each is a fold decision per module, not a sweep; the floor moves when one is taken.\n\nCommand: python tooling/architecture/bridge_budget.py --count"
}
