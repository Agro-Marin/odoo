{
  "count": 177522,
  "measured_at": "5e71e8f83d6348bda9325d86eff1af1dd4b3292a",
  "measured_root": "",
  "note": "Measured in detached worktrees: 177505 at ddbcff1c46a (parent, equals the floor) -> 177522 at 5e71e8f83d6. The +17 is all 5e71e8f83d6: StockMove grows by the unit conversion in _prepare_generated_move_line_vals (~9 lines) and StockMoveLine by three multi-line _get_putaway_strategy calls that now pass quantity_product_uom (~8 lines); the new TestPutawayQuantityUnit class is under the 400-line budget and contributes nothing. Banked as interim; the two classes are the first and third largest under addons/stock and owe a split, not more banking.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nMeasured in a detached worktree at e2c886f27e7: 177612 -> 177505. Two moves inside that: 41a33409a15 (stock sibling-reservation reader and its test class) had taken the floor 177588 -> 177612 unbanked, and e2c886f27e7 (stock_picking_batch) gives back 107 -- StockMoveLine loses the O(n^2) wave-compatibility loop, four never-overridden hooks and two per-line criteria methods; StockPicking loses two redundant overrides; the tests grow by eight.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nApprovalRequestHelper 1113 -> 934, the escalation/reminder cluster moved into approval_request_escalation.py (4ea9a04bb9a). The new class is 191 lines, under the 400 budget, so it contributes nothing: the whole -179 is the helper. Per-class diff against the previous floor tip reads 305 classes over budget on BOTH sides with exactly one changed, so nothing else entered or left the gate and the delta is attributable to this one commit rather than bracketed by two totals. This REPLACES the floor d6ba3d7551d banked at 177767 one commit earlier and repays it 25 times over: that floor recorded a5079a4ca50s +7 as unavoidable and said in its own note that what the gate wanted was this split rather than a bump. It is now made, so the earlier note should be read as history, not as outstanding debt. What remains: the helper is still 534 lines over the budget and is still the module largest class, and approval carries eight more classes over budget for 2386 excess lines in total. This floor detects the next regression; it does not claim the file is done.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-banked to restore comparison, NOT to absorb any unit of this commit or the one before it: neither adds anything to the gate. The whole +7 is ApprovalRequestHelper 1106 -> 1113 in a5079a4ca50 (\"[FIX] approval: escalate once, and post the notice as markup\"), which is a correct fix -- a translated str carrying tags reached readers as visible source, 1778 such notices in production -- and its seven lines are the markupsafe import, the rewrap of the body into Markup(...) % {...}, and a three-line comment saying why the markup is the template and the values are its arguments. Measured 177767 in a detached worktree at 1c8937169d1 and 177760 in one at aab5cabbf83, the commit that banked the previous floor, with the per-class diff between the two reading 305 classes over budget on BOTH sides and exactly one changed. So the previous floor was right at its own tip and 12 commits have landed since; the delta is attributable to one commit because the diff says so, not because two readings bracket it. What this floor does NOT settle, said plainly rather than left for a green reading to imply: ApprovalRequestHelper is 1113 lines against a 400-line budget, 713 over, and what it wants is a split -- approval.request is already spread over six _inherit files and the escalation/reminder cluster is a coherent seventh -- not a bump. Reclaiming the 7 elsewhere was looked for and refused twice: the class holds no dead member (_get_sequence_manager reads dead inside addons/ and is called by agromarin/approval_hr, which a grep of addons/ alone would have missed), and hoisting the notice template to a module constant would take the literal out of self.env._() and drop the term from approval.pot.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-measured after the 2026-09-04 origin syncs, which orphaned the stamp: the old floor named 7f821114c6c0, a commit two rebases have since rewritten away, so the gate exited 2 with \"re-measure\" and compared nothing rather than passing or failing. 178124 -> 177760 on a clean detached worktree of HEAD. The number is 364 BELOW the dead floor, so nothing is absorbed upward by this re-bank. What it does contain, said plainly rather than left for a green reading to imply: HrEmployee is 2709 lines and still carries the +100 that CLAUDE.md section 9 records as unbanked, and the addons/resource comment strip that made this gate read green by 800 is still what pays for it. This floor restores the ability to detect the NEXT regression; it does not settle that one.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-stamped at the branch commit for the reason pyclasslen gives. Count unchanged (178124).\n\nBorn at 178124: 305 classes over the 400-line budget in the bundled addons tree as one number, led by account_move.AccountMove 7,006, mixin_mail_thread.MixinMailThread 4,770, mrp_production 4,647, stock_move 4,577, account_move_line 4,213. Driven --mode no-increase like pyfunclen_addons because the tree moves both ways constantly."
}
