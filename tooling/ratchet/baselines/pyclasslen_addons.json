{
  "count": 177767,
  "measured_at": "b3860da8b00ee8e990771ba31d51555a7d3f0c12",
  "measured_root": "",
  "note": "UP 7, ALL OF IT ONE COMMIT AND ONE CLASS. Attributed rather than assumed: measured 177760 in a clean detached worktree of the previous floor's own measured_at (4a4223bf339) and 177767 here, then diffed the two per-class readings. ApprovalRequestHelper (addons/approval/models/approval_request_helper.py) goes 1106 -> 1113 and EVERY OTHER CLASS IS BYTE-IDENTICAL across the pair, so the delta is that one class and nothing else.\n\na5079a4ca50 is the commit: the escalation notice had to become Markup, because self.env._() returns a plain str and message_post escaped the tags, so 1778 notices reached readers as their own source, and both escalation triggers had to be gated on escalated_to_manager because a stalled approver bypassed the age threshold and re-escalated every day. The seven lines ARE the fix; there is nothing cosmetic to reclaim.\n\nBanked in a commit of its own rather than folded into unrelated work. A no-increase floor moved under an unrelated subject line destroys the record of what moved it, which is the failure this same gate demonstrated once already: the addons/resource comment strip bought 900 lines of headroom and took it green while HrEmployee sat untouched underneath. This re-bank absorbs none of that -- HrEmployee is still 2709 lines and its overage is still unbanked, and the floor moves by exactly the seven lines measured above.\n\nRe-stamped after rebasing onto origin/19.0-marin: the first stamp named a commit the rebase rewrote away, so --list flagged ORPHANED-BASE and the check refused to compare rather than passing. Re-measured on this worktree at 177767, unchanged, because the commit rebased over (a4d0ec6d7d4) is four XML view files and moves no Python.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-measured after the 2026-09-04 origin syncs, which orphaned the stamp: the old floor named 7f821114c6c0, a commit two rebases have since rewritten away, so the gate exited 2 with \"re-measure\" and compared nothing rather than passing or failing. 178124 -> 177760 on a clean detached worktree of HEAD. The number is 364 BELOW the dead floor, so nothing is absorbed upward by this re-bank. What it does contain, said plainly rather than left for a green reading to imply: HrEmployee is 2709 lines and still carries the +100 that CLAUDE.md section 9 records as unbanked, and the addons/resource comment strip that made this gate read green by 800 is still what pays for it. This floor restores the ability to detect the NEXT regression; it does not settle that one.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-stamped at the branch commit for the reason pyclasslen gives. Count unchanged (178124).\n\nBorn at 178124: 305 classes over the 400-line budget in the bundled addons tree as one number, led by account_move.AccountMove 7,006, mixin_mail_thread.MixinMailThread 4,770, mrp_production 4,647, stock_move 4,577, account_move_line 4,213. Driven --mode no-increase like pyfunclen_addons because the tree moves both ways constantly."
}
