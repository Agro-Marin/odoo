{
  "count": 4,
  "note": "8 -> 4, and all four units are named. approval left this repository for odoo/addons carrying exactly four hand-rolled counters, every one of them a search_count inside a loop: approval.dashboard._compute_user_metrics counted my_pending and my_urgent per record, and approval.delegate.wizard._compute_preview counted pending and waiting per record. All four were hoisted in the move -- the dashboard compute reads nothing but the environment, so its queries left the loop entirely, and the wizard's two became one _read_group over (user_id, state) indexed in memory. THE DEBT WAS PAID, NOT RELOCATED: py_x2many_count_addons is unchanged at 24, which is what says the four did not simply reappear under the new scope. The remaining four are remote_dav_sync's, untouched. Measured against the real tree because this gate is cross-repo and cannot run from a worktree -- sibling_repos_root() finds no agromarin beside a detached checkout; the concurrent marin/ edits contribute zero findings to this gate, so the reading is clean despite the dirty tree."
}
