{
  "count": 0,
  "measured_at": "afc037e38cabbfe1dcdbf3cb6142db0c657d66d8",
  "measured_root": "",
  "note": "STAMPED, NOT MOVED. The floor carried no `measured_at` at all, so it was a number nobody could reproduce: the guard that refuses a stamp this branch never had cannot fire on a floor with no stamp, and an unstamped floor may never have been true of any tree. Re-measured on a clean detached worktree at the stamped commit -- odoo-bin -i test_lint --test-enable --test-tags /test_lint at CI scope -- and it reads 0, equal to the floor. The count does not move; only the stamp does.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n7 -> 0: odoo/orm was the last scope holding this rule, and the seven were not one problem.\n\nThree were real and are translated now: name_create's missing _rec_name (a dialog), load()'s external-id/model mismatch (raised at a user's import), and add_field's non-x_ manual field, whose SIBLING raise four lines below was already wrapped in _().\n\nFour are the rule's premise not holding, and carry a rationale instead:\n\n  * validation.py x3 (check_object_name, check_pg_name twice) read as user-facing\n    because they raise ValidationError, but BOTH callers a user can reach --\n    ir_model_fields._check_name and _check_relation_table -- catch them and\n    re-raise a translated message of their own, so this text never leaves a\n    traceback.\n  * runtime/environment.py is stronger than that: the message reports an invalid\n    language code, and translating it needs a working language, so env._()\n    re-enters the property mid-computation and the recursion ends in a\n    KeyError '_en_US' that names neither the cause nor the language. The comment\n    above it already said so; the waiver now says it too.\n\nMeasured -i test_lint at CI scope (--addons-path odoo/addons,addons) in a\ndetached worktree at c0a918326d3: 7 with nothing applied, 0 with only this\nchange's odoo/orm files copied in."
}
