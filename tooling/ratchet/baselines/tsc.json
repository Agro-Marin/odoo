{
  "count": 1978,
  "note": "1980 -> 1978, and both units are the ViewDescriptions typedef in view_service.js. It declared Record<string, ViewDescription>, which describes only the 'views' member of what loadViews returns; the real answer is {fields, relatedModels, views} and every caller reads it that way (view.js uses result.views[type] AND result.fields). @ts-check could not see the disagreement because the object is built through an any cast -- but view.test.js:1252 and :1254 build the real shape and hand it to something typed ViewDescriptions, so the wrong declaration was costing two errors in the test that was right. Measured on a clean worktree at 01309b6ee28: 1980 before, 1978 after."
}
