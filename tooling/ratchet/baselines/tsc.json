{
  "count": 1805,
  "note": "1893 -> 1805, all of it this session's strict-lock work, measured on a clean detached worktree at 36fd47d51dd (`git status --porcelain` empty, main checkout's node_modules so the pinned tsc matches CI). Attributed by diffing the full error set against b9f24b61cb2, where this floor last stood.\n\nSIXTY-FIVE OF THE EIGHTY-EIGHT ARE ONE LINE. `FieldComponent` is not generic, and three field widgets write `@extends {FieldComponent<TheirProps>}`. Against a non-generic base that annotation does not narrow anything -- it breaks the inheritance chain, so `this.props` and `this.field` stop resolving on the subclass:\n\n    datetime_field.js    43 -> 6\n    gauge_field.js       16 -> 0\n    statusbar_field.js   12 -> 0\n\nThe rest, by file:\n\n    emoji_picker.js                  7   MobilePickerHost took five collaborators through Object.assign(this, deps)\n    list_virtualization.js           4   a @typedef merged with the same-named class in the same file\n    state_selection_field.js         3   fieldDependencies typed Partial<StaticFieldInfo>, which has no `optional`\n    tree_editor_autocomplete.js      2   getTags' return, and a @ts-expect-error that was not stale after all\n    many2one_reference_field.js      2   props.domain, off a literal that spread away its index signature\n    ace_editor_hook.js               1   requestAnimationFrame's handle returned from onMounted\n    tree_editor_components.js        1   a field assigned in setup() and never declared\n    core/utils/hooks.js              1   element[property] where the union of .value and .checked is `never`\n    search_query_mixin.js            1   .find()'s result destructured unchecked\n    command_palette.js               1   ForwardRef declared `Function` while every consumer reads its `el`\n    (ten more in tests, knock-on from the above)\n\nTen units moved the other way in the same window and are not this session's: scrolling.js +1, ui.js +3, search_model.js +1, dropdown.test.js +3, assets.test.js +2, from other sessions' commits. The -88 is net of them."
}
