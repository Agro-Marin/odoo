{
  "count": 2166,
  "note": "2170 -> 2166 (-4). The floor was honest (the pre-rebase tree measures exactly 2170); this is a real improvement. Rebasing the error-beacon work in added 7 errors in module_loader.test.js, all TS2339 on odoo.loader._beacon, and the fix exposed that @types/odoo.d.ts had drifted from the shim it says it mirrors: neither the _beacon test seam nor odoo.isReady was declared, though both have always existed at runtime -- isReady is written by boot/start.js through an /** @type {any} */ cast precisely because the contract was missing. Declaring both retired remote's 7 and 3 pre-existing casts-around-the-gap in error_beacon.test.js; the 4th is that test's deliberately-malformed reportJsError({}), now cast explicitly so the invalid argument reads as the assertion it is. Command: npx tsc --project tsconfig.json --noEmit 2>&1 | grep -c 'error TS'"
}
