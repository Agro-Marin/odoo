{
  "count": 23,
  "measured_at": "9faddca461e1436c100065885a15176d86144e30",
  "measured_root": "",
  "note": "Opening floor for the gate added in 9faddca461e1. Measured at test_lint's narrow scope (--addons-path=odoo/addons,addons, -i test_lint): 326 tests, this the only failing subtest at 23. The population is untriaged on purpose: the rule reports the SILENCE, not either choice, so every site is a per-model decision between NULLS NOT DISTINCT where the key must hold over the empty rows and a partial WHERE where the exemption is meant. Banked so the gate is green and any NEW one is visible; driving it down is per-site work for whoever owns each model."
}
