{
  "count": 25139,
  "note": "26441 -> 25139, re-measured on the rebased tree with the CI command (py_function_length.py --addon addons --count).\n\n  origin/19.0-marin        26441   (its committed floor, 26441, reproduces)\n  merged                   25139\n\nNot measurable on this branch pre-rebase: --addon addons is origin's own flag, added by the commit that opened this gate, so the script on the pre-rebase tip does not accept it and returns nothing. That is why the move is quoted against origin alone and not as a three-way.\n\nThe -1302 is the same mail extraction pyfunclen_mail records, seen through the whole-addons scope: this gate counts excess lines over 80 across every addon, and addons/mail is where all of it moved. It is a no-increase gate on a tree that swings ~1700 a month, so the floor is set to the measurement rather than argued down unit by unit."
}
