{
  "count": 26441,
  "note": "28842 -> 26441, banking origin's addons sweep. This floor runs --mode no-increase, so the improvement was free rather than owed and the gate passed at 28842; it is taken because a floor sitting 2401 above the tree tolerates a regression of that size in silence, which is the one thing a ratchet exists to prevent.\n\n  this branch, pre-rebase  28727   (against a committed 28842)\n  merged                   26441\n\nNeither the merge-base nor origin can be measured for comparison: --addon addons is this branch's own addition to py_function_length.py, so the flag does not exist in either tree and both report nothing. The whole -2286 is therefore origin's work arriving under a gate origin never ran, which is exactly the seam this floor was added to cover -- the receiving tree is the one nobody onboarded. The largest single contributor is the mail sweep that took mail.message.create from 139 lines to 46."
}
