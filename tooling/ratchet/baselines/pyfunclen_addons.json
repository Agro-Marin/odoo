{
  "count": 20176,
  "note": "Re-measured on the linear replay: 23896 -> 20176. The floor was this branch's, taken against `addons/` before origin's 23 commits and their function splits were under it. `py_function_length.py --addon addons --count` reads 20176 on the replayed tree. It sits below the 20245 `0bead38ba05` recorded because this tree also drops `addons/mrp/models/product.py`, 730 lines the merge kept through a delete/modify conflict although the merged `mrp/models/__init__.py` imports `product_template` and `product_product` instead and never imports it."
}
