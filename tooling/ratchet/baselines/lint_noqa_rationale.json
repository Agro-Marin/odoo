{
  "count": 23,
  "measured_at": "35657b8f5c731f2fe09e61db9b49c398f0932a80",
  "measured_root": "",
  "note": "19 -> 23 at 35657b8f5c73, the 2026-09-11 sync rebase (odoo onto origin eb02e3d03179) rewriting the stamp 086aa2f55919. THE READING IS tooling/lint/py_lint.py over odoo/addons addons, NOT the graded narrow-scope test_lint run (not executed by this sync); py_lint on the pre-rebase tip 7af10ee9ce9a reads exactly the banked 19, so the runners agree on this floor. The four units are origin's, banked rather than fixed because a sync is not the place to judge them: calendar/models/booking/appointment_type.py:1691, calendar/models/booking/calendar_event.py:53, :54 (two DTZ901 on the datetime.min rounding) and :374. Origin's own tip reads 91 against its banked 19 by the same runner: the fold's units that 086aa2f55919 pays down on this branch, and these four.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n88 -> 19 at 086aa2f5591, the paydown the previous note asked for: every one of the 69 units the account_reports fold brought (5d35a5e93aa) is gone by the code fix its rule named -- 21 B023 lambdas bind their loop variable, 17 PERF401 loops are extend(generator), 10 B904 raises say from None / from e, 6 F841 assignments dropped, 3 PLR1704 loop variables renamed inside their loop, 4 B018 cache reads are fetch([...]), and SIM105, SIM113, E722, E721, B007, C400, RUF069 one each; the single noqa that remains in that set (B008 on an SQL() default) carries its reason and is not counted. 19 is exactly the floor's pre-fold number, measured in a clean detached worktree of 086aa2f5591. The 19 that remain are the pre-fold set outside addons/account (bus, iot_drivers, resource, ...), untouched here.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n19 -> 88, +69, every unit a # noqa without a rationale that arrived with the account_reports fold (5d35a5e93aa, 2026-09-09) and was never banked on this side; enterprise's floor for the same rule had already been lowered when account_reports was deleted (it reads exactly its floor of 82 today), so this bank completes that fold's accounting rather than accepting new debt. Attributed by diffing py_lint.py --rule noqa-rationale per file between the floor's own stamp c65b67c822e and 81b5842174e: account_return +10, account_report_expression_eval +8, account_report_engine +5, tests/test_report_engines +5, account_report_export +4, account_report_lines +4, account_reconcile_wizard_amounts +3, and 2 or 1 each across 24 more account files -- 69, no residue, nothing outside addons/account. Most are ruff B023 (loop variable in a lambda), B904 (raise inside except without from) and PERF401, so the paydown is the code fix that removes the noqa, not a sentence after it. Measured in a clean detached worktree of 81b5842174e.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRE-STAMP ONLY, re-measured and unmoved at 19 on the merged tree. The second rebase onto origin/19.0-marin rewrote the commit this floor was stamped at, leaving it ORPHANED-BASE or UNCHECKED -- which makes it unfalsifiable, the failure mode that let jsclasslen hide a 79-line regression earlier in this session. Re-stamped at a commit this branch holds.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRE-STAMP ONLY, re-measured and unmoved at 19 on the merged tree. Rebasing onto origin/19.0-marin rewrote the stamped commit. The count itself is RUF100 removing 15 suppressions that suppressed nothing during the ruff sweep of addons/.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRUF100 (unused-noqa) removed 15 suppressions that were suppressing nothing during the ruff sweep of addons/, and each one it removed was also a noqa this gate was asking for a rationale on. 34 -> 19. No rationale was written and none was deleted: the comments themselves are gone because ruff proved they had no effect.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REPAIRED AFTER THE SECOND SYNC OF 2026-09-07, NUMBER UNCHANGED. Five commits landed on origin/19.0-marin while this branch was being verified; replaying onto them rewrote every SHA again, so the stamps written minutes earlier were orphaned by the same mechanism, for the fourth and fifth time in two days. Re-measured on the replayed tip with the gate own runner and it reads what the floor already said, so origin five commits move this gate by nothing. Only the provenance moves.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSYNC ONTO origin/19.0-marin, 2026-09-07: 36 -> 34, BOTH UNITS ORIGIN'S. Measured with tooling/lint/py_lint.py odoo/odoo odoo/addons --rule noqa-rationale --count on clean detached worktrees at three points: the floor commit b67fa612706 reads 36, pure origin 9021c4d2aba reads 34, the rebased tip reads 34. Our commits move this gate by nothing, so origin carries the whole paydown; a sync is what banks the other side's numbers. The gate is exact-mode, so an unbanked paydown leaves the branch red in the direction nobody looks.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REFRESHED AFTER THE 2026-09-04 REBASE, NUMBER UNCHANGED. 19.0-marin was replayed onto origin, so every SHA changed and the stamps written hours earlier were orphaned again -- the same mechanism, and the flag working. Re-measured at b67fa612706 in a clean detached worktree, and it reads what the floor already said. The invocation is confirmed rather than assumed: lint_sql_injection is not orphaned, its floor is 9, and the same run reads 9. Scope trap worth recording, because I hit it: run from the REPO ROOT, where 'odoo/' means the core package and the lint roots are 'odoo addons'; run one level up and ruff reports 3,117 findings from the bundled addons against a hard-zero floor.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REFRESHED, NUMBER UNCHANGED. Re-measured with 'py_lint.py odoo/odoo odoo/addons --count' in a clean detached worktree of ed0d70a60d6. The scope is confirmed rather than assumed: lint_sql_injection is NOT orphaned, its floor is 9, and the same command reads 9, so this invocation reproduces the odoo-scope floors the test_lint lane sets. The old stamp named a commit the 2026-09-02 sync replayed away, so architecture.yml exited 2 asking to be re-measured rather than comparing.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-measured at 24f92b0f744 after a rebase orphaned the previous stamp b7b596cd004; count unchanged (36). Measured on a clean detached worktree of that commit through the test_lint lane's own reader: odoo-bin --addons-path=odoo/addons,addons -i test_lint --test-enable --test-tags /test_lint on a fresh database, where test_python_lint's noqa-rationale subtest held at the floor.\n\nRe-measured after rebasing this branch onto origin/19.0-marin (214 upstream commits). Neither side's floor described the merged tree: both had driven counts down independently from a common base, so every one of these had to be re-derived rather than merged. Measured at HEAD in the workspace checkout; mypy per section 9.3 in a detached worktree with mypy==2.3.0 alone."
}
