{
  "count": 36,
  "measured_at": "3cfc16542e70bf004fc1f1973b8aa878594de3cd",
  "note": "A floor re-derived without saying which units were real is the same defect in fresher paint, so: the FIGURE was already right and only its provenance was wrong. 36 at the old floor, 36 measured at d700c33e3d7 in a detached worktree, agreeing across two independent readings (test_lint at CI scope and tooling/lint/py_lint.py). Re-stamped rather than moved, so the number now has a commit anyone can check out behind it instead of b7b596cd004b, which git cannot resolve.\n\n--- RE-MEASURED 2026-09-01 ---\nThis branch was squashed from 42 commits and replayed onto origin/19.0-marin (99 upstream commits), so every SHA the note above cites was rewritten and resolves to nothing. The figure below is re-derived in a clean detached worktree at 3cfc16542e7, with a second worktree at origin/19.0-marin as the control, so the branch's own contribution is attributed rather than assumed.\norigin/19.0-marin 36, this tip 36. Unchanged by this branch; the figure was already right and only its provenance was wrong. Measured with tooling/lint/py_lint.py at the CI scope odoo/CLAUDE.md fixes, `odoo/addons addons`."
}
