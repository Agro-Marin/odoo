{
  "count": 10,
  "note": "seeded at the community-scope residue: 10 demo-data refs whose xmlids are minted by Python helpers that compute the name at runtime (_get_suffixed_ref_name), so no literal a static scan can read declares them. None is install-breaking. The three genuine finds this gate made on its first run were fixed in the same change: l10n_ro_cpv_code -> product.view_product_template_form, and the unfounded account.return.type in l10n_tw_reports."
}
