{
  "count": 0,
  "measured_at": "c65b67c822e7fcc3cbf5140cd7c92a5c8cf8b0d1",
  "measured_root": "",
  "note": "RE-STAMP ONLY, and origin s number, not this branch s. 0 is a contract: npx prettier --list-different \"**/*.d.ts\" reports nothing on the merged tree. The gate arrived in origin/19.0-marin and its stamp did not survive the rebase this branch was replayed through, so it read UNCHECKED -- unfalsifiable. Count untouched; provenance restored so it can fail again.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nHard zero over repo-wide .d.ts format drift, the sibling of prettier_scss for declaration files.\n\nnpx prettier --list-different \"**/*.d.ts\" | grep -c . , measured in a clean detached worktree with node_modules symlinked in, since a worktree carries none.\n\n36 -> 0. Every .d.ts prettier would rewrite, rewritten: 36 of 79 files across 12 addons, led by web 12 and spreadsheet 5.\n\nTHE FLOOR IS NEW BECAUSE THE GATE IS NEW, AND THE GATE IS NEW BECAUSE NOTHING WATCHED THESE FILES. Every files: glob in eslint.config.mjs is *.js, so a .d.ts matched no config block and eslint reported 'File ignored because no matching configuration was supplied' -- a WARNING that reads like a finding and means eslint formed no opinion. It cannot: espree does not parse TypeScript and no TS parser is a dependency. Prettier can parse them, but runs here only as an eslint rule, so eslint declining the file took prettier with it, and the standalone prettier gate covered **/*.scss alone.\n\nVerified by tsc rather than by a byte comparison: 1484 = floor, error set identical to the parent's ignoring line numbers. esbuild is NOT a sound comparator here -- it erases types to zero bytes, so a before/after minify of a .d.ts compares two empty strings and reports identical."
}
