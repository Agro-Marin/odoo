{
  "count": 76,
  "note": "78 -> 76, composed while rebasing this branch onto origin/19.0-marin. Two independent moves were measured on separate branches and the rebased tree carries both: origin moved 78 -> 75, this branch moved 78 -> 79. Neither branch's figure describes the merged tree, so the floor is the composition and is re-measured against the rebased tree before it is trusted.\n\nORIGIN'S MOVE (78 -> 75)\n\nmail: mail.message.create 25 -> under the threshold. Its base64-to-attachment substitution, its attachment access check and its tracking-value commands are now _extract_body_attachments, _check_created_attachments_access and _create_tracking_values, and the per-record reply-to resolution is _get_reply_to_batch. The method is 139 lines -> 46 with no behaviour change: verified by /mail,/test_mail at 62 failed of 1069 against a clean worktree at the same commit, character-identical failure sets.\n\nTHIS BRANCH'S MOVE (78 -> 79)\n\napi_gateway dissolved into api_transport, 78 -> 79. The unit is oauth.py::callback at 22, which did not change: it moved address, out of agromarin and into this repo's addons/, which is the scope this gate measures. It is not refactored here because the extraction that would fix it -- lifting the twelve state checks (signature, nonce, db, uid, timestamp skew) into a helper -- is a rewrite of OAuth state validation, and the two tests that came with the controller both cover the token exchange, not one of those branches. Refactoring untested security validation to satisfy a complexity budget is the wrong order; characterise the branches first, then split, then lower this back to 78.\n"
}
