{
  "count": 54,
  "note": "hr.employee.write dropped below the complexity ceiling. 55 -> 54, and the whole of the -1 is that one function.\n\n`ruff check addons/ --no-cache --select C901 --output-format=concise`, the finding\nsets diffed line by line between a detached worktree at HEAD and the same worktree\nwith the round-3 hr audit copied in:\n\n  removed  addons/hr/models/hr_employee.py:2273  write  25 > 20\n  added    none\n\nTwo blocks came out of it, each of which was a job of its own rather than a branch\nof the write: `_repoint_bank_accounts` (move the accounts onto the new work\ncontact, dropping trust on the way, because an account that changes owner must be\nre-approved) and `_propagate_calendar_to_resources` (push a written schedule onto\nresource.resource, but only for the version that is current, since the resource\nhas no notion of versions). Nothing else in the method changed.\n\nThe same pass took every function in addons/hr inside the 80-line budget --\n`py_function_length.py --addon hr` reports no offender, 119 excess lines to 0 --\nwhich is why pyfunclen_addons falls 20866 -> 20747 in the same change.\n\nMeasured per 9.3 in a detached worktree, the shared working tree being unusable\nfor a count (other sessions are holding addons/website, addons/base_order and\nodoo/orm).\n\nDetail: agromarin-knowledge/research/2026-08-26-hr-module-audit-round3.md"
}
