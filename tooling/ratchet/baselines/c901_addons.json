{
  "count": 92,
  "note": "Down one from the initial 93: account.move._post_validate was split into its invoice and move halves and left the list, taking account's own share from 11 to 10. Scope unchanged -- 'ruff check addons/ --no-cache --select C901 --statistics', threshold [lint.mccabe] max-complexity = 20, ruff 0.16.2. Worth knowing for next time: this drop stayed invisible for a while because another module regressed by one in the same window and the two cancelled out. An exact-mode floor compares a single number for the whole tree, so a green 'no drift' does not mean nothing moved; a per-module breakdown in the ruff.yml summary would close that gap. Remaining top offenders: web/models/web_read.py::web_read 67, l10n_in _get_l10n_in_gstr_section 57, account/chart_template.py::_pre_reload_data 56 -- that file alone holds four of account's ten, and its complexity is genuine shared state across four model branches rather than accidental nesting, so decomposing it needs a reload-context type."
}
