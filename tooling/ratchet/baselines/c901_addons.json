{
  "count": 62,
  "note": "63 -> 62: account.move._prepare_invoice_aggregated_taxes measured 22 against a ceiling of 20 and is now three named methods -- base-line preparation, the totals pass, the tax-details pass -- plus a helper for the grouping-key fold that both passes had spelled out. Re-measured at 45d5e297081 in a detached worktree, so the reading carries none of the working tree's concurrent edits.\n\n68 -> 67, and the whole of the -1 is the offender-set diff, not arithmetic.\n\n`ruff check addons/ --no-cache --select C901 --output-format=concise` in two worktrees pinned to the same commit, this change and its parent:\n\n  parent (08271816cdf)   68\n  this commit            67\n\ncomm returns one removal and no additions:\n\n  stock/models/stock_picking_type.py::write  23 -> gone\n\nIt was 128 lines doing five jobs -- reject a company change, remember which types are moving warehouse, rename sequences, maintain stock.move.date_reservation, re-seed default locations -- and the fourth longest function in the addon. It is now _check_company_change, _update_move_reservation_dates, _update_default_locations_for_warehouse and _update_reference_sequences, each under the ceiling on its own. Three of the defects that commit fixes were only visible once the steps had names: the sequence rename read the pre-write sequence_id, it read the pre-write warehouse_id, and neither ran at all unless sequence_code was in vals."
}
