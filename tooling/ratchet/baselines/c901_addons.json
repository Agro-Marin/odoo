{
  "count": 41,
  "measured_at": "b67fa6127061dbc04247d1d4d07019c19b2b7ed0",
  "measured_root": "",
  "note": "STAMP REFRESHED AFTER THE 2026-09-04 REBASE, NUMBER UNCHANGED. 19.0-marin was replayed onto origin, so every SHA changed and the stamps written hours earlier were orphaned again -- the same mechanism, and the flag working. Re-measured at b67fa612706 in a clean detached worktree, and it reads what the floor already said. The invocation is confirmed rather than assumed: lint_sql_injection is not orphaned, its floor is 9, and the same run reads 9. Scope trap worth recording, because I hit it: run from the REPO ROOT, where 'odoo/' means the core package and the lint roots are 'odoo addons'; run one level up and ruff reports 3,117 findings from the bundled addons against a hard-zero floor.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REFRESHED, NUMBER UNCHANGED. 'ruff check addons/ --select C901' reads 41 in a clean detached worktree of ed0d70a60d6, complexity over the configured max-complexity of 20. Re-stamped because the old stamp named a commit the 2026-09-02 sync replayed away.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-measured at 24f92b0f744 after a rebase orphaned the previous stamp a9364a9b989; count unchanged (41). Measured on a clean detached worktree of that commit with ruff 0.16.2 (the pinned version): ruff check addons/ --no-cache --select C901 --statistics, summed, as ruff.yml runs it.\n\nhr_work_entry: both units left with b1c7684780b, which reworked `_get_version_work_entries_values` (25 > 20) and `_generate_work_entries_postprocess` (21 > 20) while fixing the five state defects. Neither was addressed as complexity; the complexity left with the rework, and the floor was not moved with it.\n\nNo new offender anywhere in addons/: the full 43 and the full 41 were diffed by file+function, so `hr_holidays._process_accrual_plans` going 39 -> 40 while staying over the threshold is not mistaken for a move.\n\n  43 -> 41\n\nCommand: ruff check addons --no-cache --select C901 --statistics"
}
