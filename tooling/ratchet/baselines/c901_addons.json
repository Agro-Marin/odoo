{
  "count": 75,
  "note": "78 -> 75, on the tree left by rebasing this branch onto origin/19.0-marin. Measured with the CI command (ruff check addons/ --no-cache --select C901 --statistics, summed) on the rebased tree: 75, and ruff's own summary line agrees (\"Found 75 errors.\").\n\nAGAINST THAT BASE THIS BRANCH'S NET MOVE IS ZERO, and the two units that make it up cancel:\n\n  addons/api_transport/tools/api_client.py::request    25 -> gone\n  addons/api_transport/controllers/oauth.py::callback  22 -> new\n\nAttribution is an offender-set diff, not arithmetic: the same command run in a worktree at origin/19.0-marin also lists 75, and comm against this tree's list returns exactly the pair above and nothing else. api_client.request left the count when api_gateway was absorbed into api_transport and the vendor knowledge came out of the outbound client -- the note that set 78 named it the single largest offender and the obvious next target, and that absorption did it. oauth.callback did not change: it moved address, out of agromarin and into this repo's addons/, which is the scope this gate measures. It is not refactored here because the extraction that would fix it lifts twelve state checks (signature, nonce, db, uid, timestamp skew) out of OAuth state validation, and the two tests covering that controller both exercise the token exchange rather than any of those branches. Characterise the branches first, then split, then lower this to 74.\n\nThe floor briefly read 76 in this branch's history: composing origin's move with this branch's predicted one unit that measurement then refuted. The measured set is the record."
}
