{
  "count": 87,
  "note": "project cleared its whole share, 3 -> 0, by splitting the three offenders along the phases they already had in comments. project_task.write (44, the module worst) interleaved milestone propagation, step stamping, recurrence, assignee dating, privacy-scoped sudo writes, dependency sync, rating mail and two notification paths; each is now a named _write_* phase and the ORDER of the phases -- the part that actually matters, and which the comments alone used to carry -- reads in one screen. project_task.create (35) gave up its per-vals body as _create_prepare_vals and its follower/token/CC loop as _create_subscribe_followers. project_project.action_compute_critical_path (38) became the five steps it always was: _cpm_collect_edges, _cpm_check_acyclic, _cpm_topological_order, _cpm_forward_pass, _cpm_backward_pass, _cpm_write_results. No behaviour change: 0 failed, 0 errors of 381 project tests after each split, and the CPM helpers keep the iterative three-colour DFS and Kahn order that the 1200-task chain test pins. Attribution measured as the guide asks: 90 on a pristine HEAD worktree, 87 with only this change applied to it, so the -3 is this commit and not a neighbour lowering the same gate."
}
