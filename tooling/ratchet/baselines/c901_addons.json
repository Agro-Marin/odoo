{
  "count": 78,
  "note": "Gate was red at HEAD for everyone: three changes moved the count since a8000219292 set 79 and none re-floored. Two functions cleared the 20-unit budget: stock_move_line._action_done at 5c4aeaf760f (reserve where the line reserves) and stock_warehouse.write at b941d472548 (hoist the two rename searches out of their loop), -2. One arrived above it: api_transport tools/api_client.py::request at 25, which did not exist when the floor was set -- 0a5a7872888 promoted the module into the fork carrying it, +1. Net -1. Measured with the CI command (ruff check addons/ --no-cache --select C901 --statistics, summed) on a clean worktree at HEAD: 78, and identically in the working tree. Attribution is per-function, bisected by running C901 over each file version at the candidate commits. api_client.request (25) is now the single largest offender and the obvious next target; it is not touched here because reshaping a transport retry path is not a drive-by."
}
