{
  "count": 80,
  "note": "82 -> 80 (-2). addons/calendar's two findings cleared: calendar.event.create (27) and calendar.event.write (24) are now named steps below the threshold, taking calendar's share from 2 to 0.\n\ncreate() split into _create_apply_defaults / _create_prepare_activities (+ _prepare_meeting_activity_vals) / _create_prepare_attendees / _create_prepare_contact_description / _create_split_by_recurrency / _create_sync_activities / _create_setup_alarms; write() into _write_sync_videocall_members / _write_reschedule_alarms / _write_reset_organizer_answer / _write_notify_attendees. Each was already a paragraph-with-a-comment inside one body, and none of them was reachable from a test on its own -- the decomposition is what makes the delete-policy and time-predicate fixes in the same change testable.\n\nAttribution: measured on a pristine HEAD worktree with only addons/calendar and addons/microsoft_calendar replaced, 82 -> 80, the per-module finding lists differing by exactly those two lines. The shared checkout carries other sessions' in-progress mrp/project work and measures differently; it was not used."
}
