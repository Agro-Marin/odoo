{
  "count": 79,
  "note": "point_of_sale: pos_session._accumulate_amounts cleared the 20-unit budget, 26 -> under it, by naming two of the three jobs it held in one body -- _accumulate_order_payments (sorting each payment into a receivable bucket: split per customer or combined per method, cash / bank / pay-later, and whether the order was invoiced) and _accumulate_stock_amounts (valuing the deliveries the closing entry is answerable for). No behaviour change: every accumulator keeps its name, so the moved bodies are the same text at a shallower indent, and the buckets are still the caller dicts mutated in place. Verified by the ten accounting suites that exercise the closing entry directly, 25 of 25 green. stock_picking._add_mls_related_to_order (21) is now point_of_sale's only remaining offender."
}
