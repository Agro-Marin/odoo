{
  "count": 1049,
  "note": "1042 -> 1049, measured the way py_typecheck.yml prescribes -- mypy 2.3.0 and NOTHING ELSE in the venv, --no-incremental, the six packages -- in two worktrees:\n\n  origin/19.0-marin   1043   (its committed floor says 1042)\n  merged              1049\n\nONE OF THE SEVEN IS ORIGIN'S, NOT THIS BRANCH'S. origin measures 1043 against the 1042 it committed, so the gate was red by one before the rebase. It is carried rather than subtracted: the floor is what the tree measures, and quoting 1048 would bank a unit nobody has fixed.\n\nThe other six are this branch's, and a per-file diff of the two error sets accounts for every one:\n\n  +3  odoo/orm/tests/test_savepoint_cache_reclear.py   0 -> 3   (new file)\n  +1  odoo/orm/runtime/savepoint.py                    6 -> 7\n  +1  odoo/orm/models/mixins/read.py                   4 -> 5\n  +1  odoo/orm/models/mixins/unlink.py                 2 -> 3\n\nNo other file in the six packages moves in either direction. Three of the six are the savepoint-rollback fix's own test module, and the unlink one is the _log_unlinked_ids extraction passing self.ids (a tuple) where the annotation says list[int] -- the annotation is what is wrong there, and it is left for the change that revisits it rather than widened here.\n\nMeasured against 1.19.1-era numbers it will not reproduce: the venv resolves lxml/psycopg/dateutil to real stubs where CI resolves them to Any. Re-measure, never subtract."
}
