{
  "count": 1600,
  "note": "1602 -> 1600, both units from _RegistryStubs.has_unaccent being declared bool when modules.db.has_unaccent returns the tri-state FunctionStatus (MISSING/PRESENT/INDEXABLE). (1) _registry_schema.py:144 'if self.has_unaccent == FunctionStatus.INDEXABLE' was reported [comparison-overlap] -- statically unsatisfiable under the declared type, since a bool is 0 or 1 and INDEXABLE is 2. It is not dead: measured on a registry built from tpl_p314o19marin, which ships unaccent(text) as provolatile='i', has_unaccent IS INDEXABLE, so that branch is the one that runs and it is the only path applying unaccent() inside a trigram index expression. (2) registry.py's 'self.has_unaccent = modules_db.has_unaccent(cr)' was [assignment] against the same wrong type. Fixing the declaration exposed a third error -- test_registry_check_indexes.py assigned False -- fixed in the same commit with FunctionStatus.MISSING, so it never reaches a count. NOTE for whoever moves this next: the figure is CACHE-DEPENDENT. A warm .mypy_cache carried across the edit reported 1599 for this same tree while a cold run reports 1600, the delta being two dateutil [import-untyped] errors in orm/constants.py that incremental mode suppresses. CI is always cold (fresh checkout); measure with 'rm -rf .mypy_cache' first or you will commit a floor CI cannot reproduce."
}
