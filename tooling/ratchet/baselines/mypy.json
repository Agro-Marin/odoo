{
  "count": 1147,
  "note": "1205 -> 1147, all of it odoo/libs: 12 more files cleared to zero (libs 36 -> 24 exceptions, 76% -> 84% locked). Four recurring shapes, not 58 unrelated fixes: a bool-typed sentinel that only ever holds False (email/parsing, filesystem/mimetypes' three sub-checkers), one name bound to two types (html.py's _style_whitelist, mimetypes' extension walrus), an unstated payload/definition shape (set_expression's dict[str, object] -> TypedDict), and deliberate supertype violations that had never been marked as such (frozendict). No behaviour change intended anywhere; 724 odoo/libs tests pass."
}
