{
  "count": 1267,
  "note": "odoo/db driven to zero: 19 errors cleared across 11 files. Three were real -- connection_info_for returned None from a str-declared function for postgresql:/// (no path, user or host), and two fetchone() unpacks were subscripted unguarded. The rest were annotation debt and two incomplete test doubles (BaseCursor.execute lacks prepare= on both fakes; _FakePool had no getconn at all). PG_RECOVERABLE_EXCEPTIONS/PG_USER_FAULT_EXCEPTIONS narrowed from type[BaseException] to type[Exception], which is what every member is and what _log_sql_error takes."
}
