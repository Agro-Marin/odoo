{
  "count": 1042,
  "note": "1072 -> 1042, on the tree left by rebasing this branch onto origin/19.0-marin, and measured there.\n\nTwo disjoint moves against the same 1072 base: origin 1072 -> 1071 (batching moved out of _write_multi into _update_rows_sql, taking an arg-type error at write.py:240 with it), and this branch 1072 -> 1043 (four root causes, none of them notation: _RegistryStubs' empty __slots__, and the three recorded with them). The composition 1071 - 29 = 1042 is exact, and the measurement agrees to the unit.\n\nMeasured the section 9.3 way and no other: python3 -m venv, mypy 2.3.0 and NOTHING else installed, --no-incremental, --config-file mypy.ini over the six packages, in a git worktree detached at this commit so no other session's uncommitted work is in scope. \"Found 1042 errors in 138 files (checked 480 source files)\". A run inside p314o19m would be wrong twice over -- it resolves lxml/psycopg/dateutil to real stubs where CI resolves them to Any, and its mypy is 1.19.1 against the pinned 2.3.0."
}
