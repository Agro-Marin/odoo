{
  "count": 1379,
  "note": "1389 -> 1379. All ten units are the same finding, in ORM many2many: 'SQL.identifier(str | None)' where relation/column1/column2 are declared 'str | None' because a Many2many exists before setup runs. Six of the ten went away on their own when F1 moved the m2m SQL behind the env.backend port into _query.py, whose parameters are declared 'str' -- and that drop was NOT an improvement: env.backend reaches Layer 1 through _ModelStubs.env, declared 'typing.Any', so the port calls are unchecked and the None-ness went exactly nowhere. Verified with reveal_type: 'records.env.backend' is Any while 'self.relation' is still 'str | None'. Six fewer findings, identical risk -- the shape this file's previous note calls the honest-vs-lower tension, here arriving as erasure rather than a false declaration. Many2many._relation_columns() is the answer: it states the real invariant once (setup_nonrelated sets all three under 'store' and clears all three otherwise, and row I/O only runs on a stored field), asserts it, and returns tuple[str, str, str]. That makes the six narrowings real at runtime and legitimately removes four more at sites mypy could still see (read's JOIN branch and _condition_to_sql). many2many's SQL.identifier count: 13 -> 3. MEASURE COLD: rm -rf .mypy_cache first; a warm cache reads two lower."
}
