{
  "count": 0,
  "note": "759 -> 0. The gate's SCOPE changed: it now measures 'ruff check odoo/ --no-cache --ignore D', with pydocstyle moved to its own ruff_docstring floor (758). This is not a paydown of 759 findings; 758 of them were D1xx and moved, and the remaining 1 was a genuine defect removed here (F401 'threading' unused in odoo/service/model.py, which had been invisible inside the aggregate). Split for the reason c901 was already kept separate: one exact-match integer cannot distinguish an added docstring from a new lint defect, so 758 fungible units could pay for real regressions. Non-D now sits at a HARD ZERO, so any new finding fails on its own. Command: ruff check odoo/ --no-cache --ignore D --statistics | awk '{s+=$1} END {print s+0}'"
}
