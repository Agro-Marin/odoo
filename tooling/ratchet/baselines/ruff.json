{
  "count": 762,
  "note": "1278 -> 762 (-516). The floor is now EXACTLY the pydocstyle debt left by the comment/docstring strip of odoo/, and nothing else: D102 224, D105 185, D103 175, D100 64, D101 60, D107 35, D104 19. Every other rule in ruff.toml reports zero over odoo/ -- `ruff check odoo/ --ignore D` is clean. The 516 non-D findings were paid down rather than parked; the ones worth naming because they were BUGS, not style: ir_ui_view._validate_view removed children while iterating the lxml node, so any nested view tag following another was silently never validated (B909); odoo/tools/pdf returned `[]` from inside a generator, which is a StopIteration value and not an empty yield (B901); `_apply_onchange_` in tests/form.py carried a stray trailing underscore; a dead `import collections`, `import threading` x2, `import pytest` and three unused `datetime.timezone` imports; and eight `_logger.info(\"\\n\" + \"=\"*80)` concatenations that defeated lazy logging formatting (G003). Two rules were RETIRED with the rationale recorded in ruff.toml (PLC2801 and PLW3201 -- 31 findings between them, every one a deliberate fork idiom), and six per-file-ignores were added for shapes the rules cannot see: the odoo/tools + odoo/service re-export facades (F401), the test-addon `_inherit` same-name class idiom (F811), the ORM 'touch a field to trigger the fetch' test idiom (B018/B015), 3.14 as fixture data (FURB152), odoo/init.py's ordered bootstrap (E402) and compare.py's CLI prints (T201). Everything else that stayed is a `# noqa` carrying a reason, per test_noqa_rationale. Docstrings were deliberately RETAINED in orm/__init__.py, orm/models/mixins/_metadata.py, service/__init__.py, service/db.py and http/tests/test_openapi.py, whose docstrings are machine-checked contracts in tooling/architecture/test_architecture_doc.py and tests/service/; do not strip those. Measured with pinned ruff 0.16.0: ruff check odoo/ --no-cache --statistics | awk '{s+=$1} END {print s+0}'. `ruff check tooling/ tests/` is at zero as the workflow requires (it was red by 4 before this)."
}
