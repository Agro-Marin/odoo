{
  "count": 759,
  "note": "761 -> 759. Two D violations in odoo/libs/worker_thread.py -- D100 on the module and D103 on current_worker_thread -- both pre-existing, both fixed while completing the WorkerThread Protocol. D and ANN are enforced only in odoo/libs/ and odoo/orm/components/ (the negated glob in ruff.toml's per-file-ignores), which is why a file this small carried two. Nothing else in this change moved the count: the _field_access test-mixin work and the worker-thread conversions are type declarations, and the one F401 they created (threading left unused in orm/tests/test_replica_breaker.py after switching to current_worker_thread) was removed in the same commit rather than absorbed. Command: ruff check odoo/ --no-cache --statistics | awk '{s+=$1} END {print s+0}'"
}
