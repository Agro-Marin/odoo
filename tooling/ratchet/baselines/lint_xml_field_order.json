{
  "count": 659,
  "measured_at": "24f92b0f744fe7664a897896eb4ab25bf1f647ae",
  "note": "Re-measured at 24f92b0f744 after a rebase orphaned the previous stamp b7b596cd004; count unchanged (659). Measured on a clean detached worktree of that commit through the test_lint lane's own reader: odoo-bin --addons-path=odoo/addons,addons -i test_lint --test-enable --test-tags /test_lint on a fresh database, where test_xml_records.test_xml_record_field_order held at the floor; reproduced standalone with the same _field_order checker over the same core_data_files selection at both commits, 659 to the unit on each. Unchanged is not clean: per the workspace CLAUDE.md section 4, the checker still skips every <record> that holds an XML comment (test_xml_records.py lines 70-71), so 16 genuinely out-of-order records in 14 files remain hidden and the reading with that guard deleted is 675. A delta reader should expect this floor to move 659 -> 675 the day the guard goes, with no ordering change anywhere. The checker is not touched here.\n\nRe-measured after rebasing this branch onto origin/19.0-marin (214 upstream commits). Neither side's floor described the merged tree: both had driven counts down independently from a common base, so every one of these had to be re-derived rather than merged. Measured at HEAD in the workspace checkout; mypy per section 9.3 in a detached worktree with mypy==2.3.0 alone."
}
