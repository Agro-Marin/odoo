{
  "count": 24156,
  "measured_at": "5003d4ac8ae026cb51d0b7ea8e4e08914006466a",
  "measured_root": "",
  "note": "provenance only: the 2026-09-11 sync rebase (odoo onto origin eb02e3d03179) rewrote the stamp; 3e92ec35100 replays as 5003d4ac8ae026cb51d0b7ea8e4e08914006466a and the tree reads 24156 there, 462 over the previous floor of 23694 against the +456 the note below attributes to this commit.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\npyclasslen 23422 -> 23878, +456, all lines 3e92ec35100 put inside fourteen core classes over budget for the debug-logger campaign (see 03ed253c1c4 for the base half): configmanager +8, PreforkServer +19, Field +8, ReadGroupMixin +32, LoadMixin +32, PostgresBackend +72, Cursor +43, CreateMixin +28, Registry +72, xml_import +34, ConnectionPool +41, Environment +24, _ModuleLoader +30, and ReadMixin newly over at 413 (+13 over). Measured in a clean detached worktree of 16a7b696fd2 against one of 03ed253c1c4; py_class_length.py --top 0 in both, diffed. Comes back down when tooling/debuglog/debuglog.py --strip runs.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n22525 -> 23422, +897, and every unit is a debug-logger line in a base class already over the 400-line budget, from 9c10c2e4e07 alone. Measured in a clean detached worktree of 9c10c2e4e07; its parent 06dad2df462 reads 22525 = the floor in the same worktree, so nothing else is in the delta. Per class, py_class_length --top 0 diffed between the two (line numbers stripped), thirty classes, summing to 897 with no residue: IrModelFields +68, IrUiView +61, IrModuleModule +55, IrAttachment +50, IrJob +49, IrActionsServer +41, IrModelData +41, IrCron +39, IrSequence +38, IrMail_Server +37, IrActionsReport +34, ResUsers +32, AssetsBundle +32, IrQweb(ir_qweb) +31, MixinMerge +31, ResPartner +31, IrModelFieldsSelection +28, IrModel +25, IrActionsActions +24, IrFieldsConverter +21, BasePartnerMergeAutomaticWizard +21, ResCompany +20, Base(ir_ui_view_base) +18, IrQweb(ir_qweb_assets) +13, IrUiMenu +13, ResCurrency +11, ResLang +11, IrQweb(esbuild) +9, IrAsset +7, ResGroups +6. THIS IS DELIBERATE, TEMPORARY MASS, NOT DEBT TO PAY DOWN BY SPLITTING: the sites are the quality campaign's instrument (agromarin-knowledge/reference/dev/debug-loggers.md) and are stripped mechanically by tooling/debuglog/debuglog.py when the campaign ends, at which point this floor comes back down by the same 897 in a commit that names them. Do not relieve it by moving a logger into a helper -- the strip grammar wants the sites where they are. A sibling session's core-package loggers move this floor next, banked separately with their own delta.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nprovenance only: the fourth rebase of 2026-09-11 (origin's wizards/ and reports/ rename, odoo e0ee15233fbb, agromarin 3cdb34663) rewrote the stamp; survey.py reads every floor at its number.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nprovenance only: the third rebase of 2026-09-11 (odoo onto d38ec9582cee) rewrote the stamp; re-measured unchanged.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n22547 -> 22525 after 54a8d5d5f180: _ModuleLoader 492 -> 424 (two bodies to module functions) and IrUiView 2946 -> 2924 (the view-mode scrub to ir.actions.act_window). The merged tree had read 22625, +78, every unit predating the sync; IrQweb (esbuild) at 403 is the one offender that arrived and stayed, inside the new total.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nUP FORTY-TWO, BANKED RATHER THAN SPLIT, ALL IN TWO base CLASSES. +23 ResPartner (1759 -> 1782), d9e1e3f3398a: industry_id becomes the industry_ids many2many, plus a stored primary_industry_id and its compute. The stamp e29122ccf6a3 reads 22505, but 0e9e3d015275, which banked it, reads 22528, because the previous sync put d9e1e3f3398a under that commit after the measurement. +19 from cb2ad89679d4: ResPartner +14 (three _display_name_* attributes, six context keys as a ruff-wrapped tuple, and a two-line comment on the complete_name normalisation) and ResCompany +5 (three attributes and a two-line comment). These are declarative lines in classes already 1,300+ lines over budget, and splitting res.partner over them is a refactor for its own branch, not a floor fix. Attributed by diffing py_class_length --addon core --top 0 across e29122ccf6a3, 0e9e3d015275, 3e297bb9ff4b and 60c45785955e; no other class moved. Measured in a clean worktree of cad752ac96c3.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nIrUiMenu +11, and that is the whole move: 22494 -> 22505 across 33410db803ce, the module-category vocabulary sweep. Nine of the eleven are the docstring on _get_app_categories saying why ir.module.category.sequence has to travel to the client -- menus are ordered by their own sequence, so grouping apps by category alone puts the headings in the order the first app of each happens to appear. The other two are a dict key and its assignment. Attributed by diffing py_class_length --addon core --top 0 between a clean worktree of 33410db803ce^ and one of HEAD: IrUiMenu is the only class that moved, and the 18 e29122ccf6a3 banked immediately before this (ResPartner 1741 -> 1759, from 8e289d76ae2b) is a separate movement that was already in the tree when that floor was measured -- it read 22494 on a worktree that predated 33410db803ce, so this bank is the eleven it could not see, not a re-bank of its eighteen. Not shortened to keep the floor still: the trade §9 works through is exactly this one, and deleting the reason a field crosses the wire to save nine whole-tree lines is the wrong side of it. Measured on a clean detached worktree of e29122ccf6a3.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n22476 -> 22494, and the whole 18 is ONE class in ONE commit that is neither of the two sessions that found it red.\n\n8e289d76ae2b ([REF] base, geocoding: the coordinate invariant belongs to the two fields) moved the coordinate invariant out of addons/geocoding/models/res_partner.py into odoo/addons/base/models/res_partner.py. base's ResPartner went 1741 -> 1759. Diffing the --top 0 listings between this floor's previous stamp aa682b14638d and HEAD gives exactly that one changed line and nothing else in core moved at all; it is also the only commit touching that file in the range.\n\nIT IS NOT A TRANSFER BETWEEN SCOPES, AND THAT IS THE PART WORTH KEEPING. The obvious reading is that 27 lines left the addons tree and 18 arrived in core, so pyclasslen_addons should have fallen by roughly as much. It did not move at all. geocoding's ResPartner was 89 lines before the commit and 62 after -- under MAX_LINES = 400 both times, so it contributed zero excess on either side. The 18 landed in a class already 1341 lines over the threshold, where every line is excess. So moving code from a small class into a large one manufactures budget out of nothing, and a threshold gate cannot see the half that shrank. Expect this shape from any consolidation that lifts an addon's extension into base.\n\nBanked in a commit of its own rather than inside either session's work, so the note is not lost under an unrelated subject line -- the failure §9 works through. odoo-55's separate +11 on base's IrUiMenu, 9 of them a docstring they are right not to trim, is theirs to bank on top of this.\n\nMeasured 22494 in a clean detached worktree of the commit stamped here.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nTHE REPLAY MOVED IT, AND IT PREDATES BOTH FOLDS. +10 over 22466 after the 2026-09-09 sync rebased 19.0-marin onto origin. A worktree immediately before this branch's first local commit already measures 22476, so the ten lines arrived with upstream's 54 replayed commits and neither the IoT fold nor the account_accountant fold touched core class mass at all. Independently reached by a second session from the other direction. Measured on a clean detached worktree of aa682b14638d.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRE-STAMP ONLY, re-measured and unmoved at 22466 on the merged tree. The second rebase onto origin/19.0-marin rewrote the commit this floor was stamped at, leaving it ORPHANED-BASE or UNCHECKED -- which makes it unfalsifiable, the failure mode that let jsclasslen hide a 79-line regression earlier in this session. Re-stamped at a commit this branch holds.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRE-STAMP ONLY, NOT THIS BRANCH S NUMBER. 22,466 is what the merged tree reads and what this floor already said; only the provenance moved. The stamp cited a commit that no longer resolves after the rebase onto origin/19.0-marin, so ratchet.py --list reported UNCHECKED -- and an UNCHECKED floor cannot fail, which is how jsclasslen hid a 79-line HomeMenu regression earlier in this same session. Re-stamped at a commit this branch holds so it can fail again.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n22394 -> 22466 with the 2026-09-08 sync onto origin/19.0-marin, and the +72 is four classes in four named commits rather than an absorbed total.\n\nMeasured per class with py_class_length.py --top 0 in clean detached worktrees, pure origin against the replayed tip. Pure origin reads 22394, equal to the floor it already carried, so origin's 102 commits move this gate by nothing and the whole +72 is ours:\n\n  ResPartner                        1686 -> 1737  +51  cd8ffcf17c69, res.partner.bank back to a one2many and the contact naming its main phone, mobile and account\n  IrQweb                            1702 -> 1712  +10  a9a5feaea838, the parent self-bridge that blanked a dynamic child's module\n  BasePartnerMergeAutomaticWizard    699 ->  704   +5  cd8ffcf17c69, _merge_bank_accounts owning both branches now that the number is unique per company\n  IrModel                            433 ->  439   +6  9a52dc589433, the reflection window a mixin added to an installed model needed\n\n51 + 10 + 5 + 6 = 72 exactly, so nothing here is unattributed and no paydown elsewhere is paying for it. pyclasslen_addons went the other way over the same range -- 167550 to 167388, 306 of which is f978ed7f7eb5 lifting the encrypted store out of CredentialCredential into a mixin under budget -- and that headroom is deliberately NOT spent here: this floor is core and that one is addons, and section 9's worked example is about exactly the substitution that would be.\n\nNOT A SPLIT, AND THE SPLIT IS THE REAL WORK. ResPartner at 1737 lines is the largest single block and is 71% of this move on its own. Whoever takes it should read section 4's pyclasslen entry first: banking under an unrelated subject line is what destroys the record of what moved a no-increase floor, and this floor is exact-mode, so it is banked in a commit of its own with the four classes named.\n\nMeasured 22466 in a clean detached worktree of bd62aa022fbee2d19c5457f18028bebf4f695a82.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSYNC ONTO origin/19.0-marin, 22375 -> 22394, AND THE MOVE IS ATTRIBUTED FROM\nBOTH ENDS RATHER THAN INFERRED. Two readings, each on a clean detached\nworktree: pure origin at its tip c25df4eaa18 -- none of our two commits --\nreads 22457 against its own stamped floor of 22375, so origin's 61 commits\narrive +82 over their own floor and unbanked on that side. Our two commits\nremove 63 lines of class mass. 22457 - 63 = 22394, exactly what the rebased tip\nmeasures, so the arithmetic leaves no residue and nothing here is one\nimprovement paying for a regression elsewhere.\n\nA sync is what banks the other side's numbers; refusing would leave the gate\nunable to compare, which is worse than an honest floor with this note under it.\n54 of origin's 82 is e8f3e72d5a7, the cron deadlock fix, which was on this\nbranch before the sync and is now upstream's -- it added\n_raise_currently_executing and banked nothing. The remaining 28 arrives with\norigin's other 60 commits and is not analysed further here.\n\nOUR OWN -63 IS THE COMMENT/DOCSTRING STRIP OF odoo/odoo. Measured before the\nsync it was 22429 -> 22366 on the pre-rebase tree, where 22429 was itself\n54 over the then-floor for the reason above; bracketed there on a clean\nworktree, e8f3e72d5a7~1 read exactly the floor of 22375.\n\npyfunclen moved too and needed no bank: floor 0, tree 6 before the strip, 0\nafter, and it reads 0 on the rebased tip as well. That 6 predated the cron fix,\nso the strip returns that gate to its floor rather than under it.\n\nRe-stamped after the rebase; the previous stamp 27f145214ff is no longer in\nhistory, the mechanism CLAUDE.md section 4 describes, working. Measured with\ntooling/architecture/py_class_length.py --count.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REPAIRED AFTER THE SECOND SYNC OF 2026-09-07, NUMBER UNCHANGED. Five commits landed on origin/19.0-marin while this branch was being verified; replaying onto them rewrote every SHA again, so the stamps written minutes earlier were orphaned by the same mechanism, for the fourth and fifth time in two days. Re-measured on the replayed tip with the gate own runner and it reads what the floor already said, so origin five commits move this gate by nothing. Only the provenance moves.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSYNC ONTO origin/19.0-marin, 2026-09-07: 22271 -> 22375, AND THE MOVE IS UPSTREAM'S, MEASURED FROM BOTH ENDS RATHER THAN INFERRED. Three readings, each on a clean detached worktree: the merge-base 2fcf42df1fc reads 23098, pure origin at its tip 9021c4d2aba (none of our nine commits) reads 23203, and the rebased tip reads 22375. So origin's 166 commits carry +105 of class mass and our side carries -828; a sync is what banks the other side's numbers.\n\nTHE ARITHMETIC LEAVES NO RESIDUE, BUT ONLY AFTER ONE LINE IS ACCOUNTED FOR. Our branch measured -827 before the rebase and -828 after, and the difference is a single line of origin's: b6a2bccd9a8 added _check_inside_mirror(stub_path, stub_root) before the mkdir inside EsbuildCompiler._write_stubs, a class 438 lines over budget at that point, so it cost +1 excess there. Our split had already moved that function out to esbuild_stubs.write_stubs, which is module level, so the same line costs nothing here. The conflict resolution ported the guard rather than dropping it -- the refactored write_stubs guarded shim_path and not stub_path, so without the port the sync would have silently discarded a path-traversal fix while the gate said nothing.\n\nMeasured with tooling/architecture/py_class_length.py --count.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n22283 -> 22271: `collect_group_output` moved out of EsbuildCompiler.compile_group into esbuild_process (4be8846eb68), which shrank the class by a further 12 on top of the split in 9bc873f4bdc. That move was driven by pyfunclen rather than by this gate -- the split had pushed compile_group from 86 to 97 lines against a 90 budget -- and it is worth recording that the two gates pull in opposite directions: the same commit improved pyclasslen by 719 and regressed pyfunclen by 7. Measured on a clean tree at 4be8846eb68; test_assetsbundle plus the web ESM pipeline classes read 0 failed, 0 error(s) of 714 tests.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n22331 -> 22283, paying down the ESM/esbuild feature debt rather than banking it. The floor was 767 under the tree, and a per-class diff between this floor own commit (458147b0bbc, where it still reproduces at exactly 22331) and HEAD showed 736 of that is ONE feature across three classes: IrQweb in ir_qweb_assets.py +364, EsbuildCompiler +225, and a new IrQweb in ir_qweb_assets_esbuild.py +147.\n\nFive extractions, each along a seam the code already had -- esbuild_stubs (155), esbuild_process (154), ir_qweb_assets_esbuild_circuit (121), ir_qweb_assets_import_map (232), ir_qweb_assets_served_libs (65). EsbuildCompiler 838 -> 376; both IrQweb fragments now under the 400 budget.\n\nThe number lands 48 UNDER the old floor rather than exactly on it, and that is deliberate: the flag-computation group (~176 lines) would have been the cheap way to hit any target, and it stays in EsbuildCompiler because it reads essentially the whole constructor -- native_modules, javascripts, _standalone, _skip_legacy_test_imports, _import_map_included, _addon_flags_provider. A seam cut to satisfy a gate is not a seam.\n\nWorth carrying: the split surfaced a coupling nothing else would have. Methods that read a module-global (request, esm_registry) broke tests that patch BY MODULE, and the symptom is not an import error but a fake object that silently never gets written to. Three of thirteen methods touched request, so those three moved back and the fragment is provably request-free -- ruff reporting its request import unused IS the proof. All nine existing patch.object(ir_qweb_assets, \"request\") sites are untouched.\n\nVerified: test_assetsbundle 547 and the web ESM pipeline classes 167 both 0 failed 0 errors; the combined -i base,test_lint,test_orm,... run diffs EMPTY against the pre-refactor run on failure NAMES.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n22324 -> 22331: 02c57baa3a5 gave _get_asset_nodes a page= flag so collecting a bundle's cache URLs no longer counts as rendering its import map (IrQweb in ir_qweb_assets.py, +7); the fix is code, not comment, and was not banked with its commit. Measured on a clean worktree of 458147b0bbc.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nMeasured 22324 on a clean detached worktree of the 2026-09-05 rebase of 19.0-marin onto origin, attributed from both ends. Pure origin (its tip 0c7cffa9c2b) measures 22353 against its own stamp of 22400, so origin was 47 under its floor before our commits arrived; that 47 is origin's and is banked here because a sync is what banks the other side's numbers. Our branch adds exactly -29: PostgresBackend.search moved its shared query planning into _prepare_search_query (2f0e5320f2e, 584 -> 555 lines), the figure that commit banked as 22395 -> 22366 before the rebase. 22353 - 29 = 22324, no residue. The 22371 this file carried for one commit was the arithmetic combination written at conflict-resolution time, not a measurement.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRebase of 19.0-marin onto origin, 2026-09-05: two independent moves of this floor met in one conflict. Origin carried 22395 -> 22400 (+5); our branch carried 22395 -> 22366 (-29, next paragraph). Combined arithmetically to 22371 at conflict time; the sync's re-stamp commit measures it at the rebased tip.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nMeasured on clean aab72e2712d. Shared query planning moves from PostgresBackend.search into _prepare_search_query, used by both persistence adapters. PostgresBackend shrinks from 584 to 555 lines (184 to 155 excess); the aggregate moves from 22395 to 22366, entirely that 29-line reduction. InMemoryBackend remains below the class budget. This extraction gives the in-memory adapter PostgreSQL domain, join and ordering flush metadata; it is tested with deferred recursive computations on both adapters.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-stamped at 315c153efcd after the rebase of 19.0-marin onto origin; the previous stamp 6577ca2c7d9 is no longer in history. Measured in a detached worktree. History: 22395 -> 22400 at the S3/attachment commit (ir.attachment gains _zip_detached_reader), by odoo-d4.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n22395 -> 22400 at 6577ca2c7d9, measured in a detached worktree: ir.attachment gains _zip_detached_reader (the hook the Documents zip streamer calls for content the server does not hold, moved here from documents so cloud_storage can override it) and keeps a declared file_size when no content is written. No unit of the previous figure moved.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRe-stamped after rebasing onto origin/19.0-marin; the number is unchanged. The floor was banked hours earlier at a commit the replay rewrote away, so --list flagged it ORPHANED-BASE and refused to compare -- the mechanism CLAUDE.md section 4 describes, working. Re-measured at fb7fe6f33bf in a clean detached worktree: pyclasslen reads 22395 there, exactly what the orphaned floor said, so origin's three commits (a5079a4ca50, e5b050c5d54, 532091ea401) move this gate by nothing.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nThe esbuild subject left IrQweb. ir_qweb_assets.py was 2009 lines and its IrQweb carried 1925 over the 400-line budget; the circuit breaker, the advisory lock, the compile path and the relative-import check that raises their error moved to ir_qweb_assets_esbuild.py as a second _inherit = \"ir.qweb\" extension, the shape ir_ui_view_base.py already uses. IrQweb 1925 -> 1665, and the new class is under budget, so the gate goes 22655 -> 22395. Member for member the composite model is unchanged on purpose: the ESM suites call _acquire_esbuild_lock and _open_esbuild_circuit directly and patch three of them, so module functions would have rewritten 25 call sites to buy the same lines. THE RESIDUE IS NOT THAT COMMITS. Between the previous stamp f654fe0d29f and 9ed4c8bc454 the gate rose 275 over 24 classes, all from the base audit and all one author -- IrModelFieldsSelection +41, MixinMerge +34, IrActionsServer +30, IrModelFields +30, ResPartner +27, configmanager +22, IrActionsActions +20, IrSequence +14, IrActionsReport +13, offset by IrUiView -64, IrAttachment -22, IrModelData -17, ResUsers -15 and IrAsset -8; the per-class deltas sum to exactly 275 with no residue. The split pays back 241 of it, plus 2 the traversal type repair cost, leaving this floor 17 above the old one rather than 275. Measured on a clean detached worktree of 34ca3a03c44.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nMOVED BY THE 2026-09-04 SYNC, NOT BY THIS BRANCH, AND ATTRIBUTED RATHER THAN ASSUMED. Measured at the rebase's onto-tip a1ba7203ac3 -- pure origin, none of our 57 commits -- it reads 22378, the same as it reads at f654fe0d29f. So origin's 139 commits carry the whole move and our side added nothing to it. A sync is what banks upstream's numbers; refusing to would leave the gate unable to compare, which is worse than an honest floor with this note under it. Re-measured in a clean detached worktree, and the invocation confirmed against lint_sql_injection, which is not orphaned and reads its stamped floor of 9. The +101 is class mass arriving with origin; nothing in the core package grew on our side.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nMOVED DOWN BY 32, RE-STAMPED AT 16691613dd8, AND THE ARITHMETIC IS WRITTEN OUT BECAUSE A WHOLE-TREE SUM HIDES ITS OWN COMPOSITION. Re-measured in a clean detached worktree; this checkout cannot answer, because tooling/.venv-gates is gitignored and a tree-walking gate reads it.\n\nThe floor of 22309 was banked at 1106c554f3f and was exact there. Bracketed forward from it: 4d44c24f358 took it to 22321, 605428f31cc gave 9 of that back to 22312, and it sat 3 above the floor from then until tonight. So 3 of the 32 this commit banks is refactor churn nobody banked deliberately, not a repair -- it is named here rather than absorbed silently, and 4d44c24f358 is where to look if it ever matters.\n\nThe 35 that carries the rest is ResPartner, 1678 to 1641. Verified per class over HEAD~60..HEAD that NOTHING in the core package grew across that window, so this decrease is not one improvement paying for a regression elsewhere. That check is the reason this floor is banked and pyclasslen_addons is not: over there the same question has a known answer, an 800-line comment strip in addons/resource masking a +100 HrEmployee regression its owner has not settled.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nO6 moved ten per-type domain optimisations out of orm/domain/optimizations.py onto the field class that owns the type, behind Field._optimize_condition. Properties keeps a nine-line hook and is already 372 lines over budget, so it is charged for all nine (+9); Many2one and the moved bodies are module-level functions and stay under 400. O4 took Field and CreateMixin down 6 between them. 22306 -> 22309."
}
