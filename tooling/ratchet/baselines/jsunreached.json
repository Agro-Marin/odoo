{
  "count": 62,
  "measured_at": "600e97b75fa01d0acffa3f4f0bde58371c760e5a",
  "measured_root": "",
  "note": "STAMP REFRESHED AFTER THE 2026-09-08 SYNC, NUMBER UNCHANGED. 19.0-marin was replayed onto origin/19.0-marin (10 commits, 2 conflicts resolved), so every SHA changed and this stamp was orphaned by the replay -- the flag working, not debt appearing. Re-derived at the landed tip 600e97b75fa in a CLEAN detached worktree, not the shared checkout, which carried 42 uncommitted paths of a peer session at the time; the gate reads exactly what the floor already said, so no unit of this figure was ever phantom and nothing here is attributable to the sync. The gate resolves its own ROOT from __file__ over odoo/ and addons/ and loads no sibling repo, so the reading is sibling-proof and the odoo-only worktree is a sound scope for it. node_modules was symlinked and esbuild verified executable before measuring -- without it this gate exits on a bare Node banner with no count, which reads as a broken gate rather than a missing dependency.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nSTAMP REFRESHED AFTER THE 2026-09-07 SYNC, NUMBER UNCHANGED. 19.0-marin was replayed onto origin (192 upstream commits, 60 of ours), so every pre-rebase SHA was rewritten and this floor pointed at 1223bbff922, which the branch no longer holds -- ratchet.py --list flagged it ORPHANED-BASE. Re-measured with js_unreached_assertions.py --count in a clean detached worktree of the tip being banked and it reads 62, exactly what the floor already said, so the replay moves this gate by nothing and only the provenance moves. Measured in a worktree rather than the shared checkout because a peer session was holding uncommitted work elsewhere in the workspace at the time; the worktree needs node_modules symlinked from the shared checkout or the .mjs runner exits with a bare Node banner and no count, which reads as a broken gate rather than a missing dependency.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\n66 -> 62: two tests in core/utils/indexed_db.test.js gained expect.assertions guards, four sites in one commit (1223bbff922). Both assert inside the callback handed to IndexedDB.execute(), so a callback that stopped being invoked would take the store-names assertion with it while the read assertions outside stayed green. Counts are 6 and 8; the first attempt wrote 4 and 6 because ensureDbIsAbsent() asserts twice on its own, and Hoot reported the true number rather than leaving them counted by eye. Measured in a clean detached worktree of HEAD with node_modules symlinked; the shared checkout carries another session's orphaned comment sweep and is not usable for this reading.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nNew gate, first floor. 66 masked assertion sites in addons/web/static/tests: an assertion inside a deferred function (a handler, a callback, a class method) that runs only if something invokes it, in a test that ALSO asserts in its own straight-line flow. That second condition is the gate -- Hoot already fails a test running no assertion at all, so an unmasked dead handler is caught today and reporting it would be debt the tree does not have; without the masking condition the count is 112. Guards recognised and not reported: expect.assertions(n)/verifySteps, a sentinel the function sets and the test asserts outside it, and the asserted result of the call the function was handed to. Validated against the defect it was built for: reports plop() on t_custom_click.test.js at 9e26c220cdf~1, silent on the fixed file. The class-method form -- the one 800c9912f02's defect hid behind -- is at zero; the 66 are inline callbacks, a smaller risk each and a test-by-test judgement to drive down, not a sweep. Measured in a clean detached worktree of the commit that adds the gate, node_modules symlinked."
}
