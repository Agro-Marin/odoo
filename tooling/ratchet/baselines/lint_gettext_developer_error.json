{
  "count": 37,
  "note": "account_move_mail._routing_check_route wrapped a ValueError message in _(). A ValueError reaches a reader as a traceback, never as UI, so the translation is dead weight; it is an f-string now.\n\n38 -> 37. Measured HEAD 42 -> 41 in a detached worktree carrying only this change; the floor was already at 38 from another session, so this banks that 38 minus the one counted here.\n\nCommand: odoo-bin -d <db> -i test_lint --test-enable --test-tags /test_lint"
}
