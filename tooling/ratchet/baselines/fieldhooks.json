{
  "count": 225,
  "measured_at": "35a01c2a87f608aa51098b8600da02d16292fa90",
  "measured_root": "",
  "note": "field_hook_naming.py gained a selection= branch, closing the gap §2.4.2 records as \"enforced by nothing\": selection= is a sixth field-declaration keyword ATTRS stops short of, and the Selection row asserts the OPPOSITE of every other rule in the file -- a selection hook is named for its VALUES, so one method serves fields of several names (_selection_target_model is pointed at by resource_ref, parent_ref and preview_record_ref). Through ATTRS the rule would have demanded a rename of exactly those three, so selection is a scope of its own whose whole assertion is the _selection_ prefix, and it resolves the forwarding lambda as well as the string because §2.4.2 says the lambda is where the binding stops being visible. 206 -> 225 is three movements and only two are this change. (1) The parent commit 7e36f8314f5 already measures 205, not 206, and the -1 is UNATTRIBUTED: odoo-29 measured this gate at 8e705b6ea44~1, 8e705b6ea44, 13a873d2a07, 689d0843e1d9, 042da509ff6 and 0b36645d63c, one detached checkout each, and every one reads 205 -- so none of that day's vocabulary commits moved it. The drop is somewhere in the 1418 commits between this floor's previous stamp 8f4a84e8151 and 8e705b6ea44~1; the 30 oldest of that range read 206. (2) The new branch adds 22, of which 20 are under addons/ and 2 under odoo/odoo/addons/base. (3) Both of those 2 are fixed in the same commit -- ir.model.fields _field_types -> _selection_field_types and res.lang _get_date_format_selection -> _selection_date_formats, each a module-level function passed by REFERENCE rather than by name, which is why no scan resolving only selection=\"_x\" had ever read either, and why §2.4.2's frozen figure is the smaller half of the residue. That leaves 20 as debt: 8 are l10n, and the commonest spelling is the read verb (_get_year_selection, _get_task_states) or no verb at all (_l10n_bg_document_type_selection_values). Measured at 35a01c2a87f in a detached worktree, parent measured at 7e36f8314f5 the same way, because the shared checkout was carrying five sessions' uncommitted work. General finding worth more than this number: ratchet.py --list resolves a stamp and never re-measures a count, so an un-orphaned floor can be silently stale in the LOWERING direction indefinitely -- nothing routine runs this gate, and nobody has checked the other 80.\n\nPREVIOUS NOTE, kept because a floor that loses its history costs more than the stamp gains:\n\nRebase resolution set this to the replayed side (244); origin had 206 and the merged tree measures 206. field_hook_naming.py --count on a clean tree at this commit reads 206."
}
