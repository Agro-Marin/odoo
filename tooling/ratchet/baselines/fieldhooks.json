{
  "count": 273,
  "note": "base_install_request._compute_modules_description served BOTH module_ids and modules_description while named for one -- the gate reports that as `misleading`.\n\nSplit into _compute_module_ids (depends module_id) and _compute_modules_description (depends module_ids). The split surfaces a dependency the merged hook hid: the description is a RENDERING of the app set, so it should recompute when that set changes, not when the root module does.\n\nTHIS GATE WAS RED AT HEAD, +1 THE RISING WAY. Committed 274, tree measured 274 -- but at the prior HEAD it measured 284 against a committed 283. Re-measured here in a detached worktree at HEAD with only this change applied.\n\n  274 -> 273"
}
